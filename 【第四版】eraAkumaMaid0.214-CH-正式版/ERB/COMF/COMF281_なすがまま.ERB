@COM281
;顺其自然
;ぶっちゃけ、絶対に脱出しない”挣扎”です
IF (STATE("恍惚", PLAYER) && FLAG:恍惚状態 == 1) || STATE("脫力", PLAYER)
	CALL SHOW_STATUS
	FLAG:COM_ABLEの一括OFF = 0
	SIF PREVCOM >= 0
		CALL SHOW_PREVCOM
ELSE
	PRINTL 顺其自然
ENDIF

STR:前回指令 = 顺其自然
CALL KOJO_MESSAGE_COM
PRINTL 

SIF REFUSE_CHECK()
	RETURN 0

TFLAG:カウンター中 = 1

;FIRSTCOMフラグON
CALL SET_CHOSECOM, TARGET, TNUM("顺其自然")

CALL SET_COUNTER_ABL

SIF TEQUIP:Ｗ推倒
	CALL SET_SELECTCOM_MULTI, "Ｗ推倒"

TRYJUMPFORM COM{SELECTCOM}

RETURN 1
