@COM267
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%

RETURN 1

@FLAG_COM267, ARG

@SOURCE_COM267, ARG
CALL VARSET_COMF

SOURCE:0:恐惧 = 2000
SOURCE:0:逸脱 = 1000

CALL CALC, "調教ソース", ARG


@TRAIN_MESSAGE_COM267
PRINTFORML 要對%CALLNAME:TARGET%的哪裡使用%TEXTR("筋弛緩劑/肌肉鬆弛劑")%？
PRINTFORML (注意：因為属於烈性薬、所以短時間内只能対一処用薬)
PRINTFORML  [ 0] 肛門
SIF TALENT:男性 == 0
	PRINTFORML  [ 1] 尿道周辺
SIF TALENT:男性 == 0
	PRINTFORML  [ 2] 阴部
SIF TALENT:男性 == 0 && TALENT:処女 == 0 && TALENT:妊娠 == 0
	PRINTFORML  [ 3] 子宮口
PRINTFORML [100] 返回

IF TALENT:男性 == 0 && TALENT:処女 == 0 && TALENT:妊娠 == 0
	CALL INPUT_SELECT, 4, 100
ELSEIF TALENT:男性 == 0
	CALL INPUT_SELECT, 3, 100
ELSE
	CALL INPUT_SELECT, 1, 100
ENDIF

SELECTCASE RESULT
CASE 100
	TFLAG:指令中止 = 1
	RETURN 0
CASEELSE
	TFLAG:地の文分岐 = RESULT
	SETBIT TEQUIP:筋弛緩剤, RESULT

	SELECTCASE RESULT
	CASE 0
		PRINTFORMW 在嚴格的按照計量使用%TEXTR("筋弛緩劑/肌肉鬆弛劑")%後、%CALLNAME:TARGET%的肛門週邊的肌肉漸漸鬆弛了。
		IF TALENT:薬毒耐性
			ABL:Ａ拡張 += 1
			PRINTFORMW Ａ拡張レベル＋１
		ELSE
			ABL:Ａ拡張 += 2
			PRINTFORMW Ａ拡張レベル＋２
		ENDIF
		CALL GET_EXABL_A, TARGET
		ABL:Ａ名器度 = ABL_A(TARGET)
	CASE 1
		PRINTFORMW 在嚴格的按照計量使用%TEXTR("筋弛緩劑/肌肉鬆弛劑")%後、%CALLNAME:TARGET%的尿道括約筋漸漸鬆弛了。
	CASE 2
		PRINTFORMW 在嚴格的按照計量使用%TEXTR("筋弛緩劑/肌肉鬆弛劑")%後、%CALLNAME:TARGET%的膣道漸漸鬆弛了。
		IF TALENT:薬毒耐性
			ABL:Ｖ拡張 += 1
			PRINTFORMW Ｖ拡張レベル＋１
		ELSE
			ABL:Ｖ拡張 += 2
			PRINTFORMW Ｖ拡張レベル＋２
		ENDIF
		CALL GET_EXABL_V, TARGET
		ABL:Ｖ名器度 = ABL_V(TARGET)
	CASE 3
		PRINTFORMW 在嚴格的按照計量使用%TEXTR("筋弛緩劑/肌肉鬆弛劑")%後、%CALLNAME:TARGET%的子宮口漸漸變得柔軟了。
	ENDSELECT
ENDSELECT

