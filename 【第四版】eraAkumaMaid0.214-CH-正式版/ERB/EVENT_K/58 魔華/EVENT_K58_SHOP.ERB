;-------------------------------------------------
;交易画面と、购物の際の台詞を記述します
;-------------------------------------------------
;交易画面での台詞。悪魔以外は大部分が不要です
;-------------------------------------------------
@KOJO_WISH_K58, ARGS
#DIM MEMO_LINECOUNT

MEMO_LINECOUNT = LINECOUNT

SELECTCASE ARGS
CASE "交易開始"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の交易")
		;しかも挿入中！
		IF INSERT("挿入相手") == MASTER

		ELSE

		ENDIF
	ELSE
		PRINTFORMW 「好的、可以哦、在%CNAME("一人称")%能做到的范围里的话」
	ENDIF
CASE "愛の告白"
	;告白成功！
	IF TALENT:相思相愛
		PRINTFORMW 「・・・我明白了」
		PRINTFORMW 「但是、真的可以嘛？在其他人面前也会表现出lovelove的样子哦？」
		PRINTFORMW 「有了年长的男朋友的话、一般都会引起其他人的注意吧？」
		PRINTFORMW 「不过、对于%CNAME("二人称")%来说、又是怎么样的呢？」
		PRINTFORMW 「唔、青春期的表现欲吗？」
		PRINTFORMW 「像这样表现出非处女的样子、会不会影响到在班里的人气呢？」
		PRINTFORMW 「说不定那群男生全都会幻灭、然后人气一落千丈呢」
		PRINTFORMW 「嗯...不过、说不定在那群妄想着%CNAME("一人称")%被年长男性这样那样的群体里、会意外的人气大提升呢」
		PRINTFORMW 「那么、从现在開始。%CNAME("一人称")%就是%CNAME("二人称")%专用的了」
	;失敗
	ELSE
		PRINTFORMW 「不...这个...怎么说呢...有些为难呢」
	ENDIF
;胖次のお願いした時に相手が没穿内褲だった
CASE "没穿内褲"
	IF COND("合意：胖次交易")
		PRINTFORMW 「真可惜、%CNAME("二人称")%居然那么喜欢胖次、但是今天没有穿哦？」
		PRINTFORMW 「虽然没有胖次能让%CNAME("二人称")%拿来自慰・・・但是在别的地方也可以让%CNAME("二人称")%%CNAME("射精音")%的射精哦？」
	ELSE
		PRINTFORMW 「不行呢・・・因为没有穿哦、请自行想象一下风吹过裙子里的感觉吧」
	ENDIF
;胖次を要前の会話。
;ただし、悪魔の場合には魂を払っているので確実に渡すことになる
;ここで本来胖次を给かどうかなどを加味して分岐を設ける事も出来る。特に書かなくてもよい
;TFLAG:取引承諾 = 0とすると胖次拒否に行くようになり、TFLAG:取引承諾 = 1とすると判定無視で胖次を渡せるようになる
;TFLAG:取引価格 に数字を入れる事で好きな額を要求できるようになる。基準相場は$500です
;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで胖次を渡す展開に出来る
;TFLAG:取引拒否 = 1 とすると本来交易できるかに関わらず、交易しなくなる
CASE "胖次交易"
	;ＯＫ！
	IF TFLAG:取引承諾
		;日期と時間参照随机
		IF COND("合意：胖次交易") && RANDC(4) == 0
			PRINTFORMW 
			TFLAG:取引価格 = 1000
		ELSE
			PRINTFORMW
		ENDIF
	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		TFLAG:値上げ交渉 = 500
	ENDIF
CASE "胖次"
	;初回
	IF DEVENT(ARGS) == 0
		PRINTFORML 「对%CNAME("二人称")%来说胖次果然很重要呢・・・我明白了」
		PRINTFORML 「%CNAME("一人称")%其实有经历换衣服的时候被偷走了两次胖次呢...」
		PRINTFORML 「那个胖次，估计被凶手弄得满是都是精液吧…………」
		PRINTFORMW 「啊、%CNAME("二人称")%的话、不用去偷哦？只要和我说一句「给我」、%CNAME("一人称")%会很乐意给你的」
	;二回目以降
	ELSE
		IF CHARA_NUM("室内の邪魔者")
			PRINTFORML 「不要因为没有其他人就这么大胆啊、%CNAME("二人称")%…」
			PRINTFORML 「唔姆、怎么说呢、这里的大家也是%CNAME("一人称")%的伙伴呢」
			PRINTFORMW 「虽然在无関人员面前有点抵触，但在这里没问题」
		ELSE
			PRINTFORML 「可以哦?用来做什么?一边闻一边用手自慰吗?？」
			PRINTFORMW 「果然男生们都想要胖次拿去做色色的事呢、但是只有%CNAME("二人称")%是特别的哦」
		ENDIF
	ENDIF
	;胖次を脱いで渡してくる汎用地の文
	CALL DAILY_MESSAGE, "脱下胖次交了出去"

CASE "胖次拒否"
	;交易を拒否した際
	IF TFLAG:取引拒否

	ELSE

	ENDIF

;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで服を脱掉展開に出来る
;TFLAG:取引拒否 = 1 とすると本来交易できるかに関わらず、交易しなくなる
CASE "全裸交易"
	;ＯＫ！
	IF TFLAG:取引承諾

	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		;TFLAG:値上げ交渉 = 2000
	ENDIF
;ここ玛丽桑の真似
CASE "全裸"
	IF SAVESTR:現在位置 == "公園"
		;汎用地の文
		CALL EVENT_DATUI, "裸体"
		PRINTFORML 「没有人能想到会有初中部的女孩子会裸体的站在这里吧・・・」
		PRINTFORML 「如果被谁看到的话...一定会饥渴难耐的冲过来强奸我吧、让GinGin勃起的欧金金把%CNAME("一人称")%弄的乱七八糟呢」
		PRINTFORMW 「如果真的有人想要强奸我的话・・・%CNAME("二人称")%？可要保护好我哦？」
	ELSEIF CHARA_NUM("室内の邪魔者") == 0
		PRINTFORMW 「哪怕真是的飞机杯、也不会在别人面前随便開封吧、%CNAME("二人称")%真是鬼畜呢、不过、%CNAME("一人称")%多少也有点想尝试一下呢、脱了哟？」
		;汎用地の文
		CALL EVENT_DATUI, "裸体"
	ELSE
		PRINTFORML 「%CNAME("二人称")%、完全是一副把飞机杯从盒子里拿出来的感觉呢...」
		PRINTFORMW 「嗨嗨、どうぞ（douzo）、男孩子们妄想的身体、看个够吧」
		;汎用地の文
		CALL EVENT_DATUI, "裸体"
	ENDIF
CASE "全裸拒否"
	;交易を拒否した際("全裸交易"で設定した場合のみ)
	IF TFLAG:取引拒否

	;二人きりなら脱いでくれるけど状況が悪い
	ELSEIF COND("合意：全裸化") && (CHARA_NUM("室内の邪魔者") || PLACE("個室") == 0)

	ELSE

	ENDIF

;TFLAG:取引拒否 = 1 とすると本来交易できるかに関わらず、交易しなくなる
CASE "事后避孕药交易"

CASE "事后避孕药"
	;代金をまけてくれた場合
	IF TFLAG:値切った
		;所持金が足りなかった場合です
		;現状、ここに来るのは恋慕无の時のみ
	;拝み倒したら代金を払わなくても許してくれた場合
	ELSEIF TFLAG:取引価格 == 0
		;現状、ここに来るのは恋慕有の時のみ
	ELSE
		;普通にお金を払った場合
		;口上无でも自然な感じです
		;ですが恋慕のときなどは、なにか一言あってもいいかもですね
	ENDIF

CASE "事后避孕药拒否"
	;これはCASE "事后避孕药交易"内でTFLAG:取引拒否 = 1 となる分岐を作った場合のみ
	IF TFLAG:取引拒否

	;拝み倒して失敗した場合
	ELSEIF TFLAG:取引価格 == 0
		;轻轻地地の文が表示されます。口上有无どちらでも大丈夫そうです。
	;其他、你が薬代を不支付場合
	ELSE
		;恋慕状態で、それなりに妊娠の恐惧がある場合
		IF ( COND("受精確認") || COND("胎内の精液量") ) && TALENT:恋慕
			;お金がないか、不支付を選んだ場合です。
			;それなら避妊できなくて妊娠しちゃっても仕方ないね…(爱心)って感じのやつです。
			;地の文が表示されるので何も書かないほうが良いかもしれません。
		;不支付、を選んだ場合
		ELSEIF MONEY >= TFLAG:取引価格
			;口上无でも自然な感じです
		;お金が足りなかった場合
		ELSE
			;不支付を選んだ場合も、お金が足りないときはここへ来ます。
		ENDIF
	ENDIF
CASE "同居拒否"
	PRINTFORMW 「%CNAME("二人称")%希望我住在你家里？这么理解没错吧？」
	PRINTFORMW 「把中学生関在家里当成飞机杯来使用的性犯罪者的妄想是很难实现的哦？」
CASE "同居希望"
	CALL PRINT_STRW, @"「%CNAME("二人称")%希望我住在你家里？这么理解没错吧？_H_」"
	CALL PRINT_STRW, @"「可以哦、在%CNAME("二人称")%喜欢的时候随时都可以使用我这个飞机杯_H_」"
	CALL PRINT_STRW, @"「想自慰的时候随时都可以拿出来呢_H_」"
	CALL PRINT_STRW, @"「中学生的肉体、在%CNAME("二人称")%家里要被随意使用了呢_HH_」"
CASE "同居頻度的相談"

CASE "便当拒否"

CASE "做便当"
	;便当のお願い２回目以降
	IF COND("合意：便当")

	;以下は初めて作ってもらうよう变成了とき
	ELSE
		IF COND("同居中")

		ELSE

		ENDIF
	ENDIF

;COND("口服避孕薬派")かどうかで初回が"口服避孕薬を飲んで欲しい"or"口服避孕薬を止めて欲しい"に分かれます
CASE "口服避孕薬"
	;角色が口服避孕薬派なら、こっちだけ書いてください
	IF COND("口服避孕薬派")
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬

		;口服避孕薬の服用を止めてもらう
		ELSE
			;初回
			IF DEVENT(ARGS) == 0

			ELSE

			ENDIF
		ENDIF
	;角色が口服避孕薬派でないなら、こっちだけ書いてください
	ELSE
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬
			PRINTFORMW 「…也就是说、%CNAME("二人称")%想要在中学生的子宫里随意射精呢、真是鬼畜坏心眼」
			PRINTFORMW 「看样子是不打算在中学生飞机杯上使用避孕套呢？」
			PRINTFORMW 「真是任性的欧金金呐、还是说%CNAME("二人称")%其实只靠性欲做事吗？」
			;屈服刻印=3
			IF MARK:屈服刻印 == 3
				PRINTFORMW 「可以哦、好不容易用上了中学生的欧金金专用小洞、如果不能好好的感受一下精液的话、我觉得挺可惜的」
			ELSE
				PRINTFORMW 「唔姆、好吧、要被%CNAME("二人称")%用欧金金在子宫里随意中出了」
			ENDIF
		;口服避孕薬の服用を止めてもらう
		ELSE
			PRINTFORMW 「…中学生的肉体可是已经能够生育了哦？」
			PRINTFORMW 「在作为「飞机杯」使用的时候要好好的戴上避孕套哦？」
		ENDIF
	ENDIF
;COND("口服避孕薬派")の人に口服避孕薬服用を止めてもらうお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬中断拒否"

;COND("讨厌口服避孕薬")の人に口服避孕薬服用のお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬服用拒否"

CASE "淫紋拒否"

;"淫紋贴纸直前"or"淫紋贴纸"の片方を書けば十分だと思います
CASE "淫紋贴纸直前"
	;初回
	IF DEVENT(ARGS) == 0

	ELSE

	ENDIF

CASE "淫紋贴纸"
	;台詞を書いたら下のRETURN 0を消してください
	RETURN 0
	;初回
	IF DEVENT(ARGS) == 0

	ELSE

	ENDIF
;"淫紋直前"or"淫紋"の片方を書けば十分だと思います
CASE "淫紋直前"
	SELECTCASE MARK:淫紋
	CASE 2
		PRINTFORML 「要在这里做记号吗？像是淫纹之类的」
		PRINTFORML 「每一个女孩子都有、让欧金金觉得舒服的地方」
		PRINTFORMW 「没関系、就在这个中学生小穴上做记号吧」
	CASE 3

	CASE 4

	ENDSELECT
CASE "淫紋"
	;台詞を書いたら下のRETURN 0を消してください
	SELECTCASE MARK:淫紋
	CASE 2
		PRINTFORML 「・・・总觉得、有点、不、是觉得相当色情」
		PRINTFORML 「被男孩子摸的时候会有这种感觉吗？」
		PRINTFORMW 「%CNAME("二人称")%专用的感觉・・・有点奇怪呢」
	CASE 3
		PRINTFORMW 「好像・・・被%CNAME("二人称")%改造成专用的身体了？是真的想要把%CNAME("一人称")%改造成专用飞机杯？」
	CASE 4
		PRINTFORMW 「%CNAME("二人称")%専用飛機杯、只对%CNAME("二人称")%一个人榨精的小洞・・・」
	ENDSELECT

CASE "拜托処理性欲"
	PRINTFORML 「原来如此、%CNAME("二人称")%真的是随时随地都在发情呢、真是鬼畜」
	PRINTFORML 「为了让%CNAME("二人称")%的精液能够舒舒服的射出来」
	PRINTFORML 「中等部的%CNAME("一人称")%要特意跑到高等部、在大家「哇、好色情的后辈」这样想的时候」
	PRINTFORML 「成为在休息时间能够随意处理性欲的性奴力、是吧？」
	PRINTFORML 「听好了哦、我们班的男生也是在用%CNAME("一人称")%的身体进行幻想哦？」
	PRINTFORML 「基本上就是这样。反过来说、%CNAME("二人称")%想要在%CNAME("一人称")%的服务下射精对吧？真的是完全不在乎其他男生的感受呢」
	PRINTFORML 「是这样没错吧？呋呋、多么任性的%CNAME("二人称")%啊・・・」
	PRINTFORMW 「不、我会做的、毕竟是%CNAME("二人称")%的飞机杯嘛、谁让%CNAME("一人称")%是沐浴完%CNAME("二人称")%精液浴后就会返回初中部的笨女人呢」
CASE "性処理の一時中断"

CASE "性処理拒否"

;悪魔、淫魔、妖狐専用
CASE "给我看尾巴"

CASE "给我看尾巴拒否"

;改变髪型のは、長さが披肩以上の人のみ。未設定だと披肩扱いです
;髪型を弄る際に手伝ってくれる人がASSIになっています。お手伝い桑を指名したい場合にはその人をASSIにして下さい
CASE "改变髪型"

CASE "髪型変更の感想"
	;どの髪型变成了かで分岐可能
	SELECTCASE CONDS("髪型の種類")
	CASE CONDS("普段の髪型")
		PRINTFORMW 
	CASE "齐刘海"
		PRINTFORMW 
	CASE "直髪"
		PRINTFORMW 
	CASE "卷髪"
		PRINTFORMW 
	CASE "外八"
		PRINTFORMW 
	CASE "双马尾"
		PRINTFORMW 
	CASE "马尾辫"
		PRINTFORMW 
	CASE "三股辫"
		PRINTFORMW 
	CASE "编起来"
		PRINTFORMW 
	CASE "団子頭"
		PRINTFORMW 
	;其他
	CASEELSE
		PRINTFORMW 
	ENDSELECT
CASE "改变髪型拒否"

;これは普段から连褲襪を履く人じゃないとあんまり見る機会ないと思う
CASE "连褲襪着替え"
	;台詞を書いたら下のRETURN 1を消してください
	RETURN 1
	;どれに履き替えてもらったか
	IF CHECK_BOTTOM("開放型连褲襪")
		PRINTFORMW 
	ELSEIF CHECK_BOTTOM("渔网连褲襪")
		PRINTFORMW 
	ELSEIF CHECK_BOTTOM("连褲襪")
		PRINTFORMW 
	;生足
	ELSE
		PRINTFORMW 
	ENDIF
CASE "连褲襪着替え拒否"

CASE "巨根化して"
	;これは"大きくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸")
	CASE "短小"

	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並以上
	CASEELSE

	ENDSELECT

CASE "巨根化して拒否"

CASE "短小化して"
	;これは"小さくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸")
	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並、規格外
	CASEELSE

	ENDSELECT

CASE "短小化して拒否"

;恋慕のみ　避妊の護符を取下
CASE "避妊解除可"

CASE "避妊解除拒否"

;一度解除した避妊の護符の再装着をお願いした場合
CASE "避妊の護符"

;以下は悪魔専用
CASE "魂が足りない"

CASE "想要钱"

CASE "甜言蜜语"

CASE "謎之魅力"

CASE "射精量強化"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の交易")
		;しかもＶ挿入中！
		IF INSERT("Ｖ挿入相手") == MASTER

		ELSE

		ENDIF
	ELSE

	ENDIF
CASE "巨根化"
	;これは"大きくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸", PLAYER)
	CASE "短小"

	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並以上
	CASEELSE

	ENDSELECT
CASE "短小化"
	;これは"小さくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸", PLAYER)
	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並、規格外
	CASEELSE

	ENDSELECT
ENDSELECT

;以下は汎用台詞(すでに喋っている場合には下には行かない)
SIF LINECOUNT > MEMO_LINECOUNT
	RETURN 1

;汎用拒否
IF STRCOUNT(ARGS, "拒否")
	PRINTFORMW 「そのお願いは聞けませんね・・・流石に冗談かと思いました。」
;汎用交易（特に記述することがないならスキップ）
ELSEIF STRCOUNT(ARGS, "交易")

;汎用ＯＫ
ELSE
	PRINTFORMW 「とことん都合のいい後輩ですね、我ながら・・・いいですよ。承りましょう。」
ENDIF

;-------------------------------------------------
;购物での台詞
;-------------------------------------------------
@KOJO_SHOP_K58, ARGS

SELECTCASE ARGS
CASE "一緒に出発"
	;初回
	IF SEVENT(ARGS) == 0
		PRINTFORMW 「嗯、倒也无所谓、是哪家店呢?」
	;着替えてもらう
	ELSEIF CHECK_CLO("裸体") || CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿") || CHECK_CLO("内衣姿")
		PRINTFORMW 「对%CNAME("一人称")%来说穿成这样出門会很碍事、所以请等我换一下衣服」
	ELSE
		PRINTFORML 「是经常去的店吧?好啊、走吧」
	ENDIF
CASE "目送"
	PRINTFORMW 「慢走哦、%CNAME("二人称")%」
CASE "没収胖次出発"
	;初回
	IF SEVENT(ARGS) == 0
		PRINTFORML 「%CNAME("一人称")%到无所谓」
		PRINTFORMW 「就算被人盯着看、隔着裙子也看不出来吧」
	;既に没穿内褲
	ELSEIF EQUIP:胖次 == 0
		PRINTFORML 「其实、%CNAME("一人称")%提前猜到了%CNAME("二人称")%的想法、已经不穿了呢」
		PRINTFORMW 「%CNAME("二人称")%小心思、%CNAME("一人称")%知道的一清二楚哦」
	ENDIF
CASE "保持现在的装扮出発"
	IF CHECK_CLO("泳装姿")
		PRINTFORML 「像这样堂堂正正的穿着泳装的话、人家也只会觉得「是在拍摄什么吗？」・・・」
		PRINTFORMW 「直接过去的话好像比较困难呢、呋呋、路上的男人们会不会因为%CNAME("一人称")%身姿而勃起呢？」
	ELSEIF CHECK_CLO("体操服姿")
		PRINTFORML 「体操服的话、大人们好像觉得很色情呢」
		PRINTFORML 「网上好像流传着一个很火的话题呢、像是『初中部的女孩穿着紧身体操服的样子真是太色情了』这样一类的」
		PRINTFORML 「不过很正常吧、毕竟体操服能很明显的暴露身体曲线」
		PRINTFORMW 「%CNAME("一人称")%也可能被偷拍成为那些大人们的自慰对象哦？」
	ELSEIF CHECK_CLO("内衣姿") || CHECK_CLO("裸体")
		PRINTFORML 「多少会因为%CNAME("二人称")%各种不可思议的想法而紧张呢・・・」
		PRINTFORML 「简直就像是在拍色情作品」
		PRINTFORMW 「还是说，真的打算作为影像作品卖给各种各样的人？」
	ENDIF
CASE "会計交代"
	;初回
	IF SEVENT(ARGS) == 0 || SEVENT("商品購入") == 0
		PRINTFORMW 「我明白了、我去找一下店员。用露出胸部的姿势比较好吗？」
	ELSE
		PRINTFORMW 「我明白了、我去找一下店员」
		IF CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿")
			PRINTFORMW 「能明显的感觉到店员桑一直盯着%CNAME("一人称")%看呢、没准偷偷勃起了？」
		ELSEIF CHECK_CLO("内衣姿") || CHECK_CLO("裸体")
			PRINTFORMW 「虽然说%CNAME("一人称")%有魔法保护、但是一想到会被除%CNAME("二人称")%以外的人看到身体就很紧张呢」
		ELSE
			PRINTFORMW 「顺便问一下、需要袋子吗？」
		ENDIF
	ENDIF
CASE "商品購入"
	;今買った物
	SELECTCASE STR:購入道具
	;ギリギリ通常商品の範疇or你用
	CASE "避孕套"
		PRINTFORML 「%CNAME("一人称")%向店员桑说了呢、%CNAME("一人称")%会用自己的%CNAME("膣")%让男人%CNAME("射精音")%的射出来」
		PRINTFORML 「结果店员桑因为工作的缘故、所以才能像往常一样不动声色吗？」
		PRINTFORMW 「还是说、其实会因为想象这样一个小姑娘怎么让欧金金%CNAME("射精音")%的射精而勃起？」
	;えっちな商品
	CASEELSE
		PRINTFORMW 「店员桑会因为妄想这样一个小姑娘要怎么使用这个而勃起吗？」
	ENDSELECT
CASE "店内待機"
	;とりあえず何か一個買った時or33％で喋る
	SIF COND("買上品数") != 1 && RAND:3
		RETURN 0
	;胖次没収中
	IF COND("買い物胖次没収中")
	ELSE
	ENDIF
CASE "询问有什么想要的东西"
CASE "购物終了"
	;胖次没収中
	IF COND("買い物胖次没収中")
		PRINTFORMW 「%TEXTR(@"……%CNAME("二人称")%有些话想对你说、如果这样子被发现了要怎么办、%CNAME("一人称")%紧张的都要死掉了、下次不要在做这种恶作剧了")%」
	;なにも買わなかった
	ELSEIF COND("買上品数") == 0
		PRINTFORMW 「嘛、来这里本身就是目的啊、回去吧」
	ELSEIF CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿")
		PRINTFORMW 「回去的时候也会被路上的行人看吧到、会有多少人呢」
	ELSEIF CHECK_CLO("内衣姿") || CHECK_CLO("裸体")
	ELSE
		PRINTFORMW 「啊，如果可以的话，我可以拿轻巧的行李哦」
	ENDIF
ENDSELECT

