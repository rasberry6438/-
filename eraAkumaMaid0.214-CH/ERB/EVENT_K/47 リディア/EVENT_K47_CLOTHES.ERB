;-------------------------------------------------
;この角色独自の衣装を設定する。制服、体操服、泳装は既存のものから選ぶのが楽です
;-------------------------------------------------
@CLOTHES_K47, ARGS

SELECTCASE ARGS
;高めの頻度で着用します。設定しなくてもかまいません
CASE "内衣"
	CALL SET_PANTIES, "條紋胖次"
	CALL SET_BRA, "運動胸罩"

;衣装に関わらず常に付けている飾品です。設定しなくてもかまいません
CASE "飾品"
	CALL SET_OTHER, "红色铃铛项圈"

;学園での便装です。設定必須です。
;水手服, 露脐装超短裙水手服, 西装外衣, 露脐装超短裙西装外衣, 美式校服など
CASE "制服"
	CALL SET_COSTUME, "旗袍"
;学園の体育の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;体操服布鲁马, 体操服五分褲, 体操服紧身短褲, 体操服运动衫など
CASE "体操服"
	CALL SET_COSTUME, "旗袍"
;学園の水泳の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;死库水, 競泳泳装, 白比基尼, 黒比基尼など
CASE "泳装"
	CALL SET_COSTUME, "旗袍"

;自宅など学園外で着ます。設定必須です
CASE "便装"
	CALL SET_COSTUME, "旗袍"

;自分的房間(not主人公的房間)で着ます
CASE "居家服"

;约会以外で家から外出際に着ます。情人旅館呼出とかね
CASE "外出装"
	CALL SET_COSTUME, "旗袍"
;もちろん约会の際に着ます
CASE "约会衣装"
	CALL SET_COSTUME, "旗袍"
ENDSELECT
SIF COND("角色メイク中")
    CALL SETFLAG, "衣装全脱ぎ"

;-------------------------------------------------
;時間帯や場所に応じた髪型、表情の変化を記述する。設定しなくてもかまいません
;-------------------------------------------------
@MAIDFACE_K47
IF PLACE("自宅") && ABL:信頼 >= 3
	CSTR:髪長 = 長
	CSTR:後髪 = 自然
	CSTR:改髪 = 无特殊髪型
;淋浴後もこの姿にしたかったけど私には無理だった
ELSEIF CFLAG:淋浴済
	CSTR:髪長 = 長
	CSTR:後髪 = 自然
	CSTR:改髪 = 无特殊髪型
ELSE
	CALL SETFLAG, "普段の髪型復元", TARGET
ENDIF
SIF ABL:信頼 >= 3
	CSTR:嘴形 = ω
