;;;%倍とか出来るようにする奴
@MathEX_Per(BaseData,Percent)
#FUNCTION
	#DIM DYNAMIC BaseData
	#DIM DYNAMIC Percent
	RETURNF BaseData * Percent / 100

;;;通貨単位ダエモスラビヤ・デーモール表示メソッド
;;;もう使わないかも
@D20XX_Demol(ARGS)
	#DIM DYNAMIC LCOUNT

	VARSET LOCALS

	SPLIT ARGS, "Д", LOCALS

	FOR LCOUNT, 0, RESULT
		IF LCOUNT >= 1
			PRINT_IMG "Demol"
		ENDIF
		PRINTFORM %LOCALS:LCOUNT%
	NEXT
	PRINTL

;;;	ユ针织をMapの海から見つける
;;;	ARG =尋找ユ针织のID
;;;	RETURN:0 = 検索結果(1 = 見つかった, 0 = 見つからなかった)
;;; RETURN:1 = 見つかったY座標
;;;	RETURN:2 = 見つかったX座標
@D20XX_UnitSearch(SearchUnit)
	#DIM DYNAMIC X_Count
	#DIM DYNAMIC Y_Count
	#DIM DYNAMIC ResultData,3
	#DIM DYNAMIC SearchUnit
	ResultData:0 = 0
	FOR Y_Count,0,200
		FOR X_Count,0,200
			IF SearchUnit == TOINT(D20XX_MAP:Y_Count:X_Count:1)
				ResultData:0 = 1
				ResultData:1 = Y_Count
				ResultData:2 = X_Count
				BREAK
			ENDIF 
		NEXT
	NEXT
	RETURN  ResultData:0,ResultData:1,ResultData:2

;
@HUNI(UniName,Ypos)
	#FUNCTIONS 
	#DIMS DYNAMIC UniName
	#DIM DYNAMIC Ypos
	#DIMS DYNAMIC ResultHtml
	ResultHtml =
	SELECTCASE UniName
		;パラレル日本語
		CASE "そそ", "つん", "なお", "まふ", "はよ", "ひに", "やよ", "ゐに", "ゐん", "逆あ", "にす", "もむ", "をね", "小ゐ", "亜漢字1", "亜漢字2", "亜漢字3", "亜漢字4"
			ResultHtml += @"<img src=\"%UniName%\" ypos=\"{Ypos}\">"
		CASE "Demol","demol"
			ResultHtml += @"<img src=\"demol\" ypos=\"{Ypos}\">"
		CASE "H"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman">%UNICODE(0x2665)%</font>
			ELSE 
				ResultHtml = %UNICODE(0x2665)%
			ENDIF
		CASE "HH"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman" color="#FFC0CB">%UNICODE(0x2665)%</font>
			ELSE 
				ResultHtml = <font color="#FFC0CB">%UNICODE(0x2665)%</font>
			ENDIF
		CASE "BH"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman">%UNICODE(0x2764)%</font>
			ELSE 
				ResultHtml = %UNICODE(0x2764)%
			ENDIF
		CASE "BHH"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman" color="#FFC0CB">%UNICODE(0x2764)%</font>
			ELSE 
				ResultHtml = <font color="#FFC0CB">%UNICODE(0x2764)%</font>
			ENDIF
		CASE "WH"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman">%UNICODE(0x2661)%</font>
			ELSE
				ResultHtml = %UNICODE(0x2661)%
			ENDIF
		CASE "WHH"
			CHKFONT "Times New Roman"
			IF RESULT
				ResultHtml = <font face="Times New Roman" color="#FFC0CB">%UNICODE(0x2661)%</font>
			ELSE
				ResultHtml = <font color="#FFC0CB">%UNICODE(0x2661)%</font>
			ENDIF
		CASE "BBH"
			CHKFONT "メイリオ"
			IF RESULT
				ResultHtml = <font face="メイリオ">%UNICODE(0x2764)%</font>
			ELSE
				ResultHtml = %UNICODE(0x2764)%
			ENDIF
		CASE "BBHH"
			CHKFONT "メイリオ"
			IF RESULT
				ResultHtml = <font face="メイリオ" color="#FFC0CB">%UNICODE(0x2764)%</font>
			ELSE
				ResultHtml = <font color="#FFC0CB">%UNICODE(0x2764)%</font>
			ENDIF
		CASE "BWH"
			CHKFONT "メイリオ"
			IF RESULT
				ResultHtml = <font face="メイリオ">%UNICODE(0x2661)%</font>
			ELSE
				ResultHtml = %UNICODE(0x2661)%
			ENDIF
		CASE "BWHH"
			CHKFONT "メイリオ"
			IF RESULT
				ResultHtml = <font face="メイリオ" color="#FFC0CB">%UNICODE(0x2661)%</font>
			ELSE
				ResultHtml = <font color="#FFC0CB">%UNICODE(0x2661)%</font>
			ENDIF
		CASE "／／／", "／"
			ResultHtml = <font color="#EF857C">%UNICODE(0x002F)%%UNICODE(0x002F)%%UNICODE(0x002F)%</font>
		CASE "U便当", "Uドラゴン", "U創口貼", "U男性", "U女性", "U扶她", "U伪娘", "UH"
			ResultHtml += @"<img src='%SUBSTRING(UniName, 1, -1)%' ypos='{Ypos}'>"
		CASE "怒"
			ResultHtml += @"<img src='Angry' ypos='{Ypos}'>"
		CASEELSE
			ResultHtml = %UniName%
	ENDSELECT

	RETURNF ResultHtml