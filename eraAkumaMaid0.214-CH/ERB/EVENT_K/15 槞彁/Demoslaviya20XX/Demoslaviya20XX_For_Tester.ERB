@D20XX_FromStartStart
	#DIM LCOUNT
	#DIM MEMO_LINECOUNT
	;Verを最新にする
	FLAG:VER_eraAkumaMaid = GAMEBASE_VERSION
	;フラグに使う現実時間を取得
	CALL GET_REALTIMEFORFLAG

	ASSI = 0
	DAY = 1
	TIME = 2

	DELCHARA 0
	;[0]番はドッペル你
	;この人の変数を対応した変数の一時保存用に使いまくるので重要な人です
	ADDCHARA 99

	;[1]番は你
	ADDCHARA 0
	MASTER = 1

	WHILE 1
		CALL D20XX_Start
	WEND 
	