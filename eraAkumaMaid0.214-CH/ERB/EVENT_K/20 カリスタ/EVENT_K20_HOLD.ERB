;CALL CALISTA_HOLD
;
;↓こんな使い方をします。
;IF TEQUIP:拘束
;	CALL CALISTA_HOLD
;ENDIF

@CALISTA_HOLD
CFLAG:随机 = 0
SELECTCASE SELECTCOM
	;愛撫系＆道具系
	CASE 0 TO 12, 30 TO 59
		CALL CALISTAHOLD_AIBU
	;挿入系
	;CASE 60 TO 69, 70 TO 79
	;	CALL CALISTAHOLD_SOUNYU
	;素股
	;CASE 93, 101
	;	CALL CALISTAHOLD_SUMATA
	CASEELSE
		RETURN 0
ENDSELECT

@CALISTAHOLD_AIBU
	PRINTFORM 「
	IF (SELECTCOM == 1 && EX:Ｃ絶頂 >= 10) || (SELECTCOM == 2 && EX:Ａ絶頂 >= 10) || (SELECTCOM == 5 && EX:Ｂ絶頂 >= 10) || (SELECTCOM == 8 && EX:Ｖ絶頂 >= 10) || (SELECTCOM == 9 && EX:Ａ絶頂 >= 10)
		REPEAT 1 + (PALAM:欲情 >= 500) + (PALAM:欲情 >= 3000) + (PALAM:欲情 >= 30000)
			IF !RAND:6 && TALENT:恋慕
				CALL PRINT_STR, "ふぉぉぉーっ_H_"
			ELSEIF !RAND:5
				CALL PRINT_STR, "ふぉぉ…_H_"
			ELSEIF !RAND:4
				CALL PRINT_STR, "ふぉっ_H_"
			ELSEIF !RAND:3
				PRINTFORM ふぉ！
			ELSEIF !RAND:2
				PRINTFORM ふぉ…
			ELSE
				CALL PRINT_STR, "ふぉ_H_"
			ENDIF
		REND
	ELSE
		SIF !RAND:3
			PRINTFORM ふぉ、
		IF !RAND:6 && TALENT:恋慕
			PRINTFORM ふぉぉぉ…
			CALL HEARTMARK
		ELSEIF !RAND:5
			PRINTFORM ふぉぉぉ…
		ELSEIF !RAND:4
			PRINTFORM ふぉぉ？
		ELSEIF !RAND:3
			PRINTFORM ふぉぉ！
		ELSEIF !RAND:2
			PRINTFORM ふぉぉぉ。
		ELSE
			PRINTFORM ふぉぉっ
			CALL HEARTMARK
		ENDIF
	ENDIF
	PRINTFORMW 」






