;-------------------------------------------------
;取引画面と、购物の際の台詞を記述します
;-------------------------------------------------
;取引画面での台詞。悪魔以外は大部分が不要です
;-------------------------------------------------
@KOJO_WISH_K150, ARGS
#DIM MEMO_LINECOUNT

MEMO_LINECOUNT = LINECOUNT

SELECTCASE ARGS
CASE "取引開始"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の取引")
		;しかも挿入中！
		IF INSERT("挿入相手") == MASTER
			IF RAND:4 == 0
				PRINTFORMW 「明明做得正開心呢...到底是怎么啦」
			ELSEIF RAND:3 == 0
				PRINTFORMW 「呐...这种事情不能等做完再说嘛？」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「什么重要的事情，非得在做爱的时候说啊...」
			ELSE
				PRINTFORMW 「哈啊，到底是什么事情，非得挑着现在说啊...」
			ENDIF
		ELSE

		ENDIF
	ELSE
		IF RAND:4 == 0
			PRINTFORML 「嗯哼？有什么事就尽管说吧」
		ELSEIF RAND:3 == 0
			PRINTFORML 「啊，怎么啦？」
		ELSEIF RAND:2 == 0
			PRINTFORML 「想做些什么呢？」
		ELSE
			PRINTFORML 「嗯，我在听哦」
		ENDIF
	ENDIF
CASE "愛の告白"
	;告白成功！
	IF TALENT:相思相愛
		PRINTFORMW 「啊嘞？这个是，表白吗...？」
		PRINTFORMW 起初听见你的这番言语时，神月甚至还稍微愣了一下
		PRINTFORMW 「当然没问题哦———！」
		PRINTFORMW 直到你再度出声确认过这番话语时———完全反应过来的少女，方才是给出了应允的回复
		PRINTFORMW 而紧接着，她就好像是害怕你后悔似的，主动地展開了自己的双臂，紧抱住了你的腰肢
		PRINTFORMW 就这样如同撒娇那般紧抱着这副\@ TALENT:MASTER:男性 ? 身体 # 娇躯 \@摩擦两道过后，她那双柔软的唇便是缓缓地凑到了你的耳畔，轻声地呢喃起来
		PRINTFORMW 「毕竟这边，对于%CNAME("二人称")%...也同样是有着那种感觉的呢」
		PRINTFORMW 「每次那个\@ TALENT:MASTER:男性 ? 熟悉 # 漂亮 \@身形出现在脑海里时...心里就会不自觉地泛起涟漪什么的」
		PRINTFORMW 在将心底所堆积着的感情以热气包裹着吹进你耳内的同时，她的双手也紧跟着開始从背部正中朝着后颈下部与尾椎附近缓缓地转移过去
		PRINTFORMW 「所以，在现在的%CNAME("二人称")%主动提出这样交往的请求的时候，我又怎么可能会说出拒绝的言语呢，呼呼」
		PRINTFORMW 而在以抚摸的形式感受片刻你身体的触感以后，少女心底所泛起的这阵浪潮也终于是逐渐平息了下来
		PRINTFORMW 随即，她便是以无比平静的语气，如此正式地承认了你与她之间，这份早已開花的恋爱関系的成立
		PRINTFORMW 「反正不管怎样...从今往后，就也请%CNAME("二人称")%多多指教啦...♥」
	;失敗
	ELSE
		PRINTFORMW 「啊嘞？这个是，表白吗...？」
		PRINTFORMW 「现在聊这些，是不是还有些为时过早啦？」
		PRINTFORMW 「总之还是留到下次再说吧」
	ENDIF
;胖次のお願いした時に相手が没穿内褲だった
CASE "没穿内褲"
	IF COND("合意：胖次交易")
		IF RAND:3 == 0
			PRINTFORMW 「那个...很抱歉，至少现在，这边没办法实现%CNAME("二人称")%的这种愿望哦？」
			PRINTFORMW 「倒也并不是不愿意啦，主要就是...我根本都没穿嘛，怎么给呀...」
		ELSEIF RAND:2 == 0
			PRINTFORMW 「因为现在根本就没穿，所以...没办法给%CNAME("二人称")%哦？」
			PRINTFORMW 「当然，如果%CNAME("二人称")%不介意我现在就从家里随便转移一条过来的话...」
		ELSE
			PRINTFORMW 「那个...即使是我，也是没办法给出不存在的东西的哦？」
			PRINTFORMW 「至于我这句话的真假，嗯...稍微掀開裙子看看，就知道了哦」
		ENDIF
	ELSE

	ENDIF
;胖次を要前の会話。
;ただし、悪魔の場合には魂を払っているので確実に渡すことになる
;ここで本来胖次を给かどうかなどを加味して分岐を設ける事も出来る。特に書かなくてもよい
;TFLAG:取引承諾 = 0とすると胖次拒否に行くようになり、TFLAG:取引承諾 = 1とすると判定無視で胖次を渡せるようになる
;TFLAG:取引価格 に数字を入れる事で好きな額を要求できるようになる。基準相場は$500です
;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで胖次を渡す展開に出来る
;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "胖次交易"
	;ＯＫ！
	IF TFLAG:取引承諾
		;日期と時間参照随机
		IF COND("合意：胖次交易") && RANDC(4) == 0
			TFLAG:取引価格 = 1000
			PRINTFORMW 
		ELSE
			PRINTFORMW
		ENDIF
	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		TFLAG:値上げ交渉 = 500
	ENDIF
CASE "胖次"
	;値切って相場と変わらず$500払う。ここでTFLAG:取引承諾 = 0とすることで拒否するのもよいでしょう
	IF TFLAG:値切った
		IF RAND:2 == 0
			PRINTFORMW 「真是的...至少得给我些买新内褲的钱吧？」
		ELSE
			PRINTFORMW 「嘛，虽说这边也并不缺钱就是了...」
		ENDIF
	ELSE
		IF RAND:3 == 0
			PRINTFORMW 「虽然也还算是挺能理解这种癖好的吧...」
			PRINTFORMW 「但轮到自己作为被幻想的对象时，果然多少也还是会感到奇怪的啊」
			PRINTFORMW 「总之，不管接下来要接触它的是口鼻还是哪里，之后都请一定要好好清洁哦...？」
		ELSEIF RAND:2 == 0
			PRINTFORMW 「如果%CNAME("二人称")%愿意将身段再放低些，好好地求我的话～」
			PRINTFORMW 「呜啊！只是開玩笑而已，别...至少等我一会啊，真是的...」
		ELSE
			PRINTFORMW 「那个...用完之后，一定要记得好好清洗哦？我说的是%CNAME("二人称")%的身体」
			PRINTFORMW 「就算不考虑我讨厌不干净的东西这点...细菌感染的滋味也不好受，对吧？」
		ENDIF
	ENDIF
	;胖次を脱いで渡してくる汎用地の文
	CALL DAILY_MESSAGE, "脱下胖次交了出去"

CASE "胖次拒否"
	;取引を拒否した際
	IF TFLAG:取引拒否

	ELSE

	ENDIF

;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで服を脱掉展開に出来る
;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "全裸交易"
	;ＯＫ！
	IF TFLAG:取引承諾

	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		;TFLAG:値上げ交渉 = 2000
	ENDIF
CASE "全裸"
	;値上げ交渉した("全裸交易"で設定した場合のみ)
	IF TFLAG:値上げ交渉

	;取引初成功
	ELSEIF COND("合意：全裸化") == 0

	ELSE

	ENDIF
	;汎用地の文
	CALL EVENT_DATUI, "裸体"
CASE "全裸拒否"
	;取引を拒否した際("全裸交易"で設定した場合のみ)
	IF TFLAG:取引拒否

	;二人きりなら脱いでくれるけど状況が悪い
	ELSEIF COND("合意：全裸化") && (CHARA_NUM("室内の邪魔者") || PLACE("個室") == 0)
		IF RAND:3 == 0
			PRINTFORMW 「这种事情，至少得到只有我们俩的场合才可以吧...？」
		ELSEIF RAND:2 == 0
			PRINTFORMW 「那个...至少找个清净的地方？毕竟这里还有其他人在呢...」
		ELSE
			PRINTFORMW 「那...那个，就算再怎么样，这边也还是...不太能接受在人前脱衣的哦？」
		ENDIF
	ELSE
		IF RAND:2 == 0
			PRINTFORMW 「不管再问多少遍，我的答案都肯定是“不行”哦」
		ELSE
			PRINTFORMW 「...至少我的開放程度，还没%CNAME("二人称")%想象的那么高哟？」
		ENDIF
	ENDIF

;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "事后避孕药取引"

CASE "事后避孕药"
	;代金をまけてくれた場合
	IF TFLAG:値切った
		;所持金が足りなかった場合です
		;現状、ここに来るのは恋慕无の時のみ
	;拝み倒したら代金を払わなくても許してくれた場合
	ELSEIF TFLAG:取引価格 == 0
		;現状、ここに来るのは恋慕有の時のみ
	ELSE
		;普通にお金を払った場合
		;口上无でも自然な感じです
		;ですが恋慕のときなどは、なにか一言あってもいいかもですね
	ENDIF

CASE "事后避孕药拒否"
	;これはCASE "事后避孕药取引"内でTFLAG:取引拒否 = 1 となる分岐を作った場合のみ
	IF TFLAG:取引拒否

	;拝み倒して失敗した場合
	ELSEIF TFLAG:取引価格 == 0
		;轻轻地地の文が表示されます。口上有无どちらでも大丈夫そうです。
	;其他、你が薬代を不支付場合
	ELSE
		;恋慕状態で、それなりに妊娠の恐惧がある場合
		IF ( COND("受精確認") || COND("胎内の精液量") ) && TALENT:恋慕
			;お金がないか、不支付を選んだ場合です。
			;それなら避妊できなくて妊娠しちゃっても仕方ないね…(爱心)って感じのやつです。
			;地の文が表示されるので何も書かないほうが良いかもしれません。
		;不支付、を選んだ場合
		ELSEIF MONEY >= TFLAG:取引価格
			;口上无でも自然な感じです
		;お金が足りなかった場合
		ELSE
			;不支付を選んだ場合も、お金が足りないときはここへ来ます。
		ENDIF
	ENDIF
CASE "同居拒否"

CASE "同居希望"
	;初回
	IF DEVENT(ARGS) == 0
		PRINTFORMW 「那么，今后也请多多指教啦♪」
	;二回目以降
	ELSE
		IF RAND:2 == 0
			PRINTFORMW 「该庆幸自己没把行李都搬走嘛...」
		ELSE
			PRINTFORMW 「又要重新收拾东西了嘛...」
		ENDIF
	ENDIF
CASE "同居頻度的相談"

CASE "便当拒否"

CASE "做便当"
	;便当のお願い２回目以降
	;IF COND("合意：便当")
		IF RAND:3 == 0
			PRINTFORMW 「嗯，可以啊——」
		ELSE
			PRINTFORMW 「嗯哼，当然可以哦」
		ENDIF

		IF RAND:2 == 0
			PRINTFORMW 「正好也能借机提升自己的厨艺呢♪」
		ELSE
			PRINTFORMW 「刚好这边最近也研究了几种不错的新菜式呢♪」
		ENDIF
	;以下は初めて作ってもらうよう变成了とき
	;ELSE
		;IF COND("同居中")

		;ELSE

		;ENDIF
	;ENDIF

;COND("口服避孕薬派")かどうかで初回が"口服避孕薬を飲んで欲しい"or"口服避孕薬を止めて欲しい"に分かれます
CASE "口服避孕薬"
	;角色が口服避孕薬派なら、こっちだけ書いてください
	IF COND("口服避孕薬派")
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬

		;口服避孕薬の服用を止めてもらう
		ELSE
			;初回
			IF DEVENT(ARGS) == 0

			ELSE

			ENDIF
		ENDIF
	;角色が口服避孕薬派でないなら、こっちだけ書いてください
	ELSE
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬
			;初回
			IF DEVENT(ARGS) == 0

			ELSE

			ENDIF
		;口服避孕薬の服用を止めてもらう
		ELSE

		ENDIF
	ENDIF
;COND("口服避孕薬派")の人に口服避孕薬服用を止めてもらうお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬中断拒否"

;COND("讨厌口服避孕薬")の人に口服避孕薬服用のお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬服用拒否"

CASE "淫紋拒否"

;"淫紋贴纸直前"or"淫紋贴纸"の片方を書けば十分だと思います
CASE "淫紋贴纸直前"
	;初回
	IF DEVENT(ARGS) == 0

	ELSE

	ENDIF

CASE "淫紋贴纸"
	;台詞を書いたら下のRETURN 0を消してください
	RETURN 0
	;初回
	IF DEVENT(ARGS) == 0

	ELSE

	ENDIF
;"淫紋直前"or"淫紋"の片方を書けば十分だと思います
CASE "淫紋直前"
	SELECTCASE MARK:淫紋
	CASE 2

	CASE 3

	CASE 4

	ENDSELECT
CASE "淫紋"
	;台詞を書いたら下のRETURN 0を消してください
	RETURN 0
	SELECTCASE MARK:淫紋
	CASE 2

	CASE 3

	CASE 4

	ENDSELECT

CASE "拜托処理性欲"

CASE "性処理の一時中断"

CASE "性処理拒否"

;悪魔、淫魔、妖狐専用
CASE "给我看尾巴"

CASE "给我看尾巴拒否"

;改变髪型のは、長さが披肩以上の人のみ。未設定だと披肩扱いです
;髪型を弄る際に手伝ってくれる人がASSIになっています。お手伝い桑を指名したい場合にはその人をASSIにして下さい
CASE "改变髪型"

CASE "髪型変更の感想"
	;どの髪型变成了かで分岐可能
	SELECTCASE CONDS("髪型の種類")
	CASE CONDS("普段の髪型")
		PRINTFORMW 
	CASE "齐刘海"
		PRINTFORMW 
	CASE "直髪"
		PRINTFORMW 
	CASE "卷髪"
		PRINTFORMW 
	CASE "外八"
		PRINTFORMW 
	CASE "双马尾"
		PRINTFORMW 
	CASE "马尾辫"
		PRINTFORMW 
	CASE "三股辫"
		PRINTFORMW 
	CASE "编起来"
		PRINTFORMW 
	CASE "団子頭"
		PRINTFORMW 
	;其他
	CASEELSE
		PRINTFORMW 
	ENDSELECT
CASE "改变髪型拒否"

;これは普段から连褲襪を履く人じゃないとあんまり見る機会ないと思う
CASE "连褲襪着替え"
	;台詞を書いたら下のRETURN 1を消してください
	RETURN 1
	;どれに履き替えてもらったか
	IF CHECK_OTHER("開放型连褲襪")
		PRINTFORMW 
	ELSEIF CHECK_OTHER("渔网连褲襪")
		PRINTFORMW 
	ELSEIF CHECK_OTHER("连褲襪")
		PRINTFORMW 
	;生足
	ELSE
		PRINTFORMW 
	ENDIF
CASE "连褲襪着替え拒否"

CASE "巨根化して"
	;これは"大きくなる前"のサイズなのに注意
	SELECTCASE CONDS("潜在的含む阴茎尺寸")
	CASE "短小"

	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並以上
	CASEELSE

	ENDSELECT

CASE "巨根化して拒否"

CASE "短小化して"
	;これは"小さくなる前"のサイズなのに注意
	SELECTCASE CONDS("潜在的含む阴茎尺寸")
	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並、規格外
	CASEELSE

	ENDSELECT

CASE "短小化して拒否"

;恋慕のみ　避妊の護符を取下
CASE "避妊解除可"

CASE "避妊解除拒否"

;一度解除した避妊の護符の再装着をお願いした場合
CASE "避妊の護符"

;以下は悪魔専用
CASE "魂が足りない"

CASE "想要钱"

CASE "甜言蜜语"

CASE "謎之魅力"

CASE "業物"

CASE "射精量強化"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の取引")
		;しかもＶ挿入中！
		IF INSERT("Ｖ挿入相手") == MASTER

		ELSE

		ENDIF
	ELSE

	ENDIF
CASE "巨根化"
	;これは"大きくなる前"のサイズなのに注意
	SELECTCASE CONDS("潜在的含む阴茎尺寸", PLAYER)
	CASE "短小"

	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並以上
	CASEELSE

	ENDSELECT
CASE "短小化"
	;これは"小さくなる前"のサイズなのに注意
	SELECTCASE CONDS("潜在的含む阴茎尺寸", PLAYER)
	CASE "普通"

	CASE "巨根"

	CASE "馬根"

	;腕並、規格外
	CASEELSE

	ENDSELECT
ENDSELECT

;以下は汎用台詞(すでに喋っている場合には下には行かない)
SIF LINECOUNT > MEMO_LINECOUNT
	RETURN 1

;汎用拒否
IF STRCOUNT(ARGS, "拒否")
	[IF_DEBUG]
		PRINTFORMW @KOJO_WISH_K150, 汎用拒否
	[ENDIF]
;汎用取引（特に記述することがないならスキップ）
ELSEIF STRCOUNT(ARGS, "取引")

;汎用ＯＫ
ELSE
	[IF_DEBUG]
		PRINTFORMW @KOJO_WISH_K150, 汎用ＯＫ
	[ENDIF]
ENDIF

;-------------------------------------------------
;购物での台詞
;-------------------------------------------------
@KOJO_SHOP_K150, ARGS

SELECTCASE ARGS
CASE "一緒に出発"
	;着替えてもらう
	IF CHECK_CLO("裸体") || CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿") || CHECK_CLO("内衣姿")
		PRINTFORMW 「啊啦，要和我一起去购物吗？」
		PRINTFORMW 「请稍等一下哦，我先换一套能够出門的衣服...」
	ELSE
		PRINTFORMW 「嗯，那就走吧」
	ENDIF
CASE "目送"
	PRINTFORMW 「一路顺风———」
CASE "没収胖次出発"
	;既に没穿内褲
	IF EQUIP:胖次 == 0
		PRINTFORMW 
	ELSE
		PRINTFORMW
	ENDIF
CASE "保持现在的装扮出発"
	IF CHECK_CLO("泳装姿")
		PRINTFORMW 「嗯...嘛，反正也勉强算是能穿出去的衣服...」
	ELSEIF CHECK_CLO("体操服姿")
		PRINTFORMW 「穿着这个出門...会很惹眼的吧？」
		PRINTFORMW 「嘛，反正如果是%CNAME("二人称")%的愿望的话...」
	ELSEIF CHECK_CLO("内衣姿") || CHECK_CLO("裸体")
		PRINTFORMW 「......」
		PRINTFORMW 并没有做出言语上的回应，神月只是顶着脸上的那一大片红晕，慢慢地穿起了鞋...
	ENDIF
CASE "会計交代"
	;初回
	IF SEVENT(ARGS) == 0 || SEVENT("商品購入") == 0
		PRINTFORMW 「让我看看总价格是...」
		PRINTFORMW 「嗯，都付好了」
	ELSE
		IF RAND:3 == 0
			PRINTFORMW 「出門在外总让女孩子帮忙付钱可不是绅士的作风呀...真是的」
		ELSEIF RAND:2 == 0
			PRINTFORMW 「虽说也不是心疼钱啦...但总这样做，给人留下的印象总归也还是不会太好的哦」
		ELSE
			PRINTFORMW 「那就稍微让一让吧？我来刷卡」
		ENDIF
	ENDIF
;ここはwaitを使わないようにしてください(PRINTFORMW の代わりに PRINTFORML を使う)
CASE "询问有什么想要的东西"
	;PRINTFORML 
CASE "商品購入"
	;今買った物
	SELECTCASE STR:購入道具
	;ギリギリ通常商品の範疇or你用
	CASE "撮影機材", "潤滑液", "媚薬", "飞机杯"

	CASE "避孕套"
		;この商品を初めて買わされた
		IF SEVENT(STR:購入道具) == 0

		ELSE

		ENDIF
	;えっちな商品
	CASEELSE

	ENDSELECT
CASE "店内待機"
	;とりあえず何か一個買った時or33％で喋る
	SIF COND("買上品数") != 1 && RAND:3
		RETURN 0
	;胖次没収中
	IF COND("買い物胖次没収中")
		IF RAND:3 == 0
			PRINTFORMW 「...那个，东西到底都选好了嘛」
			PRINTFORMW 「好歹别忘了我现在可是真空出行的状态啊...」
		ELSEIF RAND:2 == 0
			PRINTFORMW 「啊...想要快点回去了」
			PRINTFORMW 「毕竟这样不穿内褲的话，总感觉没什么安全感呢」
		ELSE
			PRINTFORMW 「那个，能不能快点呀...」
			PRINTFORMW 「我的里面现在可是什么都没穿的哦...？能快就快点吧」
		ENDIF
	ELSE

	ENDIF
CASE "购物終了"
	;胖次没収中
	IF COND("買い物胖次没収中") || CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿")
		PRINTFORMW 「嘛...也总算是能回去了」
	;なにも買わなかった
	ELSEIF COND("買上品数") == 0
		PRINTFORMW 「也的确，都没什么好买的呢...」
	ENDIF
ENDSELECT

