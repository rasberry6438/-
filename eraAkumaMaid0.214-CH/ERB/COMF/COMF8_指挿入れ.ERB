@COM8
;指挿入
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%

RETURN 1

@FLAG_COM8, ARG
IF CHECK_COM("玩弄Ｇ点")
	CALL FLAG_INSERTION, "指挿入", ARG, 150
ELSEIF TEQUIP:振動棒
	CALL FLAG_COM31, 130
ELSE
	CALL FLAG_INSERTION, "指挿入", ARG, 100
ENDIF

@SOURCE_COM8, ARG
IF CHECK_COM("玩弄Ｇ点")
	CALL SOURCE_INSERTION, "指挿入", ARG, 150
ELSEIF TEQUIP:振動棒
	CALL SOURCE_INSERTION, "振動棒", ARG, 130
ELSE
	CALL SOURCE_INSERTION, "指挿入", ARG, 100
ENDIF


@TRAIN_MESSAGE_COM8
PRINTFORM %CALLNAME:PLAYER%将
IF TEQUIP:接吻
	PRINTFORM %TEXTS("接吻継続")%、
	CALL SETFLAG, "追加接吻フラグ", TARGET
ELSEIF TEQUIP:六九式
	PRINTFORM %TEXTS("６９")%
ELSEIF INSERT("Ａ挿入") && PREVCOM != SELECTCOM
	PRINTFORM 侵犯著肛門的同時、
ELSEIF PREVCOM == 1 && PENIS(TARGET) == 0 && CHECK_CLO("Ｖ露出")
	SIF PALAM:欲情 >= 3000 && ABL:Ｃ感覚 >= 3
		PRINT 興奮勃起的
	PRINTFORM 陰舔舐著蒂仔細的同時、
ELSEIF PREVCOM == 13 && TEQUIP:振動棒 == 0
	PRINT 將眼前的陰唇張開的同時、
ELSEIF TEQUIP:電話中
	IF PREVCOM == 13 && TEQUIP:振動棒 == 0
		PRINTFORM %TEXTS("電話しながら")%单手将%TEXTS("阴部：視覚")%帳開的
	ELSE
		PRINTFORM %TEXTS("電話中の")%
	ENDIF
ENDIF
PRINTFORM %CALLNAME:TARGET%的

;振動棒
IF TEQUIP:振動棒
	;穴開きでない胖次を是ていて、ずらされていないなら、必ずずらす
	SIF CHECK_CLO("Ｖ露出") == 0
		CALL SETFLAG, "下身衣装を露出：テ接吻ト有些", TARGET
	;道端の菠萝包加筆分
	IF ABL:Ｖ拡張 - SIZE("振動棒", TARGET) >= 2
		PRINTFORM 拡張された膣に咀嚼されている
	;ここまで加筆分
	ELSEIF RAND:2 == 0
		PRINTFORM 阴道穴中深埋著的
	ELSE
		PRINTFORM 阴道道中不斷振動著的
	ENDIF

	SELECTCASE SIZE("振動棒")
	CASE IS >= 3
		IF RAND:2 == 0
			PRINT 兇惡的
		ELSE
			PRINT 極大
		ENDIF
	CASE 2
		IF RAND:2
			PRINT 粗大
		ELSE
			PRINT 粗大的
		ENDIF
	ENDSELECT

	;ピストン
	IF RAND:2 == 0
		PRINTFORM 振動棒
		IF (ABL:Ｖ感覚 >= 3 || ABL:Ｖ拡張 >= SIZE("振動棒") || ABL:PLAYER:施虐属性 >= 3) && RAND:3 == 0
			PRINT 以幾乎拿不穩的最大頻率激烈振動著的
		ELSEIF RAND:2 == 0
			PRINT 不斷調節著振動快慢的
		ELSE
			PRINT 緩慢的增大頻率的
		ENDIF
		PRINTFORM 反復插拔著
		IF ABL:Ｖ拡張 < SIZE("振動棒") && RAND:3 == 0
			PRINTFORMW 、將被過大的異物強行擴張的膣穴更加激烈地蹂躪起來……
		ELSEIF (ABL:Ｖ感覚 >= 3 || ABL:PLAYER:施虐属性 >= 3) && RAND:4 == 0
			PRINTFORMW 、用堅硬冰冷的頂端輕輕戳弄著子宮的入口……
		ELSEIF RAND:3 == 0
			PRINTFORMW 、在不斷攪拌中讓膣道內的汁液浮起白沫並變得更加黏稠……
		ELSEIF RAND:2 == 0
			PRINTFORMW 、用無機質的振動棒摩擦著膣道中的粘膜……
		ELSE
			PRINTFORMW ……
		ENDIF
	;スイッチON
	ELSE
		IF EX:処女喪失
			PRINTFORM 将振動棒的檔位%TEXTR("稍微增强了一些之后/的强度来回变化着/调到最弱/一下打開一下関掉着玩弄着")%、
		ELSE
			PRINTFORM 将振動棒的檔位%TEXTR("毫不遲疑的推到最高之後/的强度来回变化着/一下变强一下变弱地玩弄着/一下打開一下関掉着玩弄着")%、
		ENDIF
		IF (ABL:Ｖ感覚 >= 4 || ABL:Ｖ拡張 >= SIZE("振動棒") || ABL:PLAYER:施虐属性 >= 3) && RAND:3 == 0
			PRINTFORM %TEXTR("抉るように/容赦なく")%
		ELSEIF ABL:Ｖ拡張 <= SIZE("振動棒") && (ABL:Ｖ感覚 >= 3 || ABL:PLAYER:施虐属性 >= 3) && RAND:2 == 0
			PRINTFORM 為了更加深入膣道而不斷加力的
		ELSEIF RAND:3 == 0 && EX:処女喪失 == 0
			PRINTFORM 在深處後轉動著的
		ELSEIF RAND:2 == 0
			PRINTFORM 在輕聲詢問著感覺的同時、
		ELSE
			PRINTFORM 左右捻動、轉動著的
		ENDIF
		PRINTFORMW 來回翻攪著蜜壺……
	ENDIF
ELSEIF CHECK_COM("玩弄Ｇ点")
	IF RAND:2
		PRINTFORM 阴道穴中的敏感
		IF RAND:2
			PRINTFORM 部分用指尖執拗地揉捏、摩擦著
		ELSE
			PRINTFORM 粘膜正隨著手指的摩擦而微微地蠢動著
		ENDIF
	ELSE
		PRINTFORM 阴道道中將手指彎曲、
		IF RAND:2
			PRINTFORM 將微微顫動著的的頂端刺激著
		ELSE
			PRINTFORM 僅僅將觸感有著微妙差異的纖細粘膜集中地觸摸、愛撫著
		ENDIF
	ENDIF
	;前回こらえた＆そろそろ限界
	IF ABL:Ｖ感覚 >= 3 && PREVEX("绝顶忍耐") && NOW_PALAM("快Ｖ") > MAX(MULTIPLY(MAXBASE:快感許容値女性, 70), 10000)
		PRINTFORMW 
		IF RAND:3 == 0
			PRINTFORM 、像是在回應被給予的劇烈快感一樣、
		ELSEIF RAND:2
			PRINTFORM 、就像是無法忍耐下體傳來的激烈刺激一樣、
		ELSE
			PRINTFORM 、完全的沉浸在膣道被手指玩弄所產生的快感之中、
		ENDIF
		SIF TEQUIP:電話中
			PRINTFORM %TEXTR(@"和%TEXTS("彼氏")%的電話中的同时漏出了烦悩的吐息/因为不想被%TEXTS("彼氏")%听到而压抑下的声音漏了出来")%、

		IF ABL:Ｖ名器度 >= 30 && RANDIF(2, CUP:Ｖ潤 >= 1000)
			PRINTFORMW %CALLNAME:TARGET%的膣道像是在配合手指的細微活動一樣微微地痙攣起來、而又在刺激過於激烈的時候用力地閉合膣口夾緊了指節……
		ELSEIF CUP:Ｖ潤 >= 1000 && RAND:2
			PRINTFORMW %CALLNAME:TARGET%的膣道洋溢出數量驚人的黏稠愛液、將深入的指節微微推出的同時、也將%CALLNAME:PLAYER%的整隻手都用愛液弄髒了……
		ELSE
			PRINTFORMW %CALLNAME:TARGET%\@ TALENT:好色 && RAND:2 ? 積極的 # 無意識的 \@隨著%CALLNAME:PLAYER%手部的動作而劇烈的扭動著腰肢……
		ENDIF
	ELSE
		PRINTFORMW …
	ENDIF
;指
ELSE
	LOCALS = 
	;穴開きでない胖次を是ていて、ずらされていないならずらすことも
	IF CHECK_CLO("Ｖ露出") == 0
		IF RAND:3 == 0
			CALL SETFLAG, "下身衣装を露出：テ接吻ト有些", TARGET
		ELSE
			PRINTFORM %CLO("股間")%的空隙中將手深入、將
		ENDIF
		LOCALS = 下身衣装描写
	ENDIF
	IF PREVEX("Ｖ中出し") && CHECK_STAIN(STAIN:Ｖ, "精液")
		PRINTFORMW 手指慢慢插入中出之後的膣道、將肉襞的褶皺上黏著的精液慢慢的攪拌、掏出……
	ELSE
		IF CHECK_CLO("Ｖ露出") == 0 || RAND:3 == 0
			SIF PALAM:Ｖ潤 >= PALAMLV:4
				PRINT 被雌性汁液充分濡濕的
			IF TALENT:娇小
				PRINT 幼嫩秘縫
			;2020/2/1　加筆
			SIF AGE(TARGET) == 4
				PRINTFORM %TEXTR("肉厚的/成熟大人样的/发黑的")%
			ELSE
				PRINT 秘裂
			ENDIF
		ELSE
			PRINTFORM 被%TEXTS("阴部：感触")%
		ENDIF

		IF PREVCOM == SELECTCOM
			PRINTFORMW 用指尖來回的翻攪著……
		ELSE
			PRINTFORM に
			IF ABL:Ｖ拡張 >= 4 && EXP:Ｖ拳交経験 >= 5 && CHECK("Ｖ拳交") && RAND:2
				PRINTFORM 對齊了手指的手背直接插入體內
			ELSE
				IF ABL:Ｖ拡張 >= 4 || (ABL:Ｖ拡張 >= 2 && RAND:2)
					PRINTFORM 中指和薬指
				ELSE
					PRINTFORM \@ RAND:2 ? 中 # \@指
				ENDIF
				PRINTFORM \@ RAND:2 ? 直達根部地 # 輕柔緩慢地 \@挿入體內
			ENDIF

			IF LOCALS == "下身衣装描写"
				PRINTFORMW ……
			ELSE
				IF RAND:3 == 0
					PRINTFORMW 、將柔軟的膣壁用指腹摩挲著……
				ELSEIF RAND:2
					PRINTFORMW 、將肉襞的褶皺來回翻攪著……
				ELSE
					PRINTFORMW 、享受著膣道的緊縮觸感……
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	IF TEQUIP:電話中
		PRINTFORMW %TEXTR(@"和%TEXTS("彼氏")%的電話中的/和%TEXTS("彼氏")%通話中的")%%CALLNAME:TARGET%%TEXTR("为了不让声音漏出来而拼命忍耐着/拼命压抑住了快要漏出来的声音")%…
	ELSEIF PREVEX("Ｖ中出し") && CHECK_STAIN(STAIN:Ｖ, "精液")
		PRINTFORM 從膣道中拔出的手指沾滿渾濁的精液、看見這淫靡的景色之後、
		IF ABL:精液中毒 > 3 && PALAM:欲情 >= 10000
			PRINTFORMW %CALLNAME:TARGET%像是十分渴望一樣的舔舐著唇角……
		ELSEIF TALENT:恋慕
			PRINTFORMW %CALLNAME:TARGET%用恍惚而快樂的眼神注視著它……
		ELSE
			IF RAND:2
				PRINTFORMW %CALLNAME:TARGET%的臉頰因羞澀染上紅潮……
			ELSE
				PRINTFORMW %CALLNAME:TARGET%害羞的偏開視線……
			ENDIF
		ENDIF
	ELSEIF EXP:Ｖ経験
		SELECTCASE EXP:Ｖ経験
		CASE IS <= 30
			PRINTFORMW %CALLNAME:TARGET%將體內手指的觸感忍耐著、不禁漏出了灼熱的吐息……
		CASE IS <= 50
			PRINTFORMW %CALLNAME:TARGET%開始嘗試享受手指抽插的觸覺……
		CASE IS <= 80
			PRINTFORMW %CALLNAME:TARGET%恍惚地感受著手指抽動的觸覺與快樂……
		CASE IS <= 120
			PRINTFORMW %CALLNAME:TARGET%因腹腔內部被輕輕翻攪搔弄而產生的快樂而苦悶的漏出了灼熱的喘息……
		CASEELSE
			IF ABL:Ｖ感覚 >= 3 && RAND:2
				PRINTFORMW 即使將%CALLNAME:TARGET%被充分地烙印下性的快樂的雌穴激烈地蹂躪、她也完全沒有感到任何痛苦、倒不如說是非常愉悅的吞嚥著手指……
			ELSEIF TALENT:Ｖ性向 && TALENT:好色 + TALENT:不知羞恥 + TALENT:淫乱
				PRINTFORMW %CALLNAME:TARGET%無意識的流下涎水、貪圖快樂地揮舞著腰肢套弄著手指……
			ELSE
				PRINTFORMW %CALLNAME:TARGET%露出一副非常舒服的表情、自觉地动着腰享受起%CALLNAME:PLAYER%的手指…
			ENDIF
		ENDSELECT
	ENDIF
ENDIF
