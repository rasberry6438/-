@COM37
;飛機杯
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%


RETURN 1

@FLAG_COM37, ARG
;阴部を露出させる
CALL SETFLAG, "下身衣装を露出：テ接吻ト没有", TARGET


@SOURCE_COM37, ARG
CALL VARSET_COMF

CALL SOURCE_DOWNBASE_HP, 37

SOURCE:0:露出 = 300
SOURCE:0:逸脱 = 300

;ABL:受虐属性をみる
SELECTCASE ABL:受虐属性
CASE 0 TO 5
	SOURCE:0:達成感 = ABL:受虐属性*500
CASEELSE
	SOURCE:0:達成感 = 2500 + (ABL:受虐属性 - 5)*200
ENDSELECT

;男性か扶她
IF PENIS(TARGET)
	;ABL:Ｃ感覚をみる
	SOURCE:0:Ｃ快楽 = MULTIPLY(CALCF("Ｃ刺激", 0), 90 + ITEM:飞机杯*10)
	SOURCE:0:Ｃ快楽 = SOURCE_EXABL(SOURCE:0:Ｃ快楽, "指靈活", PLAYER)
	;阴茎への刺激にはBOUNS
	SOURCE:0:Ｃ快楽 += 500
ENDIF

CALL CALC, "調教ソース", ARG


@TRAIN_MESSAGE_COM37
PRINTFORMW 將%CALLNAME:TARGET%%TEXTR("兇猛的/可愛的")%陰莖插入飛機杯中、開始%TEXTR("激烈地/靈巧地/時快時慢地")%套弄起來……

