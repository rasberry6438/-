@COM178
;打电话
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%

RETURN 1


@FLAG_COM178, ARG
IF TEQUIP:電話中 == 0
	TEQUIP:電話中 = 1
ELSE
	TEQUIP:電話中 = 0
ENDIF

@SOURCE_COM178, ARG

@TRAIN_MESSAGE_COM178
PRINTFORM 
IF TEQUIP:電話中 == 0
	PRINTFORM %CALLNAME:PLAYER%在%CALLNAME:TARGET%
	IF TALENT:有男朋友 == 2
		PRINT 给旦那
	ELSEIF TALENT:有男朋友
		PRINT 给彼氏
	ENDIF
	PRINTFORMW 打了电话。
ELSE
	PRINTFORM %CALLNAME:TARGET%%TEXTR("用适当的借口/用有要紧事为理由/说着有要紧事必须要出門")%将
	IF TALENT:有男朋友 == 2
		PRINT 给旦那
	ELSEIF TALENT:有男朋友
		PRINT 给彼氏
	ENDIF
	PRINTFORMW 的电话挂断了。
ENDIF
