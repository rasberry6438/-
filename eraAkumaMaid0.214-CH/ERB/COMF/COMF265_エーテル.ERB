@COM265
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%

TEQUIP:MASTER:芳香醚 = 1
ITEM:芳香醚 -= 1

BASE:精力 = MIN(MAXBASE:精力, BASE:精力 + MAXBASE:精力/2)
BASE:体力 = MIN(MAXBASE:体力, BASE:体力 + MAXBASE:体力/2)

BASE:MASTER:精力 = MIN(MAXBASE:MASTER:精力, BASE:MASTER:精力 + MAXBASE:MASTER:精力/2)
BASE:MASTER:体力 = MIN(MAXBASE:MASTER:体力, BASE:MASTER:体力 + MAXBASE:MASTER:体力/2)

IF ASSI && STATE("撃沈", ASSI) == 0
	BASE:ASSI:精力 = MIN(MAXBASE:ASSI:精力, BASE:ASSI:精力 + MAXBASE:ASSI:精力/2)
	BASE:ASSI:体力 = MIN(MAXBASE:ASSI:体力, BASE:ASSI:体力 + MAXBASE:ASSI:体力/2)
ENDIF

IF NOTTIME_PLAY(265)
	CALL SETFLAG, "今回指令"
	RETURN 0
ENDIF
RETURN 1


@FLAG_COM265, ARG

@SOURCE_COM265, ARG
CALL VARSET_COMF

CALL CALC, "調教ソース", ARG


@TRAIN_MESSAGE_COM265
IF PLAYER == MASTER && ASSI && STATE("撃沈", ASSI) == 0
	PRINTFORML %CALLNAME:MASTER%被%CALLNAME:ASSI%使用了芳香醚！
ELSE
	PRINTFORML %CALLNAME:MASTER%使用了芳香醚！
ENDIF
PRINTFORML 全員的体力与精力回復50％！！
CALL PRINT_STRW, "イエロー_※芳香醚每次Ｈ只能使用一次※"
