@料理メニューコンボ_营养均衡餐厅(ARGS)
SELECTCASE ARGS
	CASE "コンボ判定"
		LOCALS:0 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:1, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:1 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:2, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:2 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:3, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:3 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:4, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:4 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:5, "メイン食材ID", 1), "食材分類", 1)%
		;メインの食材カテゴリが全種制覇
		SIF MATCH(LOCALS, "魚") < 1
			RETURN 0
		SIF MATCH(LOCALS, "肉") < 1
			RETURN 0
		SIF MATCH(LOCALS, "野菜") < 1
			RETURN 0
		SIF MATCH(LOCALS, "穀物") < 1
			RETURN 0

		発動中コンボ名 = 营养均衡餐厅
		詳細文文字列受け渡し変数 = 使菜单效果带来的卢比获取量增加10％。
		RETURN 1
		;効果処理は@EVENTEND0で記述
ENDSELECT

@料理メニューコンボ_新鲜海鲜餐厅(ARGS)
SELECTCASE ARGS
	CASE "コンボ判定"
		LOCALS:0 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:1, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:1 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:2, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:2 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:3, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:3 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:4, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:4 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:5, "メイン食材ID", 1), "食材分類", 1)%
		;魚メイン料理３つ以上
		SIF MATCH(LOCALS, "魚") < 3
			RETURN 0

		発動中コンボ名 = 新鲜海鲜餐厅
		詳細文文字列受け渡し変数 = 使菜单效果带来的卢比获取量增加10％。
		RETURN 1
ENDSELECT

@料理メニューコンボ_肉类自助餐厅(ARGS)
SELECTCASE ARGS
	CASE "コンボ判定"
		LOCALS:0 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:1, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:1 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:2, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:2 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:3, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:3 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:4, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:4 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:5, "メイン食材ID", 1), "食材分類", 1)%
		;肉メイン料理３つ以上
		SIF MATCH(LOCALS, "肉") < 3
			RETURN 0

		発動中コンボ名 = 肉类自助餐厅
		詳細文文字列受け渡し変数 = 使菜单效果带来的卢比获取量增加10％。
		RETURN 1
ENDSELECT

@料理メニューコンボ_健康蔬菜餐厅(ARGS)
SELECTCASE ARGS
	CASE "コンボ判定"
		LOCALS:0 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:1, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:1 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:2, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:2 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:3, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:3 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:4, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:4 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:5, "メイン食材ID", 1), "食材分類", 1)%
		;野菜メイン料理３つ以上
		SIF MATCH(LOCALS, "野菜") < 3
			RETURN 0

		発動中コンボ名 = 健康蔬菜餐厅
		詳細文文字列受け渡し変数 = 使菜单效果带来的卢比获取量增加10％。
		RETURN 1
ENDSELECT

@料理メニューコンボ_饱腹谷物餐厅(ARGS)
SELECTCASE ARGS
	CASE "コンボ判定"
		LOCALS:0 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:1, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:1 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:2, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:2 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:3, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:3 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:4, "メイン食材ID", 1), "食材分類", 1)%
		LOCALS:4 = %DT_CELL_GETS("所持素材データベース", DT_CELL_GET("料理メニューデータベース", 現在設定メニュー:5, "メイン食材ID", 1), "食材分類", 1)%
		;穀物メイン料理３つ以上
		SIF MATCH(LOCALS, "穀物") < 3
			RETURN 0

		発動中コンボ名 = 饱腹谷物餐厅
		詳細文文字列受け渡し変数 = 使菜单效果带来的卢比获取量增加10％。
		RETURN 1
ENDSELECT
