
@指輪個別文章表示_精灵之戒・火
LOCALS = 
LOCALS += @"■精灵之戒・火<br><br>"
LOCALS += "寄宿上位火精灵之力的戒指。<br>"
LOCALS += "具备侧重攻击性能的能力与强化火属性特性的力量。<br>"
LOCALS += "寄宿了强敌「火灵巴塞洛缪斯」力量的极为稀有的戒指。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 15000

@指輪基礎効果文章表示_精灵之戒・火(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基础效果】<br>"
LOCALS += "最大ＨＰ+350 攻击力+450 行动速度+400 火强化+15 风耐性+10<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【选项效果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

;ランダム抽選では出現しない（入手法：ダンジョン「バルツ地下廃工場」でボス「火霊バルトロメウス」を初回撃破）
;@指輪出現率_精灵之戒・火(入手タイプ, 装備ランク)

@指輪ステータス決定_精灵之戒・火(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_350_攻撃力_450_行動速度_400_火強化_15_風耐性_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 4
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv4のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(4)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "精灵之戒・火", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
