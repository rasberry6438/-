;------------------------------------------------------------------------------
;谷間に射精すると開いて見せてくれる
;------------------------------------------------------------------------------
@PRE_AUTOCOM1

CALL SOURCE_CALC_露出_反感無し(AUTOCOM_実行番号保存, PLAYER, 50)
EXP:AUTOCOM_実行番号保存:精液経験 += 1

@PRE_AUTOCOM_ABLE1(キャラ番号)
#DIM キャラ番号
;巨乳以上
SIF TALENT:キャラ番号:バストサイズ < 1
	RETURN 0
;乳間射精直後
SIF RFLAG:フィニッシュ場所 != 3
	RETURN 0
;フィニッシュ相手であること
SIF キャラ番号 != RFLAG:フィニッシュ相手番号
	RETURN 0
SIF PLAYER != MASTER
	RETURN 0
RETURN 1

@MESSAGE_PRE_AUTOCOM1
PRINTFORM %CALLNAME:AUTOCOM_実行番号保存%将巨乳向两侧分开，乳沟间沾满
SIF RFLAG:フィニッシュフラグ > 1
	PRINT 大量的
PRINT 精液，
IF TALENT:AUTOCOM_実行番号保存:羞恥心 > 0
	PRINT 羞涩的
ELSEIF TALENT:AUTOCOM_実行番号保存:性的興味 && 知識素質:AUTOCOM_実行番号保存:性知識 < 3
	PRINT 饶有兴趣的
ELSEIF TALENT:AUTOCOM_実行番号保存:恋慕
	PRINT 充满爱意的
ENDIF
PRINTL 凝视着…

