;===================================================
;兜合わせ
;===================================================
@COMNAME91
#FUNCTIONS
TSTR:コマンド名受渡 = 龟头交合

;許可時の特別文章はここに記入
IF TFLAG:オートコマンドフラグ >= 4000
	;反転時、つまりあなたがタチ
	許可取り文章 = %CALLNAME:TARGET%想与%CALLNAME:PLAYER%进行[龟头交合]…
ELSE
	許可取り文章 = %CALLNAME:PLAYER%想与%CALLNAME:TARGET%进行[龟头交合]…
ENDIF


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_91
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
弱点コマンドカテゴリ = C_ちんこ_同性
オートコマンド許可 = 3
反転オート許可 = 3


;===================================================
;ツールチップ文章
;===================================================
@COM_TOOLTIP_91
TSTR:ツールチップ受渡 '= "<br>■龟头交合<br>"
TSTR:ツールチップ受渡 += "互相摩擦龟头冠状沟给予快感的指令。<br>进入龟头交合模式，在切换模式或结束性行为前将持续触发特定效果。"
TSTR:ツールチップ受渡 += "<br>指令类型：嘿咻<br>获取来源：快Ｃ・暴露・接触・不洁"
TSTR:ツールチップ受渡 += "<br>专属经验值：无<br>"


;===================================================
;コマンド実行処理
;===================================================
@COM91
JUMP 疑似性交COM("兜合わせ", 200, 200)


;===================================================
;実行許可判定
;===================================================
@CAN_COM91(ARG)
JUMP 疑似性交CAN_COM(ARG, "兜合わせ", 16)


;===================================================
;実行可能判定
;===================================================
@COM_ABLE91
;兜合わせ実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(91)
	RETURN RESULT

;PLAYER側の判定
SIF !疑似性交COM_ABLE_男性キャラ(PLAYER)
	RETURN 0
;ある程度勃起してないとダメ(PLAYER)
SIF BASE:PLAYER:勃起 < 1000
	RETURN 0
;TARGET側の判定
SIF !疑似性交COM_ABLE_男性キャラ(TARGET)
	RETURN 0
;ある程度勃起してないとダメ(TARGET)
SIF BASE:TARGET:勃起 < 1000
	RETURN 0
RETURN 1


;===================================================
;実行時メッセージ
;===================================================
@MESSAGE_COM91
PRINTFORML %CALLNAME:PLAYER%与%CALLNAME:TARGET%相对而坐，将彼此龟头冠状沟紧密贴合，开始摆动腰部摩擦…
