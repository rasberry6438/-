;===================================================
;貝合わせ
;===================================================
@COMNAME90
#FUNCTIONS
TSTR:コマンド名受渡 = 磨豆腐

;許可時の特別文章はここに記入
IF TFLAG:オートコマンドフラグ >= 4000
	;反転時、つまりあなたがタチ
	許可取り文章 = %CALLNAME:TARGET%想与%CALLNAME:PLAYER%进行[磨豆腐]…
ELSE
	許可取り文章 = %CALLNAME:PLAYER%想与%CALLNAME:TARGET%进行[磨豆腐]…
ENDIF


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_90
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
弱点コマンドカテゴリ = C_女_同性
オートコマンド許可 = 3
反転オート許可 = 3


;===================================================
;ツールチップ文章
;===================================================
@COM_TOOLTIP_90
TSTR:ツールチップ受渡 '= "<br>■磨豆腐<br>"
TSTR:ツールチップ受渡 += "互相摩擦阴唇给予快感的指令。<br>进入阴唇交合模式，在切换模式或结束性行为前将持续触发特定效果。"
TSTR:ツールチップ受渡 += "<br>指令类型：嘿咻<br>获取来源：快Ｃ・暴露・接触・不洁"
TSTR:ツールチップ受渡 += "<br>专属经验值：无<br>"


;===================================================
;コマンド実行処理
;===================================================
@COM90
JUMP 疑似性交COM("貝合わせ", 400, 400)


;===================================================
;実行許可判定
;===================================================
@CAN_COM90(ARG)
JUMP 疑似性交CAN_COM(ARG, "貝合わせ", 19)


;===================================================
;実行可能判定
;===================================================
@COM_ABLE90
;貝合わせ実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(90)
	RETURN RESULT

;PLAYER側の判定
SIF !疑似性交COM_ABLE_女性キャラ(PLAYER)
	RETURN 0
;TARGET側の判定
SIF !疑似性交COM_ABLE_女性キャラ(TARGET)
	RETURN 0
;潤滑が高くないとダメ(両者の合計)
SIF PALAM:PLAYER:潤滑 + PALAM:TARGET:潤滑 < 2000
	RETURN 0

;お風呂場プレイ中は游戏垫が無いとダメ
IF BATHROOM(CFLAG:PLAYER:現在マップ種別, CFLAG:PLAYER:現在位置)
	SIF ITEM:游戏垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1


;===================================================
;実行時メッセージ
;===================================================
@MESSAGE_COM90
PRINTFORM %CALLNAME:PLAYER%将湿润的阴唇贴合在仰卧的%CALLNAME:TARGET%
SIF PALAM:PLAYER:潤滑 >= PALAMLV:4 || PALAM:TARGET:潤滑 >= PALAMLV:4
	PRINT 湿漉漉的
PRINTL 阴唇上，开始缠绵的磨蹭动作…
