

@COMNAME23
#FUNCTIONS
TSTR:コマンド名受渡 = 任其摆布

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_23
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ_被挿入中")

@COM_TOOLTIP_23
LOCALS = <br>■任其摆布<br>
LOCALS += "保持被动允许对方自由行动的指令。<br>将连续触发5个自动指令。"
LOCALS += "<br>指令类型：嘿咻<br>获取来源：无"
LOCALS += "<br>专属经验值：无<br>"
TSTR:ツールチップ受渡 = %LOCALS%


;なすがまま
@COM23
TFLAG:オートコマンド連続発動 = 5
TFLAG:オートコマンド連続発動_なすがまま = 1

TIME += 10
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE23
;なすがまま実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(23)
	RETURN RESULT
;指示モード・同時モードだと発動しない
SIF FLAG:モード管理 != 0
	RETURN 0
;オートコマンド禁止だと発動しない
SIF GETBIT(FLAG:オートコマンド切り替えオプション, 1) && GETBIT(FLAG:オートコマンド切り替えオプション, 3)
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM23
PRINTFORML %CALLNAME:PLAYER%任由%CALLNAME:TARGET%为所欲为…

