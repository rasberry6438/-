@MODE_持続快楽_Ａ立ちバック(実行キャラ番号, 対象キャラ番号)
#DIM 実行キャラ番号
#DIM 対象キャラ番号
CALL MODE_持続快楽_Ａ挿入系(実行キャラ番号, 対象キャラ番号, 110, 190)


@MODETYPE_Ａ立ちバック(部位番号, 処理モード, プレイヤー番号, ターゲット番号)
#DIM 部位番号
#DIM プレイヤー番号
#DIM ターゲット番号
#DIMS 処理モード

IF 処理モード == "写真用文字列_プレイヤー"
	;モード固有の「～～をする」の文章を入れ込む
	詳細文文字列受け渡し変数 = 从后方将阴茎插入站立着的%CALLNAME:ターゲット番号%的肛门
	;下の部位専有判定と別の値を返す
	RETURN 部位状況_写真用
ELSEIF 処理モード == "写真用文字列_ターゲット"
	;モード固有の「～～をする」の文章を入れ込む
	IF 陥落チェック_性的(ターゲット番号) == 0 && BASE:ターゲット番号:酩酊 > 300
		;未陥落の泥酔姦。ヤってる最中にも酩酊は下がり続けるので、泥酔の500からちょい下げて300までで
		;むしろ未陥落でもないと別に酔ってコトに及んだ情報残してもシコくなさそうなので未陥落のみ
		詳細文文字列受け渡し変数 = %TEXTR("借着酒劲/被醉意驱使/烂醉如泥")%的%TEXTR("站立后入式/站立背面位姿势")%的%CALLNAME:プレイヤー番号%的%TEXTR("阴茎/肉棒")% 
		詳細文文字列受け渡し変数 += @"正将%TEXTR("肛门/后庭/菊穴/屁穴")%%TEXTR("当作性器使用/侵犯/彻底贯穿/玩弄/当成泄欲工具/粗暴开发")%"
	ELSE
		詳細文文字列受け渡し変数 = %TEXTR("站立后入式/站立背面位姿势")%的%CALLNAME:プレイヤー番号%
		詳細文文字列受け渡し変数 += @"%TEXTR("肉棒/阴茎")%已%TEXTR("插入/捅入")%%TEXTR("肛门/后庭/菊穴")%"
	ENDIF
	;下の部位専有判定と別の値を返す
	RETURN 部位状況_写真用
ENDIF

;0=ペニス 1=クリ 2=指（手） 3=口 4=胸 5=膣 6=アナル
;挿入で使用するのはペニスと膣で1を返す
SELECTCASE 部位番号
	CASE 部位_ペニス
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_占有
	CASE 部位_口
		;口は届かない
		RETURN 部位状況_届かない
	CASE 部位_胸
		;プレイヤーの胸は届かない
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_届かない
	CASE 部位_膣
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_届かない
	CASE 部位_アナル
		SIF 処理モード == "ターゲット"
			RETURN 部位状況_占有
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_届かない
ENDSELECT


@MODE_絶頂相手_Ａ立ちバック(絶頂部位, 処理モード, 判定結果)
#FUNCTION
#DIM 絶頂部位
#DIMS 処理モード
#DIM REF 判定結果
RETURNF MODE_絶頂相手_Ａ挿入系(絶頂部位, 処理モード, 判定結果)


@MESSAGE_MODE_Ａ立ちバック解除(実行キャラ番号, 対象キャラ番号)
#DIM 実行キャラ番号
#DIM 対象キャラ番号

PRINTFORML %CALLNAME:実行キャラ番号%缓缓从%CALLNAME:対象キャラ番号%的肛门里抽出阴茎
