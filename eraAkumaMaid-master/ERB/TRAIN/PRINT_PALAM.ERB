;INFOから呼び出す
@NEW_PRINT_PALAM, ARG
#DIM LCOUNT
#DIM NO_PALAM
#DIM BAR_SIZE
#DIM ROW_SIZE
#DIM MEMO

ROW_SIZE = 5
BAR_SIZE = 5

FOR LCOUNT, 0, 16

	;10,快Ｃ　11,快Ｖ　12,快Ａ　13,快Ｂ
	;14,快Ｍ　15,快尿　20,Ｖ潤　21,Ａ潤
	; 0,恭順　 1,欲情　 2,屈服　 3,習得
	; 4,恥情　 5,苦痛　 6,恐怖　 7,反感

	;弱点は粉紅に
	SELECTCASE LCOUNT
	CASE 0
		IF TALENT:ARG:淫核
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:Ｃ性向
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 10
	CASE 1
		IF TALENT:ARG:淫壷
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:Ｖ性向
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 11
	CASE 2
		IF TALENT:ARG:尻穴狂い
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:Ａ性向
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 12
	CASE 3
		IF TALENT:ARG:淫乳
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:Ｂ性向
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 13
	CASE 4
		IF TALENT:ARG:蕩唇
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:Ｍ性向
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 14
	CASE 5
		IF TALENT:ARG:尿道狂
			SETCOLOR DEF_COLOR("粉紅")
		ELSEIF TALENT:ARG:尿道性感
			SETCOLOR DEF_COLOR("爱心粉紅")
		ENDIF
		NO_PALAM = 15
	CASE 6
		NO_PALAM = 20
	CASE 7
		NO_PALAM = 21
	CASEELSE
		NO_PALAM = LCOUNT - 8
	ENDSELECT

	PRINTFORM %PALAMNAME:NO_PALAM%%TEXTS("調教パラメータのＬＶ", GET_PALAMLV(PALAM:ARG:NO_PALAM) )%

	RESETCOLOR

	SELECTCASE PALAMNAME:NO_PALAM
	CASE "快Ｃ"
		IF NOWEX:ARG:尿道絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASE "快Ｖ"
		IF NOWEX:ARG:Ｖ絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASE "快Ａ"
		IF NOWEX:ARG:Ａ絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASE "快Ｂ"
		IF NOWEX:ARG:Ｂ絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASE "快Ｍ"
		IF NOWEX:ARG:Ｍ絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASE "快尿"
		IF NOWEX:ARG:尿道絶頂
			CALL PRINT_BAR, "ＰＡＬＡＭ絶頂", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ELSE
			CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
		ENDIF
	CASEELSE
		CALL PRINT_BAR, "ＰＡＬＡＭ", PALAM:ARG:NO_PALAM, 0, BAR_SIZE
	ENDSELECT
	PRINTFORM %PALAM_NUM(PALAM:ARG:NO_PALAM)%　

	SIF ( (LCOUNT + 1) % ROW_SIZE) == 0
		PRINTL 
NEXT
SIF (LCOUNT + 1) % ROW_SIZE
	PRINTL 

@PALAM_NUM(ARG)
#FUNCTIONS
#DIM LCOUNT
;999999を超えたらkMGTP
;Emueraではペタ程度（64ビット）まで扱えるとのこと
VARSET LOCALS
FOR LCOUNT, 0, 5
	SIF ARG < 100000
		BREAK
	SELECTCASE LCOUNT
	CASE 0
		LOCALS = k
	CASE 1
		LOCALS = M
	CASE 2
		LOCALS = G
	CASE 3
		LOCALS = T
	CASE 4
		LOCALS = P
	ENDSELECT
	ARG /= 1000
NEXT
;数値の桁数に応じて間隔を調整
IF STRLENS(LOCALS)
	LOCALS = {ARG,5}%LOCALS%
ELSE
	LOCALS = {ARG,6}
ENDIF

RETURNF LOCALS

