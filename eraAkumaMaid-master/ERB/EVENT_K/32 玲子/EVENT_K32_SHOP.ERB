;-------------------------------------------------
;取引画面と、购物の際の台詞を記述します
;-------------------------------------------------
;取引画面での台詞。悪魔以外は大部分が不要です
;-------------------------------------------------
@KOJO_WISH_K32, ARGS
#DIM MEMO_LINECOUNT

$RYOKO_WISH_LP1
MEMO_LINECOUNT = LINECOUNT

SELECTCASE ARGS
CASE "取引開始"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の取引")
		;しかも挿入中！
		IF INSERT("挿入相手") == MASTER
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	ELSE
		IF TALENT:恋慕
			SELECTCASE RAND:5
				CASE 0
					CALL PRINT_STRW,  "「啊啦、怎么了吗？」"
					CALL PRINT_STRW,  "「什么事都可……虽然不能轻易地承诺，但还是让我做些什么吧」"
			ENDSELECT
			SIF LINECOUNT - MEMO_LINECOUNT
				RETURN 1
		ENDIF

		SELECTCASE RAND:5
			CASE 0
				CALL PRINT_STRW,  "「啊啦、突然怎么了？」"
				CALL PRINT_STRW,  "「想要拜托的事情？　……啊啦啊啦、发生什么事了吗」"
				CALL PRINT_STRW,  "（……不会拜托什么奇怪的事情吧？）"
			CASE 1
				CALL PRINT_STRW, @"「有想拜托%CNAME("一人称")%？的事情？」"
				CALL PRINT_STRW,  "「可以是可以……但不要拜托些什么奇怪的事情哦」"
		ENDSELECT
	ENDIF
	
	SIF LINECOUNT - MEMO_LINECOUNT
		RETURN 1
	GOTO RYOKO_WISH_LP1
CASE "愛の告白"
	;告白成功！
	IF TALENT:相思相愛 && CEVENT("イベント：初妊娠")
		CALL PRINT_STRW,  "「～～～～～～_／／／_／／／_／／／_」"
		CALL PRINT_STRW,  "（……呜呜。丢尽大人的脸面了、真是的……_BH_）"
		CALL PRINT_STRW, @"「……那个　虽然应该保密的、%CNAME("一人称")%、其实比%CNAME("二人称")%要年长很多」"
		CALL PRINT_STRW,  "「因为各种原因、那个、虽然穿成这样、所以、这个…………」"
		CALL PRINT_STRW,  "「……会对这种成熟女性动心什么的、真是的…………_BH_BH_BH_」"
		CALL PRINT_STRW,  "「有些事情还没有说出来，有些事情还不能说。…………_／／／_」"
		CALL PRINT_STRW,  "「如果这样也可以的话，这份心意，我会很感激的接受的。」"
		IF TALENT:MASTER:男性
			CALL PRINT_STRW, @"「…………%CNAME("二人称")%不是这种拔吊无情的渣男、真是太好了_／／／_」"
		ELSE
			CALL PRINT_STRW, @"「…………现在比起男孩子，更喜欢女孩子了，真是太好了_／／／_」"
		ENDIF
		PRINTFORMW 这么说着、%CNAME("玲子")%害羞的微笑着……
	;失敗
	ELSE
		IF !CEVENT("初回：告白失敗")
			CALL PRINT_STRW,  "「…………诶、啊、那个。欸？　等、等一下、突然、说什么……_／／／_」"
			CALL PRINT_STRW,  "（刚、刚才的、是什么？　什么情况？　诶？）"
			CALL PRINT_STRW,  "（没、没听错吧……应该、吧……？_／／／_）"
			CALL PRINT_STRW,  "「不是什么……玩笑、吧」"
			CALL PRINT_STRW, @"（那、那么%CNAME("一人称")%、是真的被表白了……？）"
			CALL PRINT_STRW, @"（%CNAME("一人称")%只是、为了任务……那个……_／／／_）"
			CALL PRINT_STRW,  "（虽……虽然是比我小、但被男人这么说、还是第一次、真是的……_BH_）"
			CALL PRINT_STRW,  "「……那个。心情、是、非常。额。開心……的」"
			CALL PRINT_STRW,  "「但是、那个」"
			CALL PRINT_STRW, @"（%CNAME("一人称")%还有、任务……）"
			CALL PRINT_STRW,  "「现在还有点、就是、那个」"
			CALL PRINT_STRW,  "「能不能就保持、肉体上的関系」"
			CALL PRINT_STRW,  "「…………对不起」"
			CALL PRINT_STRW,  "（…………但是、是吗_BH_）"
			IF TALENT:MASTER:男性
				CALL PRINT_STRW, @"（%CNAME("一人称")%、被男孩子表白了……_BH_）"
			ELSE
				CALL PRINT_STRW, @"（%CNAME("一人称")%、被女孩子表白了……_BH_）"
			ENDIF

			CALL PRINT_STRW,  "「～～～～_／／／_」"
			PRINTFORMW 抱歉地低下了頭的%CNAME("玲子")%的脸，连耳朵都被染红了……
			CALL SET_CEVENT, "初回：告白失敗"
		ELSE
			; ２回目以降
			SELECTCASE RAND:5
				CASE 0
					CALL PRINT_STRW,  "「真、真是的、又在说那种事情……」"
					CALL PRINT_STRW,  "「真让人困扰……_／／／_」"
				CASE 1
					CALL PRINT_STRW, @"「就保持%TEXTR("肉体上/炮友/性伙伴")%的関系不行吗……？」"
					CALL PRINT_STRW,  "「那个……心情、说实话、虽然很高兴……」"
					CALL PRINT_STRW,  "「……对不起」"
				CASE 2
					CALL PRINT_STRW,  "（呜呜呜……这、这种事情也太意外了。根本就没有想过……_／／／_）"
					CALL PRINT_STRW,  "（是认真的吗？　是真的在表白吗？　呜呜_BH_）"
					CALL PRINT_STRW,  "（该怎么办才好啊……っ_／／／_）"
					CALL PRINT_STRW,  "「…………_／／／_」"
					PRINTFORMW %CNAME("玲子")%很抱歉地摇着頭……
			ENDSELECT
		ENDIF
	ENDIF
;胖次のお願いした時に相手が没穿内褲だった
CASE "没穿内褲"
	IF COND("合意：胖次交易")
	ENDIF

	IF TALENT:恋慕
		SELECTCASE RAND:5
			CASE 0
				CALL PRINT_STRW,  "「对、对不起_／／／_　今天没有穿……_／／／_」"
				CALL PRINT_STRW, @"「因为、%CNAME("一人称")%的屁股不是很大吗？　每天都穿的话会很难受的……」"
				CALL PRINT_STRW,  "「住在老家的时候一直都没有穿的……_／／／_」"
		ENDSELECT
		SIF LINECOUNT - MEMO_LINECOUNT
			RETURN 1
		GOTO RYOKO_WISH_LP1
	ENDIF

	SELECTCASE RAND:5
		CASE 0
			CALL PRINT_STR,  "「…………虽然很不好意思、那个、今天没有穿」_W"
			CALL PRINT_STR,  "「反、反正没有什么関系吧？　也没有给谁添麻烦」_W"
			CALL PRINT_STR,  "（都是屁股太大的错、每天穿的话会很难受的……）_W"
	ENDSELECT

	SIF LINECOUNT - MEMO_LINECOUNT
		RETURN 1
	GOTO RYOKO_WISH_LP1

;胖次を要前の会話。
;ただし、悪魔の場合には魂を払っているので確実に渡すことになる
;ここで本来胖次を给かどうかなどを加味して分岐を設ける事も出来る。特に書かなくてもよい
;TFLAG:取引承諾 = 0とすると胖次拒否に行くようになり、TFLAG:取引承諾 = 1とすると判定無視で胖次を渡せるようになる
;TFLAG:取引価格 に数字を入れる事で好きな額を要求できるようになる。基準相場は$500です
;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで胖次を渡す展開に出来る
;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "胖次交易"
	;ＯＫ！
	IF TFLAG:取引承諾
		;日期と時間参照随机
		IF COND("合意：胖次交易") && (BASE:識別番号%(DAY*10 + TIME) )%4 == 0
			PRINTFORMW 
			TFLAG:取引価格 = 1000
		ELSE
			PRINTFORMW
		ENDIF
	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		TFLAG:値上げ交渉 = 500
	ENDIF
CASE "胖次"
	;値上げ交渉した
	IF TFLAG:値上げ交渉
		PRINTFORMW 
	;相場より高い額を要求されるままに払った
	ELSEIF TFLAG:取引価格 > 500
		PRINTFORMW 
	;値切って相場と変わらず$500払う。ここでTFLAG:取引承諾 = 0とすることで拒否するのもよいでしょう
	ELSEIF TFLAG:値切った
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF
	;胖次を脱いで渡してくる汎用地の文
	CALL DAILY_MESSAGE, "脱下内裤交了出去"

CASE "胖次拒否"
	;取引を拒否した際
	IF TFLAG:取引拒否
		SELECTCASE RAND:5
			CASE 0
				PRINTFORMW 
		ENDSELECT
	ELSE
		SELECTCASE RAND:5
			CASE 0
				CALL PRINT_STR, @"「抱歉、今天那个、不是很%TEXTR("色情/可爱")%所以不行」_W"
				CALL PRINT_STR,  "（呜呜。平时就穿一些比较时尚的胖次会比较好吗）_W"
		ENDSELECT
	ENDIF

	SIF LINECOUNT - MEMO_LINECOUNT
		RETURN 1
	GOTO RYOKO_WISH_LP1

;TFLAG:値上げ交渉 に数字を入れておくと、断られた際もその値上乗せで服を脱掉展開に出来る
;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "全裸交易"
	;ＯＫ！
	IF TFLAG:取引承諾
		PRINTFORMW 
	;断られる
	ELSE
		;これを指定しておくと、実行値が足りない際にこの額上乗せで応じてくれるようになる
		;TFLAG:値上げ交渉 = 2000
		PRINTFORMW 
	ENDIF
CASE "全裸"
	;値上げ交渉した("全裸交易"で設定した場合のみ)
	IF TFLAG:値上げ交渉
		PRINTFORMW 
	;取引初成功
	ELSEIF COND("合意：全裸化") == 0
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF
	;汎用地の文
	CALL EVENT_DATUI, "裸体"
CASE "全裸拒否"
	;取引を拒否した際("全裸交易"で設定した場合のみ)
	IF TFLAG:取引拒否
		PRINTFORMW 
	;二人きりなら脱いでくれるけど状況が悪い
	ELSEIF COND("合意：全裸化") && (CHARA_NUM("室内の邪魔者") || PLACE("個室") == 0)
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF

;TFLAG:取引拒否 = 1 とすると本来取引できるかに関わらず、取引しなくなる
CASE "事后避孕药取引"
		PRINTFORMW 

CASE "事后避孕药"
	;代金をまけてくれた場合
	IF TFLAG:値切った
		;所持金が足りなかった場合です
		;現状、ここに来るのは恋慕无の時のみ
		PRINTFORMW 
	;拝み倒したら代金を払わなくても許してくれた場合
	ELSEIF TFLAG:取引価格 == 0
		;現状、ここに来るのは恋慕有の時のみ
		PRINTFORMW 
	ELSE
		;普通にお金を払った場合
		;口上无でも自然な感じです
		;ですが恋慕のときなどは、なにか一言あってもいいかもですね
		PRINTFORMW 
	ENDIF

CASE "事后避孕药拒否"
	;これはCASE "事后避孕药取引"内でTFLAG:取引拒否 = 1 となる分岐を作った場合のみ
	IF TFLAG:取引拒否
		PRINTFORMW 

	;拝み倒して失敗した場合
	ELSEIF TFLAG:取引価格 == 0
		;轻轻地地の文が表示されます。口上有无どちらでも大丈夫そうです。
		PRINTFORMW 
	;其他、你が薬代を不支付場合
	ELSE
		;恋慕状態で、それなりに妊娠の恐惧がある場合
		IF ( COND("受精確認") || COND("胎内の精液量") ) && TALENT:恋慕
			;お金がないか、不支付を選んだ場合です。
			;それなら避妊できなくて妊娠しちゃっても仕方ないね…(爱心)って感じのやつです。
			;地の文が表示されるので何も書かないほうが良いかもしれません。
		;不支付、を選んだ場合
		ELSEIF MONEY >= TFLAG:取引価格
			;口上无でも自然な感じです
		;お金が足りなかった場合
		ELSE
			;不支付を選んだ場合も、お金が足りないときはここへ来ます。
		ENDIF
	ENDIF
CASE "同居拒否"

CASE "同居希望"

CASE "同居頻度的相談"

CASE "便当拒否"

CASE "做便当"
	;便当のお願い２回目以降
	IF COND("合意：便当")

	;以下は初めて作ってもらうよう变成了とき
	ELSE
		IF COND("同居中")

		ELSE

		ENDIF
	ENDIF

;COND("口服避孕薬派")かどうかで初回が"口服避孕薬を飲んで欲しい"or"口服避孕薬を止めて欲しい"に分かれます
CASE "口服避孕薬"
	;角色が口服避孕薬派なら、こっちだけ書いてください
	IF COND("口服避孕薬派")
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬

		;口服避孕薬の服用を止めてもらう
		ELSE
			;初回
			IF DEVENT(ARGS) == 0

			ELSE

			ENDIF
		ENDIF
	;角色が口服避孕薬派でないなら、こっちだけ書いてください
	ELSE
		;口服避孕薬を服用してもらう
		IF CFLAG:口服避孕薬
			;初回
			IF DEVENT(ARGS) == 0

			ELSE

			ENDIF
		;口服避孕薬の服用を止めてもらう
		ELSE

		ENDIF
	ENDIF
;COND("口服避孕薬派")の人に口服避孕薬服用を止めてもらうお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬中断拒否"

;COND("讨厌口服避孕薬")の人に口服避孕薬服用のお願いをしたけど聞き入れてもらえなかった場合
CASE "口服避孕薬服用拒否"

CASE "淫紋拒否"

;"淫紋贴纸直前"or"淫紋贴纸"の片方を書けば十分だと思います
CASE "淫紋贴纸直前"
	;初回
	IF DEVENT(ARGS) == 0

	ELSE

	ENDIF

CASE "淫紋贴纸"
	;初回
	IF DEVENT(ARGS) == 0
		CALL PRINT_STRW,  "「啊啦、怎么了吗？　贴纸？　诶？」"
		CALL PRINT_STRW, @"「……真、真是奇怪的样式。……想要贴？　在%CNAME("一人称")%身上？」"
		CALL PRINT_STRW,  "「…………_／／／_」"
		PRINTFORMW %CNAME("玲子")%红着脸看着贴着淫纹贴纸的底纸……
		CALL PRINT_STRW,  "（从外表上看、只是个普通的纹身贴……？　是从商店买的吗……？）"
		IF COND("夏")
			CALL PRINT_STRW,  "（上游泳课的时候不会从泳衣里面露出来吧……？）"
		ENDIF
		CALL PRINT_STRW,  "「……嘛啊、如果无论如何都要的话、可以哟」"
		CALL PRINT_STRW,  "「诶？　现在在这里？　……～～～～っ_／／／_」"
		PRINTFORMW %CNAME("玲子")%的脸被染红\@ EQUIP:裙 ?自己把裙子提了起来#小心翼翼地把下装往下拉\@在%CALLNAME:PLAYER%的眼前露出了下腹部……
		CALL PRINT_STRW,  "「……要做的话就快点　……嗯っ_BH_」"
		PRINTFORMW 贴纸在%CNAME("玲子")%的子宫的位置印上去了、发热的淫纹像生物一样蠕动着，嗖的一下就在%CNAME("玲子")%的下腹部定形了……
		CALL PRINT_STRW,  "「诶？　好热!?　啊_BH_　……呜呜嗯っ_BH_BH_BH_」"
		CALL PRINT_STRW,  "（讨、讨厌、这个、这个不是单纯的贴纸……!?_BH_　唔_BH_　噢_BH_）"
		CALL PRINT_STRW,  "「噫呜嗯っ_BH_BH_BH_」"
		PRINTFORMW 为了确认淫纹是否固定了用手指划过，%CNAME("玲子")%的身体颤抖了一下。
		CALL PRINT_STRW,  "「噢_BH_　呜_BH_　嗯噫_BH_　哦っ_BH_　唔_BH_」"
		CALL PRINT_STRW,  "（这是什么ぇっ_BH_　太糟糕了_BH_　这个太糟糕了ぃ_BH_）"
		PRINTFORMW 尝试着把手指伸进已经淌满蜜液的肉壶、手指令人惊讶地被柔软的肉吞没了。
		CALL PRINT_STRW,  "「哦っ_BH_　唔_BH_　为、手、手指ぃ_BH_　为什、为什么、要插进来、呜呜_BH_　唔゛_BH_BH_BH_」"
		PRINTFORMW 与抗议的语言相反，%CNAME("玲子")%的肉壶把钻进来的%CALLNAME:MASTER%的手指紧紧的勒住了……
		PRINTFORMW …………看来淫纹成功的在%CNAME("玲子")%的子宫处固定了。
		CALL PRINT_STRW,  "「唔……_BH_　哦……_BH_　哦哦……_BH_」"
		PRINTFORMW 把沾满淫蜜的手指拔出、%CNAME("玲子")%眼中含着泪水，一副荡然的表情，像在请求一样注的视着%CALLNAME:MASTER%……
	ELSE
		SELECTCASE RAND:5
			CASE 0
				CALL PRINT_STRW,  "「又、又要来……？_BH_」"
				CALL PRINT_STRW,  "「虽然是这样的……倒不是疼什么的……」"
				CALL PRINT_STRW,  "（上瘾的话……该怎么办……_BH_）"
			CASE 1
				CALL PRINT_STRW,  "「……那、那个。要贴就赶紧贴吧」"
				CALL PRINT_STRW,  "（摸子宫附近……_BH_　有种奇怪的感觉……_BH_）"
			CASE 2
				CALL PRINT_STRW,  "「…………_BH_」"
				PRINTFORMW %CNAME("玲子")%红着脸看着%CALLNAME:PLAYER%的手……
			CASE 3
				CALL PRINT_STRW,  "「……要做、这种事情的话」"
				CALL PRINT_STRW,  "「要好好、负起责任……_BH_」"
		ENDSELECT
	ENDIF
;"淫紋直前"or"淫紋"の片方を書けば十分だと思います
CASE "淫紋直前"
	SELECTCASE MARK:淫紋
	CASE 2
		IF EQUIP:裙
			PRINTFORM 自己撩起了裙子
		ELSE
			PRINTFORM 在%CALLNAME:PLAYER%眼前脱下了下装
		ENDIF
		PRINTFORM %CNAME("玲子")%
		IF DEVENT("淫紋贴纸")
			PRINTFORM 用期待的眼神看着%CALLNAME:MASTER%……
			CALL PRINT_STRW,  "（以前只是一个试用贴纸……_BH_）"
			CALL PRINT_STRW,  "（这次是真正的色情纹身……_BH_）"
			IF TALENT:恋慕
				CALL PRINT_STRW,  "「留下标记的责任……_BH_」"
				CALL PRINT_STRW,  "「要好好、负起来……_BH_」"
			ELSE
				CALL PRINT_STRW,  "（真、真让人、没办法……_BH_）"
				CALL PRINT_STRW,  "（这也是为了任务……_BH_　这、这种程度、不忍住的话……_BH_）"
			ENDIF
		ELSE
			PRINTFORM 用掩饰不住不安的眼神看着%CALLNAME:MASTER%……
			IF TALENT:恋慕
				CALL PRINT_STRW,  "「真是的……真的是、哼…………_BH_」"
				CALL PRINT_STRW, @"「要被留下标记了、%CNAME("一人称")%……_BH_」"
			ELSE
				CALL PRINT_STRW,  "（没想到会被纹上色情纹身什么的……）"
				CALL PRINT_STRW,  "（……但没办法啊。这也是为了任务）"
			ENDIF
		ENDIF
	CASE 3

	CASE 4

	ENDSELECT
CASE "淫紋"
	SELECTCASE MARK:淫紋
	CASE 2
		
	CASE 3

	CASE 4
		CALL PRINT_STRW,  "「啊ー……_BH_　啊啊ー…………_BH_」"
		PRINTFORMW 
		IF TALENT:恋慕
			CALL PRINT_STRW,  "「好厉害……啊、这个……_BH_　嗯噫っ_BH_」"
			CALL PRINT_STRW,  "「小穴的欲求不满……_BH_　可以清楚的感觉到……_BH_」"
			PRINTFORMW %CNAME("玲子")%变得面红耳赤，委身于从体内渗出的快乐之中……
			CALL PRINT_STRW,  "「都变成这样了……」"
			CALL PRINT_STRW,  "「不照顾我到最后……可不行哦……？_BH_BH_BH_」"
		ELSE
			CALL PRINT_STRW,  "（讨厌、不行……_BH_　不行的、这样的……_BH_）"
			CALL PRINT_STRW,  "（任务……_BH_　明明还有任务……_BH_）"
			CALL PRINT_STRW,  "「呜呜～～_BH_　っ噫、ぃ……_BH_」"
			PRINTFORMW %CNAME("玲子")%试图憋住甜味的气息，拼命让自己的身体平静下来。……
			CALL PRINT_STRW,  "（…………被、教育了_BH_）"
			CALL PRINT_STRW,  "（自己是个雌性的、事实用身体记住了…………_BH_）"
			CALL PRINT_STRW,  "「～～～～～～～～_BH_」"
			PRINTFORMW %CNAME("玲子")%火辣辣的脸好像在寻求什么似的热切地注视着%CALLNAME:MASTER%……
		ENDIF
	ENDSELECT

CASE "拜托処理性欲"


CASE "性処理の一時中断"
	IF TALENT:恋慕
		CALL PRINT_STRW,  "「诶？　啊……。那个……」"
		CALL PRINT_STRW, @"「已经厌倦%CNAME("一人称")%的……事情……了吗？」"
		CALL PRINT_STRW,  "「不是？　是、是吗……那就好……_BH_」"
		CALL PRINT_STRW,  "「那个、如果又想要了就要说出来哦？　不用忍耐。约好了哦？」"
	ELSE
		CALL PRINT_STRW,  "「诶？　那个……已经不用了、是什么意思？」"
		CALL PRINT_STRW,  "（那就……有点困扰了。会成为任务的阻碍啊）"
		CALL PRINT_STRW,  "（不过，让人觉得是一个咬住不放的女人就太糟了……）"
		CALL PRINT_STRW,  "（最好是在其他时机变得积极，让对方再次提出请求？）"
		CALL PRINT_STRW, @"「……知道了。但是、如果又想让%CNAME("一人称")%照顾的话不要犹豫哦？」"
	ENDIF

CASE "性処理拒否"

;悪魔、淫魔、妖狐専用
CASE "给我看尾巴"

CASE "给我看尾巴拒否"

;以下は悪魔専用
CASE "魂が足りない"

CASE "想要钱"

CASE "甜言蜜语"

CASE "謎之魅力"

CASE "射精量強化"
	;自分を相手にＨしてる最中にお願いされた
	IF COND("Ｈ最中の取引")
		;しかもＶ挿入中！
		IF INSERT("Ｖ挿入相手") == MASTER

		ELSE

		ENDIF
	ELSE

	ENDIF
CASE "巨根化"
	;これは"大きくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸", PLAYER)
	CASE "小"

	CASE "並"

	CASE "巨根"

	CASE "馬根"

	;腕並以上
	CASEELSE

	ENDSELECT
CASE "短小化"
	;これは"小さくなる前"のサイズなのに注意
	SELECTCASE CONDS("基本阴茎尺寸", PLAYER)
	CASE "並"

	CASE "巨根"

	CASE "馬根"

	;腕並、規格外
	CASEELSE

	ENDSELECT
ENDSELECT

;以下は汎用台詞(すでに喋っている場合には下には行かない)
SIF LINECOUNT > MEMO_LINECOUNT
	RETURN 1

;汎用拒否
IF STRCOUNT(ARGS, "拒否")
	SELECTCASE RAND:5
		CASE 0
			CALL PRINT_STR,  "「…………对、对不起。这个稍微」_W"
	ENDSELECT

	SIF MEMO_LINECOUNT - LINECOUNT
		RETURN 1
	GOTO RYOKO_WISH_LP1

;汎用取引（特に記述することがないならスキップ）
ELSEIF STRCOUNT(ARGS, "取引")

;汎用ＯＫ
ELSE
	SELECTCASE RAND:5
		CASE 0
			CALL PRINT_STR,  "「这种程度的话……可以哦」_W"
	ENDSELECT

	SIF MEMO_LINECOUNT - LINECOUNT
		RETURN 1
	GOTO RYOKO_WISH_LP1
ENDIF

; 量が多いのでひとまずは個別で対応済みの箇所のみループとする
;SIF MEMO_LINECOUNT - LINECOUNT
;	RETURN 1
;GOTO RYOKO_WISH_LP1

;-------------------------------------------------
;购物での台詞
;-------------------------------------------------
@KOJO_SHOP_K32, ARGS

SELECTCASE ARGS
CASE "一緒に出発"
	;初回
	IF SEVENT(ARGS) == 0

	;着替えてもらう
	ELSEIF CHECK_CLO("裸体") || CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿") || CHECK_CLO("内衣姿")

	ELSE

	ENDIF
CASE "目送"

CASE "没収胖次出発"
	;初回
	IF SEVENT(ARGS) == 0

	;既に没穿内褲
	ELSEIF EQUIP:胖次 == 0

	ELSE

	ENDIF
CASE "保持现在的装扮出発"
	IF CHECK_CLO("泳装姿")

	ELSEIF CHECK_CLO("体操服姿")

	ELSEIF CHECK_CLO("内衣姿") || CHECK_CLO("裸体")

	ENDIF
CASE "会計交代"
	;初回
	IF SEVENT(ARGS) == 0 || SEVENT("商品購入") == 0

	ELSE

	ENDIF
CASE "商品購入"
	;今買った物
	SELECTCASE STR:購入道具
	;ギリギリ通常商品の範疇or你用
	CASE "撮影機材", "潤滑液", "媚薬", "飞机杯"

	CASE "避孕套"
		;この商品を初めて買わされた
		IF SEVENT(STR:購入道具) == 0

		ELSE

		ENDIF
	;えっちな商品
	CASEELSE

	ENDSELECT
CASE "店内待機"
	;とりあえず何か一個買った時or33％で喋る
	SIF COND("買上品数") != 1 && RAND:3
		RETURN 0
	;胖次没収中
	IF COND("買い物胖次没収中")

	ELSE

	ENDIF
CASE "购物終了"
	;胖次没収中
	IF COND("買い物胖次没収中")

	;なにも買わなかった
	ELSEIF COND("買上品数") == 0

	ELSEIF CHECK_CLO("泳装姿") || CHECK_CLO("体操服姿")
	
	ENDIF
ENDSELECT

