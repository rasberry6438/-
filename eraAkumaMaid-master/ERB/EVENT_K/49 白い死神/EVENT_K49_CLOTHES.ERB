;-------------------------------------------------
;この角色独自の衣装を設定する。制服、体操服、泳装は既存のものから選ぶのが楽です
;-------------------------------------------------
@CLOTHES_K49, ARGS

SELECTCASE ARGS
;高めの頻度で着用します。設定しなくてもかまいません
CASE "内衣"
	IF RAND:2
		CALL SET_PANTIES, "紐丁字褲/純白"
	ELSE
		CSTR:胖次 = 褶边の胖次/純白/正面蝴蝶結紐青/
	ENDIF
	CSTR:胸罩 = 吊帯衫/純白/汗水浸湿的/蕾丝情趣内衣/透明/

;衣装に関わらず常に付けている飾品です。設定しなくてもかまいません
CASE "飾品"
	CALL SET_OTHER, "十字架と首飾り/古びた手鏡"

;学園での便装です。設定必須です。
;水手服, 露脐装超短裙水手服, 西装外衣, 露脐装超短裙西装外衣, 美式校服など
CASE "制服"
	CSTR:服 = 女仆装にしか見えない制服/連衣裙/黒/
	CSTR:裙 = 围裙/連衣裙/
	CSTR:襪子 = 吊帯襪/直到大腿/純白/
	CSTR:靴 = 小皮靴/直到脚腕/
;学園の体育の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;体操服布鲁马, 体操服五分褲, 体操服紧身短褲, 体操服运动衫など
CASE "体操服"
	CALL SET_COSTUME, "体操服五分褲"
;学園の水泳の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;死库水, 競泳泳装, 白比基尼, 黒比基尼など
CASE "泳装"
	CALL SET_COSTUME, "白比基尼"

;自宅など学園外で着ます。設定必須です
CASE "便装"
	IF DAYEV("夏服期間")
		CSTR:服 = 白色和金色装饰的民族衣装/連衣裙/提起裙角/腋露出/夏服/
		CSTR:裙 = 裙/連衣裙/白色和金色装饰的民族衣装/夏服/
	ELSE
		CSTR:服 = 白色和金色装饰的民族衣装/連衣裙/提起裙角
		CSTR:裙 = 裙/連衣裙/白色和金色装饰的民族衣装/
	ENDIF

	IF COND("冬")
		CALL SET_OTHER, "白金披风"
	ENDIF
	IF DAYEV("圣诞节") || DAYEV("新年")
		CALL SET_OTHER, "白と金の薄紗"
	ENDIF
	IF PLACE("自宅")
		CALL SET_OTHER, "忌避すべき大鎌"
	ENDIF
	CSTR:靴 = 
;自分的房間(not主人公的房間)で着ます
CASE "居家服"

;约会以外で家から外出際に着ます。情人旅館呼出とかね
CASE "外出装"
	ARGS = 便装
	RESTART
;もちろん约会の際に着ます
CASE "约会衣装"
IF CFLAG:髪型変更のお願い中 == 0
	CSTR:後髪 = 卷髪
	CSTR:改髪 = 侧盘頭
ENDIF

ENDSELECT


;-------------------------------------------------
;時間帯や場所に応じた髪型、表情の変化を記述する。設定しなくてもかまいません
;-------------------------------------------------
@MAIDFACE_K49
IF PLACE("自宅")
	IF DAYEV("夏服期間")
		CSTR:改髪 = 麻花辫
	ELSE
		CALL SETFLAG, "普段の髪型復元", TARGET
	ENDIF
ELSE
	SIF DAYEV("夏服期間")
		CSTR:改髪 = 麻花辫
ENDIF
