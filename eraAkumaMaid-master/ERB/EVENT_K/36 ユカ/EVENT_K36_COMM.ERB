;-------------------------------------------------
;部員同士での交流。自分はTARGETとして呼ばれ、話相手の部員はPARTNER()として呼ばれる
;話相手への呼びかけはCNAME("partner")で出来る
;TFLAG:台詞スキップ = 1で話している相手のセリフのスキップ、TFLAG:地の文スキップ = 1で続く地の文のスキップをする事が出来る
;-------------------------------------------------
@KOJO_COMM_K36, ARGS
#DIM MEMO_LINECOUNT
;対話相手にしたい角色の名前を/区切りでこれに突っ込んでFIND_TaiTa(EVENT_TABLE)とするとそのうち一人が選ばれる
#DIMS EVENT_TABLE

EVENT_TABLE = 
MEMO_LINECOUNT = LINECOUNT

SELECTCASE ARGS
;話しかける側のセリフ。COND("対話可能")を満たす相手を選んでPARTNER()にする事で、特定の相手に話しかけることもできる
CASE "お誘いする"
	;特定の相手に話しかける場合はEVENT_TABLEに対話相手の名前を / 区切りで並べて ; を消す
	;例１）弗理希艾尔or濡烏の君or西西莉亚から一人選ぶ場合
	;今のところ比較的コミュが取りやすそうな同級生or１年生or点心をくれるフェリ桑だけ。
	;話しかけると迷惑がられるor話題が噛み合わなさそうな人には主动は絡まない
	EVENT_TABLE = 弗理希艾尔/杏珠/哈娜/織/樱/アイリ
	;例２）部員全てから一人選ぶ場合
	;EVENT_TABLE = 全員

	IF FIND_TaiTa(EVENT_TABLE)
		;話し相手をpartnerにする
		CALL SET_PARTNER, FIND_TaiTa(EVENT_TABLE)
		CALL EMOTION_PLUS, "交流中", TARGET, PARTNER()
	ENDIF
		IF NO:PARTNER() == 1 && RAND:2
			IF COND("亲手自制点心",PARTNER() )
				PRINTFORMW 「%CNAME("partner")%！　今天的零食也非常好吃，谢谢你的努力！」
			ELSEIF COND("相性○", PARTNER() )
				PRINTFORMW 「%CNAME("partner")%！　%TEXTR("有什么我能帮你的吗？ /需要帮忙扫除吗？ /现在能玩吗？")%」
			ENDIF
		ELSEIF NO:PARTNER() != 1 && COND("相性○", PARTNER() )
			PRINTFORMW 「%TEXTR("呐、/嘿、/＿（口上原作者待施工的空位）")%%CNAME("partner")%！　%TEXTR("玩游戏! /玩吧! /今天有空的话不去卡拉ok吗？ /对战!/现在想起来了……/吃点心吗？ /你在干什么？ /快看，就是这个视频")%」
		ENDIF
;話しかけられる側のセリフ。括弧は『』を使うのが望ましい
CASE "誘われた"


ENDSELECT
