;SELFの汎用口上
@TRAINVOICE_SELF, ARGS
SELECTCASE ARGS
CASE "夜袭騎乗位", "夜袭Ａ騎乗位"
	;[高慢][傲慢][坏心眼][強気]持ちなら分岐
	IF TALENT:高慢 || TALENT:傲慢 || TALENT:強気 || TALENT:坏心眼
		PRINTFORMW 「…全都是 ……都是%CNAME("二人称")%的错！！…」
	;[害羞][清楚]持ちの場合分岐
	ELSEIF TALENT:害羞 || TALENT:清楚
		PRINTFORMW 「…………！！ …对不起……对不起…」
	;[容易迷恋][献身][小悪魔]持ちなら分岐
	ELSEIF TALENT:容易迷恋 || TALENT:献身 || TALENT:小悪魔
		IF COND("種族：人形")
			PRINTFORMW 「……请让%CNAME("一人称")%变得舒服起来吧……」
		ELSE
			PRINTFORMW 「……让%CNAME("一人称")%变得舒服起来吧……」
		ENDIF
	;[開朗][好色][接受快感]持ちの場合分岐
	ELSEIF TALENT:開朗 || TALENT:好色 || TALENT:接受快感
		PRINTFORMW 「…啊～！！ ……太棒了…」
	ENDIF

CASE "休憩：お浴室で本番"
	;[高慢][傲慢][坏心眼][強気]持ちなら分岐
	IF TALENT:高慢 || TALENT:傲慢 || TALENT:強気 || TALENT:坏心眼
		PRINTFORMW 「呼呼呼…看起来很舒服的样子…♪」
	;[害羞][清楚]持ちの場合分岐
	ELSEIF TALENT:害羞 || TALENT:清楚
		PRINTFORMW 「…啊、啊啊、…啊！ 嗯嗯…嗯、呜啊啊啊……！」
	;[容易迷恋][献身][小悪魔]持ちなら分岐
	ELSEIF TALENT:容易迷恋 || TALENT:献身 || TALENT:小悪魔
		IF COND("種族：人形")
			PRINTFORMW 「嘿嘿、看起来很棒呢……请、让%CNAME("一人称")%变得舒服起来吧……」
		ELSE
			PRINTFORMW 「……让%CNAME("一人称")%变得舒服起来吧……」
		ENDIF
	;[開朗][好色][接受快感]持ちの場合分岐
	ELSEIF TALENT:開朗 || TALENT:好色 || TALENT:接受快感
		PRINTFORMW 「大～葛葛、这种地方我是第一次来哦～？ 骗～你的啦♪」
	ENDIF
ENDSELECT


@TRAIN_MESSAGE_C
SELECTCASE SELECTCOM
CASE 0, 2, 5
CASE 7
	IF CHECK_COM("擦拭")
		IF TALENT:叛逆
			PRINTFORMW %CALLNAME:TARGET%对%CALLNAME:PLAYER%警戒地看了过来…
		ELSEIF TALENT:恋慕
			PRINTFORMW %CALLNAME:TARGET%開心地凝视着%CALLNAME:PLAYER%的脸、享受着充满関怀的后戏…
		ELSEIF TALENT:恋慕 + TALENT:淫乱 == 0 && MARK:屈服刻印 < 3 && ABL:信頼 == 0
			PRINTFORMW %CALLNAME:TARGET%在询问%CALLNAME:PLAYER%的情况…
		ELSEIF TALENT:害羞
			PRINTFORMW %CALLNAME:TARGET%的脸虽然因为害羞而变红、但似乎并不讨厌…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%一边回复着妖艳的反应、一边很開心地把身体交给了%CALLNAME:PLAYER%…
		ENDIF
	ENDIF
;振動棒脱着時
CASE 31
	IF TEQUIP:振動棒 == 0
		IF TALENT:叛逆
			PRINTFORMW %CALLNAME:TARGET%用凶狠的表情盯着%CALLNAME:PLAYER%
		ELSEIF TEQUIP:推倒 == 0
			SELECTCASE CUP:快Ｖ
			CASE IS < 300
				PRINTFORMW %CALLNAME:TARGET%因为身体里的异物感消失了、露出了放心的表情……
			CASE IS < 1000
				PRINTFORMW %CALLNAME:TARGET%在被拔出振动棒时、因为感觉粘膜发痒而发出小小的可爱喘息声…
			CASE IS < 3000
				PRINTFORMW %CALLNAME:TARGET%因为身体苦闷而发出小小的喘息声…
			CASE IS < 6000
				PRINTFORMW %CALLNAME:TARGET%的膣内散发出令人害羞的%TEXTS("气味")%、同时也充满了爱液……
			CASE IS < 10000
				PRINTFORMW …最后一次插入到深处再拔出来、%CALLNAME:TARGET%咻的一声喷出爱液、身体砰的一声跳了起来…
			CASEELSE
				PRINTFORMW 在拔出来之前被尽情地激烈地抽插着、%CALLNAME:TARGET%的全身乱翻乱滚、露出了很舒服的表情…
			ENDSELECT
		ENDIF
	ENDIF
;Ａ振動棒脱着時
CASE 33
	IF TEQUIP:肛用振動棒 == 0
		IF TALENT:叛逆
			PRINTFORMW 无言的压力向%CALLNAME:PLAYER%袭来……
		ELSEIF TEQUIP:推倒 == 0
			SELECTCASE CUP:快Ａ
			CASE IS < 300
				PRINTFORMW %CALLNAME:TARGET%因为被释放而发出放心的叹息…
			CASE IS < 1000
				PRINTFORMW %CALLNAME:TARGET%因为屁股里的违和感而露出了微妙的表情…
			CASE IS < 3000
				PRINTFORMW %CALLNAME:TARGET%因为不净之穴里带来的快感、无法忍受地发出了喘息声…
			CASE IS < 6000
				PRINTFORMW %CALLNAME:TARGET%看着%CALLNAME:PLAYER%的样子、发出了混杂着甜和快乐的叹息…
			CASE IS < 10000
				PRINTFORMW %CALLNAME:TARGET%一边抚摸着菊穴、一边期待着下一次的快乐…
			CASEELSE
				IF COND("視力") && RAND:2
					PRINTFORMW %CALLNAME:TARGET%用带着热情的目光、不满足地凝视着開放的肛門…
				ELSE
					PRINTFORMW %CALLNAME:TARGET%那贪婪的肛門、像是在惋惜着自己没有被填满一样地颤抖着…
				ENDIF
			ENDSELECT
		ENDIF
	ENDIF
CASE 75
	PRINTFORM %CALLNAME:TARGET%
	IF TALENT:叛逆
		IF TALENT:高慢 + TALENT:強気 && RAND:2
			PRINTFORM 在皱着眉的同时
		ELSE
			PRINTFORM 将目光从%CALLNAME:PLAYER%身上移開、
		ENDIF
		PRINTFORMW 重复着单调的上下运动…
	ELSEIF COND("脚の使用") && (TALENT:好色 || ABL:欲望 >= 3) && RAND:3 == 0
		IF CHECK_SHIRT("和服") || CHECK_SHIRT("巫女")
			PRINTFORM 用嘴叼着和服的下摆、
		ELSEIF CHECK_SHIRT("女仆装")
			PRINTFORM 用嘴叼着卷起来的裙子、
		ELSEIF CHECK_SHIRT("旗袍")
			PRINTFORM 拉起了旗袍的下摆并支起膝盖、
		ELSEIF EQUIP:裙
			PRINTFORM 为了能清楚露出%SKIRT(TARGET, "略")%的里面而张開双腿、
		ELSEIF RAND:2
			PRINTFORM 像是要把股间突出来一样支起了膝盖、
		ELSE
			PRINTFORM 慢慢地上下移动着腰、
		ENDIF
		IF ABL:Ａ拡張 >= 3 && TALENT:好色 + TALENT:淫乱
			PRINTFORMW 展示着肛門被阴茎拉扯、把阴茎埋入深处的样子…
		ELSE
			PRINTFORMW 一边展示着结合部位一边扭动着腰…
		ENDIF
	;エロい行動
	ELSEIF TALENT:好色 + TALENT:淫乱 && RAND:2
		IF TALENT:妊娠 && RANDIF(3, COND("指の使用"), TALENT:巨乳, ABL:Ａ感覚 >= 3, ABL:施虐属性 >= 3, TALENT:大力士)
			PRINTFORM 不顾自己身上那运动困难的大肚子、
		ELSEIF COND("指の使用") && RANDIF(3, TALENT:巨乳, ABL:Ａ感覚 >= 3, ABL:施虐属性 >= 3, TALENT:大力士)
			PRINTFORM 把手贴着%CALLNAME:PLAYER%的胸口上、
		ELSEIF TALENT:大力士 && RANDIF(3, TALENT:巨乳, ABL:Ａ感覚 >= 3, ABL:施虐属性 >= 3)
			PRINTFORM 按住了%CALLNAME:PLAYER%的两只手腕把身体的动作封住了、并
		ELSEIF ABL:施虐属性 >= 3 && RANDIF(3, TALENT:巨乳, ABL:Ａ感覚 >= 3)
			PRINTFORM 一边拧着%CALLNAME:PLAYER%的乳頭、
		ELSEIF ABL:Ａ感覚 >= 3 && TALENT:男性 == 0 && RANDIF(3, TALENT:巨乳)
			PRINTFORM 移动了身体、用两个手指掰開%TEXTS("阴部：视覚")%看着、并
		ELSEIF TALENT:巨乳 && RAND:3 == 0
			PRINTFORM 揉着和身体相比过分丰满的胸部、一边展示着用舌頭转着舔乳頭的样子、一边
		ELSEIF RAND:2
			PRINTFORM 在展示着结合的地方的同时、
		ELSE
			PRINTFORM 转过身来、\@ TALENT:巨乳 ? 在大大的胸部摇动着 # 在身体转动\@的同时、一边
		ENDIF
		IF ABL:腰靈活 >= 3 && RANDIF(3, ABL:腰靈活, ABL:腰靈活 >= 2, ABL:腰靈活 >= 3)
			PRINTFORM 扭动着腰
		ELSEIF ABL:腰靈活 >= 3 && RANDIF(3, ABL:腰靈活, ABL:腰靈活 >= 2)
			PRINTFORM 用肠壁磨擦着阴茎
		ELSEIF ABL:腰靈活 >= 2 && RANDIF(3, ABL:腰靈活)
			PRINTFORM 像是画圆一样的扭着腰
		ELSEIF ABL:腰靈活 && RAND:3 == 0
			PRINTFORM 使劲地上下扭腰
		ELSEIF ABL:欲望 > MAX(3, ABL:奉仕精神) && RAND:3 == 0
			PRINTFORM 用龟頭摩擦着令自己变得舒服的地方
		ELSEIF RAND:2
			PRINTFORM 把套弄着阴茎的菊穴\@ ABL:Ａ名器度 >= 40 ? 紧紧地 # \@收缩
		ELSE
			PRINTFORM \@ ABL:欲望 >= 3 ? 积极地 # \@动起了腰
		ENDIF
		IF (ABL:欲望 >= MAX(3, ABL:奉仕精神) || TEQUIP:推倒) && RAND:2
			PRINTFORM 来贪图快感
		ELSEIF ABL:奉仕精神 >= MAX(3, ABL:欲望) && RAND:2
			PRINTFORM 来进行服务
		ENDIF
		PRINTFORMW …
	;汎用
	ELSE
		IF TALENT:妊娠 && RANDIF(2, COND("指の使用"), ABL:施虐属性 >= 3, TALENT:大力士)
			PRINTFORM 不顾自己身上那运动困难的大肚子、
		ELSEIF TALENT:大力士 && RANDIF(2, COND("指の使用"), ABL:施虐属性 >= 3)
			PRINTFORM 按住了%CALLNAME:PLAYER%的两只手腕把身体的动作封住了、并
		ELSEIF ABL:施虐属性 >= 3 && RANDIF(2, COND("指の使用") )
			PRINTFORM 一边拧着%CALLNAME:PLAYER%的乳頭、
		ELSEIF COND("指の使用") && RAND:2
			PRINTFORM 把手贴着%CALLNAME:PLAYER%的胸口上、
		ELSE
			PRINTFORM 转过身来、\@ TALENT:巨乳 ? 在大大的胸部摇动着 # 在身体转动\@的同时、一边
		ENDIF
		IF ABL:腰靈活 && RAND:3 == 0
			PRINTFORM 使劲地上下扭腰
		ELSEIF RAND:2
			PRINTFORM 把套弄着阴茎的菊穴\@ ABL:Ａ名器度 >= 40 ? 紧紧地 # \@收缩
		ELSE
			PRINTFORM \@ ABL:欲望 >= 3 ? 积极地 # \@动起了腰
		ENDIF
		SIF ABL:奉仕精神 >= MAX(3, ABL:欲望) && RAND:2
			PRINTFORM 来进行服务
		PRINTFORMW …
	ENDIF
;口内放尿など
CASE 138
	IF CHECK_COM("口内放尿")
		IF CHECK("口内放尿")
			IF EXP:飲尿経験
				IF TEQUIP:口枷 && RAND:2
					PRINTFORMW 由于口枷的原因无法合上嘴的%CALLNAME:TARGET%、一边为了防止溢出来而抬高下巴、一边摆出若无其事的表情享受着饮尿PLAY…
				ELSE
					PRINTFORMW 从%CALLNAME:PLAYER%的举动中察觉到这一点的%CALLNAME:TARGET%、用已经习惯的喉咙一边发出咕噜咕噜的声音一边喝光了…
				ENDIF
			ELSE
				PRINTFORMW %CALLNAME:TARGET%对突然发生的事感到惊讶不已、通过用鼻子呼吸总算是喝光了…
			ENDIF
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			IF EXP:飲尿経験
				PRINTFORMW %CALLNAME:TARGET%は経験済みと是え倒錯極圆圈この行為に目を白黒させながら何とか飲もうとしている…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%は突然の事に目を白黒させながら何とか飲もうとしている…
			ENDIF
			PRINTFORMW …但、怎么样也咽不下全部、不小心从口边溢出来的尿弄得浑身都是…
			STAIN:Ｂ = SET_STAIN(STAIN:Ｂ, "尿")
		ELSE
			PRINTFORM %CALLNAME:TARGET%没有喝下%CALLNAME:PLAYER%大部分的尿、把尿都吐出来之后、
			IF TALENT:強気 && RANDIF(1, TALENT:高慢)
				PRINTFORMW 再狠狠地瞪了一眼…
			ELSEIF TALENT:高慢
				PRINTFORMW 发出了仿佛看见了脏东西的目光…
			ELSE
				PRINTFORMW 悲伤地回望着…
			ENDIF
		ENDIF
	ELSEIF CHECK_COM("子宮内放尿")
		IF CHECK("膣内放尿")
			IF ABL:受虐属性 >= 3 || TALENT:淫乱
				PRINTFORMW %CALLNAME:TARGET%一边呼出灼热的喘息、一边闭上眼睛享受着最重要的地方被污染的感觉……
			ELSEIF EXP:子宮浴尿経験 == 0
				PRINTFORMW 虽然%CALLNAME:TARGET%似乎并不是生气、但还是用着复杂的表情看向%CALLNAME:PLAYER%…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%虽然对做了变态PLAY的%CALLNAME:PLAYER%轻轻地责备了、但在脸上看到的却是心情舒畅的表情…
			ENDIF
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			PRINTFORMW %CALLNAME:TARGET%用难以回应的温暖笑容看着%CALLNAME:PLAYER%…
		ELSE
			PRINTFORM %CALLNAME:TARGET%向%CALLNAME:PLAYER%
			IF TALENT:強気 && RANDIF(1, TALENT:高慢)
				PRINTFORMW 狠狠地瞪了一眼…
			ELSEIF TALENT:高慢
				PRINTFORMW 投来了看着脏东西的目光…
			ELSE
				PRINTFORMW 悲伤地回望着…
			ENDIF
		ENDIF
	ELSEIF CHECK_COM("膣内放尿")
		IF CHECK("膣内放尿")
			IF ABL:受虐属性 >= 3 || TALENT:淫乱
				PRINTFORMW %CALLNAME:TARGET%一边呼出灼热的喘息、一边闭上眼睛享受着膣内被污染的感觉…
			ELSEIF EXP:膣内浴尿経験 == 0
				PRINTFORMW 虽然%CALLNAME:TARGET%似乎并不是生气、但还是用着复杂的表情看向%CALLNAME:PLAYER%…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%虽然对做了变态PLAY的%CALLNAME:PLAYER%轻轻地责备了、但在脸上看到的却是心情舒畅的表情…
			ENDIF
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			PRINTFORMW %CALLNAME:TARGET%用难以回应的温暖笑容看着%CALLNAME:PLAYER%…
		ELSE
			PRINTFORM %CALLNAME:TARGET%向%CALLNAME:PLAYER%
			IF TALENT:強気 && RANDIF(1, TALENT:高慢)
				PRINTFORMW 狠狠地瞪了一眼…
			ELSEIF TALENT:高慢
				PRINTFORMW 投来了看着脏东西的目光…
			ELSE
				PRINTFORMW 悲伤地回望着…
			ENDIF
		ENDIF
	ELSEIF CHECK_COM("温泉浣腸")
		IF CHECK("温泉浣腸")
			PRINTFORMW %CALLNAME:TARGET%在直肠末端被碰到后、感受到尿流进大肠里的感觉、脊梁一边颤抖一边享受着…
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			PRINTFORMW %CALLNAME:TARGET%用难以回应的温暖笑容看着%CALLNAME:PLAYER%…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%用一脸难以置信的样子看着%CALLNAME:PLAYER%…
		ENDIF
	ELSEIF CHECK_COM("乳内放尿")
		IF CHECK("乳内放尿")
			IF EXP:乳内浴尿経験 == 0
				PRINTFORMW 虽然%CALLNAME:TARGET%似乎并不是生气、但还是用着复杂的表情看向%CALLNAME:PLAYER%…
			ELSEIF ABL:受虐属性 >= 3 || TALENT:淫乱
				PRINTFORMW %CALLNAME:TARGET%一边呼出灼热的喘息、一边闭上眼睛享受着乳房、而且是乳房内部被污染的感觉…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%虽然对做了更加变态的PLAY的%CALLNAME:PLAYER%轻轻地责备了、但在脸上看到的却是心情舒畅的表情…
			ENDIF
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			IF RAND:2
				PRINTFORMW %CALLNAME:TARGET%好像很担心以后胸部会不会有残留的尿臭味…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%用难以回应的温暖笑容看着%CALLNAME:PLAYER%…
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:TARGET%向%CALLNAME:PLAYER%
			IF TALENT:強気 && RANDIF(1, TALENT:高慢)
				PRINTFORMW 狠狠地瞪了一眼…
			ELSEIF TALENT:高慢
				PRINTFORMW 投来了看着脏东西的目光…
			ELSE
				PRINTFORMW 悲伤地回望着…
			ENDIF
		ENDIF
	ELSEIF CHECK_COM("膀胱内放尿")
		IF CHECK("膀胱内放尿")
			IF EXP:輸尿経験 == 0
				PRINTFORMW 虽然%CALLNAME:TARGET%似乎并不是生气、但还是用着复杂的表情看向%CALLNAME:PLAYER%…
			ELSEIF ABL:受虐属性 >= 3 || TALENT:淫乱
				PRINTFORMW %CALLNAME:TARGET%一边呼出灼热的喘息、一边闭上眼睛享受着膀胱里被注入尿尿的感觉…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%虽然对做了更加变态的PLAY的%CALLNAME:PLAYER%轻轻地责备了、但在脸上看到的却是心情舒畅的表情…
			ENDIF
		ELSEIF TALENT:恋慕 || TALENT:淫乱 || ABL:信頼 >= 5 || MARK:屈服刻印 >= 3
			IF RAND:2
				PRINTFORMW %CALLNAME:TARGET%因为急速提高的尿意而感到辛苦…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%用难以回应的温暖笑容看着%CALLNAME:PLAYER%…
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:TARGET%向%CALLNAME:PLAYER%
			IF TALENT:強気 && RANDIF(1, TALENT:高慢)
				PRINTFORMW 狠狠地瞪了一眼…
			ELSEIF TALENT:高慢
				PRINTFORMW 投来了看着脏东西的目光…
			ELSE
				PRINTFORMW 悲伤地回望着…
			ENDIF
		ENDIF
	ENDIF
;什麼也不做
CASE 139
	;騎乗位中でCALL TEXT, "追加地の文：騎乗位"が発生した
	IF INSERT("騎乗位")

		SIF PALAM:欲情 >= 10000 || TALENT:好色 + TALENT:淫乱
			RETURN 0

		;淫乱or欲情4LV以上なら追加
		IF PALAM:欲情 >= 10000 && ABL:腰靈活 >= 3 && EXP:腰靈活経験 >= 20
			IF INSERT("Ｖ挿入")
				SIF ABL:Ｖ感覚 >= 3 && EXP:Ｖ性交経験 >= 20
					RETURN 0
			ELSEIF INSERT("Ａ挿入")
				SIF ABL:Ａ感覚 >= 3 && EXP:Ａ性交経験 >= 20
					RETURN 0
			ELSEIF INSERT("尿道挿入")
				SIF TALENT:尿道性感 && EXP:尿道性交経験 >= 20
					RETURN 0
			ENDIF
		ENDIF
	ENDIF
	IF INSERT("挿入")
		;貪欲
		IF (COND("要去了") || ABL:欲望 + TALENT:好色 >= 3) && COND("疲労") == 0 && (BASE:性欲 < MAXBASE:性欲 || TALENT:淫乱 || RAND:4 == 0)
			IF COND("要去了")
				PRINTFORM 接近绝顶的%CALLNAME:TARGET%无法忍受突然的停止、
			ELSE
				PRINTFORM %CALLNAME:TARGET%
				IF RAND:2
					SIF CHOSECOM(SELECTCOM) == 0
						PRINT 突然的
					PRINTFORM 放置PLAY似乎无法忍受、
				ELSE
					PRINTFORM 并不满足、
				ENDIF
			ENDIF

			IF TALENT:強気 + TALENT:高慢 && RANDIF(1, TALENT:献身, TALENT:小悪魔, TALENT:害羞)
				PRINTFORM 一边对%CALLNAME:PLAYER%恶言相向、另一边
			ELSEIF TALENT:害羞 && RANDIF(1, TALENT:献身, TALENT:小悪魔)
				PRINTFORM 虽然有点害羞、但还是
			ELSEIF TALENT:小悪魔 && RANDIF(1, TALENT:献身)
				PRINTFORM 一边目不转睛地盯着停止了腰部运动的%CALLNAME:PLAYER%、一边
			ELSEIF TALENT:献身
				PRINTFORM 一边注意着要让%CALLNAME:PLAYER%变得舒服起来、一边
			ENDIF
			PRINTFORMW 自己一个就開始\@ ABL:腰靈活 < 5 ? 僵硬地 # \@动起了腰身…
		;休憩に同意
		ELSEIF COND("疲労")
			PRINTFORMW %CALLNAME:TARGET%似乎很累了、保持着紧紧相连的姿势和%CALLNAME:PLAYER%一同休息着…
		ENDIF
	ELSEIF INSERT("挿入中")
		;休憩に同意
		IF COND("疲労")
			SIF INSERT("騎乗位", PLAYER) == 0
				PRINT …但是、
			PRINTFORMW %CALLNAME:TARGET%似乎很累了、保持着紧紧相连的姿势和%CALLNAME:PLAYER%一同休息着…
		ELSEIF INSERT("騎乗位", PLAYER)
			PRINTFORMW %CALLNAME:TARGET%虽然看起来很难移动、但还是会自己一个就把腰抬起来、\@ ABL:腰靈活 < 5 ? 僵硬地 # \@扭动着腰…
		ELSE
			PRINTFORMW …%CALLNAME:TARGET%不能抵抗是好事、%CALLNAME:MASTER%就这样一直随心所欲地侵犯着！
		ENDIF
	ELSEIF COND("液体浣腸") && RANDIF(1, TEQUIP:利尿剤, TEQUIP:拘束, TEQUIP:眼罩, TEQUIP:口枷)
		PRINTFORMW %CALLNAME:TARGET%一边流着汗、调整着呼吸、一边注意着%CALLNAME:PLAYER%的动向…
	ELSEIF TEQUIP:口枷 && RANDIF(1, TEQUIP:利尿剤, TEQUIP:拘束, TEQUIP:眼罩)
		PRINTFORMW %CALLNAME:TARGET%一边用鼻子缓慢地呼吸、注意不让口水流下来、一边注意着%CALLNAME:PLAYER%的动向…
	ELSEIF TEQUIP:眼罩 && RANDIF(1, TEQUIP:利尿剤, TEQUIP:拘束)
		PRINTFORMW %CALLNAME:TARGET%似乎认为%CALLNAME:PLAYER%会把自己放在一边去其他的地方、好像专心的听着…
	ELSEIF TEQUIP:拘束 && RANDIF(1, TEQUIP:利尿剤)
		PRINTFORMW %CALLNAME:TARGET%就以这样的姿势一直忍耐着…
	ELSEIF TEQUIP:利尿剤
		PRINTFORMW %CALLNAME:TARGET%在扭扭捏捏的同时、注意着%CALLNAME:PLAYER%的动向…
	ENDIF
;強制口交
CASE 140
	PRINTFORM %CALLNAME:TARGET%
	IF TALENT:叛逆
		IF TALENT:高慢 || TALENT:強気
			PRINTFORM 用锐利的目光盯着%CALLNAME:PLAYER%、
		ELSE
			PRINTFORM 带着已经放弃了的表情、
		ENDIF
	;環形口枷
	ELSEIF COND("開口口枷") && RAND:3 == 0
		IF ABL:喉拡張 >= SIZE("阴茎", PLAYER) && RAND:3 == 0
			PRINTFORM 一边咽下与阴茎的%TEXTS("气味")%一起溢出的唾液、一边
		ELSEIF RAND:2 == 0
			PRINTFORM 一边从口枷的缝隙间滴着口水、一边
		ELSE
			PRINTFORM 一边从下巴上滴下泡沫般的口水、一边
		ENDIF
	;奉仕精神3LVor屈服2LV+技巧3LV
	ELSEIF (MARK:屈服刻印 >= 2 || ABL:奉仕精神 >= 3 || TALENT:喉性感) && ABL:技巧 >= 3 && CUP:苦痛 <= 100
		IF ABL:喉拡張 > SIZE("阴茎", PLAYER) && RAND:3 == 0
			;IF RAND:2
			;	PRINTFORM 喉奥を犯されて涙目になり的同时、
			;ELSE
				PRINTFORM 在张開喉咙甚至进入食道的同时、
			;ENDIF
		ELSEIF ABL:精液中毒 >= 3 && RAND:4 == 0
			PRINTFORM 在闭上眼睛享受着味道和%TEXTS("气味")%的同时、
		ELSEIF ABL:受虐属性 >= 3 && RAND:4 == 0
			PRINTFORM 一边因为被当做道具来对待而兴奋、一边
		ELSEIF ABL:技巧 >= 3 && RAND:3 == 0
			PRINTFORM 灵活地使用舌頭、
		ELSEIF RAND:2
			PRINTFORM 在因兴奋和痛苦而脸红的同时、
		ELSE
			PRINTFORM 在用鼻子喘着粗气的同时、
		ENDIF
	ELSE
		;強気系で技巧がそこそこ以上ある
		IF ABL:技巧 >= 3 && TALENT:高慢 + TALENT:強気 + TALENT:倒錯的 && TALENT:坦率 + TALENT:内気 == 0
			IF COND("視力") && RAND:3 == 0
				SIF CUP:苦痛 >= 500 || (PREVCOM == SELECTCOM && CUP:苦痛 >= 100)
					PRINT 在用泪汪汪的眼睛
				PRINTFORM 看着%CALLNAME:PLAYER%的脸颊的同时、
			ELSEIF RAND:2 == 0
				PRINTFORM 被当做道具来对待、身体在颤抖着的同时、
			ELSE
				SIF CUP:苦痛 >= 500 || PREVCOM == SELECTCOM || RAND:3 == 0
					PRINT 在痛苦的
				PRINTFORM 脸上通红的同时、
			ENDIF
		ELSE
			IF RAND:3 == 0 && COND("指の使用")
				IF RAND:2
					PRINTFORM 在无意识间用手按住%CALLNAME:PLAYER%的腰的同时、
				ELSE
					PRINTFORM 用柔弱的手将%CALLNAME:PLAYER%推回去的同时、
				ENDIF
			ELSEIF RAND:2 == 0
				SIF CUP:苦痛 >= 500 || PREVCOM == SELECTCOM || RAND:3 == 0
					PRINT 在痛苦的
				IF TALENT:内気
					PRINTFORM 泪流满面的同时、
				ELSE
					PRINTFORM 眼水汪汪的同时、
				ENDIF
			ELSE
				SIF CUP:苦痛 >= 500 || PREVCOM == SELECTCOM || RAND:3 == 0
					PRINT 在痛苦的
				PRINTFORM 脸上通红的同时、
			ENDIF
		ENDIF
	ENDIF

	;奉仕精神3LVor屈服2LV+技巧3LV
	IF (MARK:屈服刻印 >= 2 || ABL:奉仕精神 >= 3 || TALENT:喉性感) && ABL:技巧 >= 3 && CUP:苦痛 <= 100
		IF RAND:4 == 0
			PRINTFORMW 贪婪地寻求着%CALLNAME:PLAYER%…
		ELSEIF RAND:3 == 0
			PRINTFORMW 习以为常地刺激着阴茎…
		ELSEIF RAND:2 == 0
			PRINTFORMW 享受着被侵犯的快乐…
		ELSE
			PRINTFORMW 积极地享受着这种PLAY…
		ENDIF
	ELSE
		IF TALENT:嘔吐反射耐性 == 0 && ABL:喉拡張 <= SIZE("阴茎", PLAYER) && RAND:3 == 0
			PRINTFORMW 拼命忍耐着涌上来的恶心…
		ELSEIF RAND:3 == 0
			PRINTFORMW 忍受袭击着喉咙的拷问般的PLAY…
		ELSEIF RAND:2 == 0
			PRINTFORMW 被插进去的阴茎蹂躏着…
		ELSE
			PRINTFORMW 忍受着无视人格的责备…
		ENDIF
	ENDIF
CASE 141 TO 143
	IF TALENT:叛逆
		;坦率、内気でない
		IF TALENT:坦率 + TALENT:内気 == 0
			IF CUP:苦痛 >= 3000 && RAND:2 == 0
				PRINTFORMW %CALLNAME:TARGET%在因为剧痛的眼角含泪的同时、用要暴起杀人的气势盯着这边
			ELSE
				PRINTFORMW %CALLNAME:TARGET%用看脏东西的眼神看着这里和拳頭…
			ENDIF
		ENDIF
	ENDIF
CASE 145
	IF TALENT:叛逆
		IF TALENT:高慢
			PRINTFORMW %CALLNAME:TARGET%用看脏东西的眼睛看着%CALLNAME:PLAYER%…
		ELSEIF TALENT:害羞
			PRINTFORMW %CALLNAME:TARGET%用带着羞辱和无情混杂的表情叹了一口气…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%一脸无聊地看着%CALLNAME:PLAYER%…
		ENDIF
	ELSE
		SELECTCASE CONDS("陰毛")
		CASE "脱毛処理"
		CASE "光滑无毛"
		CASE "保养完毕"
			;PRINTFORMW 性器を保護する陰毛を飾り立てるものへと変えられた%CALLNAME:TARGET%は、恥ずかしさに全身を火照らせている…
		ENDSELECT
	ENDIF
CASE 149
	IF MIN(ABL:露出癖, ABL:受虐属性) >= 3
		IF TEQUIP:导管
			IF RAND:2
				PRINTFORMW %CALLNAME:TARGET%一边玩弄着从尿道里飞出的导管、一边享受着被虐的乐趣……
			ELSE
				PRINTFORMW %CALLNAME:TARGET%Alice因为快要忍不住尿尿了、兴奋得心跳加速……
			ENDIF
		ELSE
			PRINTFORMW %CALLNAME:TARGET%在被拔出那深入到膀胱的导管的冲击下、咻的一声尿出来了、同时也发出了灼热的喘息……
		ENDIF
	ELSE
		IF TEQUIP:导管
			IF CUP:苦痛 >= 3000 && PENIS(TARGET)
				PRINTFORMW 因为太过强烈的疼痛而身体僵硬的%CALLNAME:TARGET%、没有多余的时间去在意失禁的情况而只能调整呼吸着…
			ELSEIF RAND:2
				PRINTFORMW %CALLNAME:TARGET%因为自己的身体完全违反着自己的意志、一直吧嗒吧嗒地撒尿、而露出了非常复杂的表情…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%因为无法忍受尿意、所以表情非常复杂…
			ENDIF
		ELSE
			PRINTFORMW %CALLNAME:TARGET%在被拔出那深入到膀胱的导管的冲击下、咻的一声尿出来了、同时也发出了放心的喘息…
		ENDIF
	ENDIF
;挿入前暴発
CASE 511
	PRINTFORM %CALLNAME:TARGET%
	IF TALENT:叛逆
		IF TALENT:高慢
			PRINTFORMW 对%CALLNAME:PLAYER%嗤之以鼻
		ELSE
			PRINTFORMW 用冷淡的目光注视着被污染的股间和%CALLNAME:PLAYER%的脸
		ENDIF
	ELSEIF COND("指の使用")
		IF TALENT:献身
			IF RAND:2
				PRINTFORMW 将%CALLNAME:PLAYER%的頭抱在胸前、一边抚摸着柔顺的頭发一边在耳边「…再一次」这样低语着
			ELSE
				PRINTFORMW 对%CALLNAME:PLAYER%温柔地微笑着、小声地说着「…再来一次」这样的话
			ENDIF
		ELSEIF TALENT:好色
			IF RAND:2
				PRINTFORMW 用手指沾了点精液舔了一下、然后像什么都没有发生过一样向%CALLNAME:PLAYER%索取着
			ELSE
				PRINTFORMW 一边用手指玩弄着弄脏了股间的精液、一边向%CALLNAME:PLAYER%露出淫荡的笑容
			ENDIF
		ELSE
			PRINTFORMW 用什么都说不出来的脸看着变小的阴茎……
		ENDIF
	ELSE
		PRINTFORMW 用什么都说不出来的脸看着变小的阴茎……
	ENDIF
CASEELSE
	TRYCALLFORM KOJO_MESSAGE_COM{SELECTCOM}
ENDSELECT

