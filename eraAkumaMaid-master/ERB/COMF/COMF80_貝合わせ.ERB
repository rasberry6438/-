@COM80
;貝あわせ
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL SETFLAG, "下半身を脱がす", PLAYER

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = %STR:今回指令%

;-------------------------------------------------
;射精格子
;-------------------------------------------------
CALL SAMEN_CHECK

RETURN 1

@FLAG_COM80, ARG
IF CHECK_COM("双頭振動棒PLAY", 80)
	IF TALENT:PLAYER:処女
		CALL SETFLAG, "処女喪失フラグ", PLAYER
		CALL SETFLAG, "処女簒奪フラグ", TARGET
	ENDIF
ENDIF
;対象のＶ⇔調教者のＶ的污垢が移動
CALL EQ_STAIN, TARGET, "Ｖ", PLAYER, "Ｖ"


@SOURCE_COM80, ARG
SIF CHECK_COM("双頭振動棒PLAY", 80)
	CALL SOURCE_INSERTION, "双頭振動棒PLAY", ARG, 100

CALL VARSET_COMF

CALL SOURCE_DOWNBASE_HP, 80

;SOURCE:0:恐惧 = 500
SOURCE:0:露出 = 250
SOURCE:0:屈従 = 400
SOURCE:0:逸脱 = 300
SOURCE:0:性行動 = 500 + ABL:奉仕精神*100
SOURCE:0:達成感 = 300

SOURCE:0:Ｃ快楽 = CALCF("Ｃ刺激", 2, 120)


SIF CHECK_COM("双頭振動棒PLAY", 80)
	TIMES SOURCE:0:Ｃ快楽, 0.30
;調教者の腰靈活をみる
SOURCE:0:Ｃ快楽 = SOURCE_EXABL(SOURCE:0:Ｃ快楽, "腰靈活", PLAYER)

;陰毛の生え具合補正
SIF COND("陰毛") <= 5 && COND("陰毛", PLAYER) >= 5
	TIMES SOURCE:0:Ｃ快楽, 1.20

IF COND("喜欢的体位中")
	TIMES SOURCE:0:Ｃ快楽, 1.20
	TIMES SOURCE:0:Ｖ快楽, 1.20
	TIMES SOURCE:0:性行動, 1.20
	TIMES SOURCE:0:達成感, 1.20
	TIMES SOURCE:0:恐惧, 0.80
	TIMES SOURCE:0:逸脱, 0.80
ENDIF

SIF TALENT:Ｃ性向
	CALL CALC, "好みのPLAY", SOURCE:0:Ｃ快楽

CALL CALC, "調教ソース", ARG


@TRAIN_MESSAGE_COM80
IF PREVCOM == SELECTCOM
	PRINTFORM %CALLNAME:PLAYER%和%CALLNAME:TARGET%
	IF RAND:3
		IF RAND:3
			PRINTFORM 两人的肌肤染上%TEXTR("桜色/朱红")%的%TEXTR("贴在一起/重叠在一起/")%的同时、
		ELSEIF RAND:2
			PRINTFORM 两人的愛液%TEXTR("像唾液那样缠在一起/缠在一起")%的同时、
		ENDIF
		PRINTFORMW %TEXTR("多次/不知道多少次地")%将%TEXTR("下流的/淫猥的/陰/")%唇%TEXTR("重叠在一起/相互亲吻着")%…
	ELSE
		IF RAND:3
			PRINTFORM 房间中响起%TEXTR("嬌声/水音")%的同时、
		ELSEIF RAND:2
			PRINTFORM 从%TEXTR("身体上/全身")%%TEXTR("散发出/散出/発散出/发出/飘出")%%TEXTR("雌香/淫臭/下流的香气/充滿情慾與雌性味道的淫香")%、
		ELSE
			PRINTFORM %TEXTR("身体/全身")%%TEXTR("流出像是玉珠一样的汗珠/变得红红的/流出晶瑩汗滴")%的同时、
		ENDIF
		SIF COND("性別嫌悪") == 0 && COND("性別嫌悪", PLAYER) == 0
			PRINT 在梦中
		IF RAND:3
			PRINTFORMW 相互将腰贴在一起…
		ELSEIF RAND:2
			PRINTFORMW %TEXTR("像是在舔舐一样/")%互相%TEXTR("摩擦/緊密而激烈的摩擦著")%陰部…
		ELSE
			PRINTFORMW %TEXTR("贪食着/分享/互相给予")%將股間相互緊密而激烈的摩擦的快樂…
		ENDIF
	ENDIF
ELSEIF TEQUIP:推倒
	PRINTFORM %CALLNAME:TARGET%将%CALLNAME:PLAYER%的身体按住、
	IF TALENT:PLAYER:処女 && CHECK_COM("双頭振動棒PLAY", 80)
		PRINTFORMW 将用股间含住的双頭振動棒的另一端压在了%CALLNAME:PLAYER%的膣口上。
		PRINTFORM 用模仿着亀頭的前端多次戳弄了入口之后、
		PRINTFORMW 振動棒便慢慢地打開了%CALLNAME:PLAYER%的膣肉、就这样拿走了処女
		PRINTFORMW 带着体重压下来的振動棒瞬间钻入了%CALLNAME:PLAYER%的膣深处、將被蹂躪的快樂與鮮明的疼痛清楚地傳遞到腦海……
	ELSE
		IF CHECK_COM("双頭振動棒PLAY", 80)
			PRINTFORMW 用%TEXTR("插在其中的/股間的")%双頭振動棒侵犯着膣的同时%TEXTR("/激烈地")%扭动起腰…
		ELSE
			PRINTFORMW 贪婪地将双方的%TEXTR("蜜裂/性器")%%TEXTR("重叠在一起/摩擦着")%…
		ENDIF
	ENDIF
ELSEIF CHECK_COM("双頭振動棒PLAY", 80)
	PRINTFORM %CALLNAME:PLAYER%
	IF TALENT:PLAYER:処女
		PRINTFORMW 跨坐在从%CALLNAME:TARGET%的膣中露出頭的振動棒上、丢掉了自己的处女…
		PRINTFORM %CALLNAME:TARGET%
		IF TALENT:恋慕 || TALENT:兩面通吃
			PRINTFORM 因为收到了%CALLNAME:PLAYER%的第一次%TEXTR("而兴奋/而兴奋地红透了脸/露出了喜色")%、
			PRINTFORMW 为了%TEXTR("奖励/抚慰")%%CALLNAME:PLAYER%似的、%TEXTR("温柔地/慢慢地")%%TEXTR("開始动腰/開始了扭动/摇晃起腰/開始摇晃腰部")%…
		ELSE
			PRINTFORMW 因為%CALLNAME:PLAYER%的行為而用難以言喻的複雜表情眺望著夾雜著痛苦與快樂的臉、開始心不在焉的配合著扭動著腰肢……
		ENDIF
	ELSEIF RAND:2 && COND("性別嫌悪") == 0 && COND("性別嫌悪", PLAYER) == 0
		PRINTFORM 和%CALLNAME:TARGET%保持著將手伸到背後支撐身體、下半身Ｍ字開腿的姿勢相對著、用激烈地振動著的雙頭振動棒將兩人連接起來、
		IF RAND:3 == 0
			PRINTFORMW 將彼此的膣道激烈的侵犯著
		ELSEIF RAND:2
			PRINTFORMW 腰を突き%TEXTR("/出し")%あった
		ELSE
			PRINTFORMW 大力的扭動著腰肢
		ENDIF

		;攻め手の判定
		SELECTCASE ABL:Ｖ技術 - ABL:Ｖ拡張 - (ABL:PLAYER:Ｖ技術 - ABL:PLAYER:Ｖ拡張)
		;TARGET有利
		CASE IS > 0
			LOCAL = TARGET
			LOCAL:1 = PLAYER
		CASE 0
			LOCAL = TARGET
			LOCAL:1 = PLAYER
			SIF RAND:2
				SWAP LOCAL, LOCAL:1
		CASE IS < 0
			LOCAL = PLAYER
			LOCAL:1 = TARGET
		ENDSELECT

		PRINTFORM %CALLNAME:LOCAL%的
		IF ABL:LOCAL:Ｖ拡張 >= 4 && RAND:3
			PRINTFORM 重复着扩张
		ELSEIF ABL:LOCAL:Ｖ名器度 >= 70 && RAND:3
			PRINTFORM 緊緊纏繞住的
		ELSEIF TALENT:LOCAL:淫壷
			PRINTFORM 因快感而溶化的
		ENDIF
		PRINTFORM 柔軟膣道、借助在%CALLNAME:(LOCAL:1)%的
		IF ABL:(LOCAL:1):Ｖ技術 >= 5 && RAND:3
			PRINTFORM 強力的吮吸著的
		ELSEIF ABL:(LOCAL:1):Ｖ拡張 <= 2 && RAND:3
			PRINTFORM 緊繃的
		ELSEIF ABL:(LOCAL:1):Ｖ名器度 >= 70 && RAND:3
			PRINTFORM 牢牢吸附住的
		ELSEIF TALENT:(LOCAL:1):淫壷
			PRINTFORM 有著無法言喻的貪欲的
		ENDIF
		PRINTFORM 淫亂膣穴中緊緊固定的雙頭振動棒、
		IF RAND:3 == 0
			PRINTFORMW 毫不留情地侵犯、翻攪著……
		ELSEIF RAND:2
			PRINTFORMW 強硬地侵犯著、\@ RAND:2 ? 反復抽插著 # 不斷振動著 \@激烈地玩弄著……
		ELSE
			PRINTFORMW 連同最深處也不留餘地的蹂躪著……
		ENDIF

	ELSE
		PRINTFORM 將從%CALLNAME:TARGET%膣道中露出另一頭的振動棒用自己的淫液稍作潤滑後、就直接地%TEXTR("/用相互摩擦彼此陰部的姿勢")%深深吞嚥進體內了、
		IF RAND:2
			PRINTFORMW 在彼此的膣道被振動棒不斷翻攪著的同時、也緊貼著陰戶相互激烈地摩擦起來、
		ELSE
			PRINTFORMW 用膣肉緊緊勒住振動棒後開始激烈地揮舞腰肢、
		ENDIF
		IF RAND:2
			PRINTFORMW 因雙頭振動棒充分發揮引導工作而愈加激烈地撞擊、摩擦起來的結合處、淫液在發出黏稠的水音的同時、也泛起了白色碎沫、讓兩人的腰部乃至於整個下半身都閃爍著濡濕的光澤……
		ELSE
			PRINTFORMW 與平日裡並不相同的膣內刺激、讓完全沒有不情願心情的兩人愈加的興奮與投入了……
		ENDIF
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:PLAYER%和%CALLNAME:TARGET%將彼此的陰部緊貼著摩擦起來、
	IF RAND:5 == 0
		IF COND("陰毛") <= 3 && COND("陰毛", PLAYER) >= 5
			PRINTFORMW PRINTFORMW 光潔無毛的陰部被糾纏在一起的濡濕陰毛刺激著、%CALLNAME:TARGET%沉浸於快感之中浮現出瘙癢的難耐表情……
		ELSE
			PRINTFORMW %TEXTR("两人的陰核/陰核")%相互摩擦刺激着、%CALLNAME:TARGET%沉浸於快感之中浮現出瘙癢的難耐表情……
		ENDIF
	ELSEIF RAND:4 == 0
		IF COND("陰毛") >= 5 && COND("陰毛", PLAYER) <= 3
			PRINTFORMW %CALLNAME:TARGET%將%CALLNAME:PLAYER%毫無防備的陰部用自己的毛髮摩擦著、愉快的享受露出%TEXTR("微笑/很舒服的表情")%……
		ELSE
			PRINTFORMW %CALLNAME:TARGET%將%CALLNAME:PLAYER%毫無防備的陰核用自己的毛髮摩擦著、愉快的享受露出%TEXTR("微笑/很舒服的表情")%……
		ENDIF
	ELSEIF RAND:3 == 0
		PRINTFORMW %TEXTR("重重疊疊的水聲/淫靡而黏稠的水音")%連同著下體傳遞的極樂快感被一併送入腦中、腰肢的活動漸漸變得更加激烈了……
	ELSEIF RAND:2
		PRINTFORMW 被緊緊擠壓在一起的四瓣陰唇%TEXTR("多次/咕啾咕啾")%变换着下流的形状、彼此的體香與溢出蜜液的淫香漸漸混合、發酵、釀造出更加淫靡激烈的濃郁气味
	ELSE
		PRINTFORMW 每次将陰唇贴在一起、两人的体香便混合在一起变成了更加淫乱的%TEXTS("气味")%…
	ENDIF
ENDIF
