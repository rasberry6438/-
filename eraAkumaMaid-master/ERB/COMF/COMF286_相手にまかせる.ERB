@COM286

PRINTL 委身于對方

CALL KOJO_MESSAGE_COM
CALL BLANKLINE

SIF CONFIG("委身于對方的警告表示")
	CALL SELECT_WARNING_COM286

SIF REFUSE_CHECK()
	RETURN 0

STR:前回指令 = 委身于對方

;FIRSTCOMフラグON
CALL SET_CHOSECOM, TARGET, TNUM("委身于對方")

IF TFLAG:地の文スキップ == 0
	PRINTFORM %CALLNAME:PLAYER%決定暫時按照%CALLNAME:TARGET%
	SIF TEQUIP:Ｗ推倒
		PRINTFORM 和%CALLNAME:(TARGET:1)%
	PRINTFORMW 的想法接受調教

	PRINTFORMW %CALLNAME:PLAYER%變成了柔若無骨狀態…
	CALL BLANKLINE
ENDIF
CALL ADD_STATE, PLAYER, "脫力"

;就这様顺其自然を実行
TRYJUMPFORM COM{281}

RETURN 1

@FLAG_COM286, ARG

@SELECT_WARNING_COM286
#DIM DYNAMIC MEMO_LINECOUNT

MEMO_LINECOUNT = LINECOUNT

CALL PRINT_STRL, "_赤_注意！在選擇了這個指令之後的數輪將無法選擇其他指令。"
PRINTL 
PRINTFORML 確定要實行嗎？
CALL PRINT_SELECT, "是/否", "打開是否顯示這個警告的設置"
IF RESULT == 0
ELSEIF RESULT == 1
	TFLAG:指令中止 = 1
	RETURN 0
ELSE
	CALL SETCONFIG, "委身于對方的警告"
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
ENDIF

RETURN 1
