@従業員部屋割り設定
#DIM ページ数
#DIM 入れ替え記録
#DIM ループ用
#DIM 退避配列, 従業員関連配列数

入れ替え記録 = -1

$表示LOOP

DRAWLINE
PRINTL 可在此调整员工宿舍分配方案。
PRINTL 请选择左侧批量调整或点击目标房间进行单独配置。
PRINTL ※当前功能处于开发阶段，可能出现运作异常。
DRAWLINE

LOCALS = <div rect='81,131,1231,2950' padding='50,25,50' border='31' bcolor='#C0C0C0'>
LOCALS += "<br>"
LOCALS += "<button value='990'>[990] 整理空闲房间</button><br>"
LOCALS += "<button value='991'>[991] 智能排序房间</button><br>"
LOCALS += "<br>"
LOCALS += "<button value='999'>[999] 返回</button><br>"
LOCALS += "</div>"
HTML_PRINT LOCALS, 1
CALL 宿泊エリア_従業員室_表示(ページ数, "入れ替え時", 入れ替え記録)

FOR LOCAL, 0, 27
	PRINTL
NEXT

BINPUT
LOCAL = RESULT
SELECTCASE LOCAL
	CASE 999
		ページ数 = 0
		入れ替え記録 = -1
		RETURN 1
	CASE 1000
		ページ数 -= 1
		GOTO 表示LOOP
	CASE 1001
		ページ数 += 1
		GOTO 表示LOOP
	CASE 990
		DRAWLINE
		PRINTL 将在保持员工当前顺序的前提下，自动填补中间的空置房间。
		PRINTL 确定要运行它吗？
		PRINTBUTTONLC "[0] 确定", 0
		PRINTBUTTONLC "[1] 取消", 1
		BINPUT
		IF RESULT == 0
			;退避配列にコピーしてからリセットし、一個ずつ登録
			VARSET 退避配列
			ARRAYCOPY "従業員部屋割り配列", "退避配列"
			VARSET 従業員部屋割り配列
			LOCAL:1 = 0
			FOR LOCAL, 0, 従業員関連配列数
				SIF 退避配列:LOCAL < 1
					CONTINUE
				従業員部屋割り配列:(LOCAL:1) = 退避配列:LOCAL
				LOCAL:1 ++
			NEXT
			PRINTW 空闲房间整理完成。
		ENDIF
	CASE 991
		DRAWLINE
		PRINTL 按员工资质与能力值进行房间排序（空置的房间将被填满）
		PRINTL 请选择排序依据
		PRINTBUTTONLC "[0] 角色编号", 0
		PRINTBUTTONLC "[1] 好感度等级", 1
		PRINTBUTTONLC "[2] 爱慕度等级", 2
		PRINTL
		PRINTBUTTONLC "[3] 战斗等级", 3
		PRINTBUTTONLC "[4] 胸围尺寸", 4
		PRINTL
		PRINTBUTTONLC "[999] 取消", 999
		BINPUT
		LOCAL = RESULT
		IF LOCAL == 999
			RESTART
		ELSE
			SELECTCASE LOCAL
				CASE 0
					PRINT 角色编号
				CASE 1
					PRINT 好感度等级
				CASE 2
					PRINT 爱慕度等级
				CASE 3
					PRINT 战斗等级
				CASE 4
					PRINT 胸围尺寸
			ENDSELECT
			PRINTL  以此方式排序，可以吗？
			PRINTBUTTONLC "[0] 确定", 0
			PRINTBUTTONLC "[1] 取消", 1
			BINPUT
			IF RESULT == 1
				RESTART
			ENDIF
		ENDIF
		VARSET 退避配列
		FOR ループ用, 0, 従業員関連配列数
			IF 従業員部屋割り配列:ループ用 < 1
				退避配列:ループ用 = 1
			ELSE
				;降順にするために全部-1を掛けておく
				SELECTCASE LOCAL
					CASE 0
						退避配列:ループ用 = 従業員部屋割り配列:ループ用 * -1
					CASE 1
						退避配列:ループ用 = CFLAG:(従業員部屋割り配列:ループ用):好感度レベル * -1
					CASE 2
						退避配列:ループ用 = CFLAG:(従業員部屋割り配列:ループ用):恋慕レベル * -1
					CASE 3
						退避配列:ループ用 = 基礎BATTLE_STATE:(従業員部屋割り配列:ループ用):Lv * -1
					CASE 4
						退避配列:ループ用 = BASE:(従業員部屋割り配列:ループ用):バスト * -1
				ENDSELECT
			ENDIF
		NEXT
		;退避配列基準でソート
		ARRAYMSORT 退避配列, 従業員部屋割り配列
		IF LOCAL == 0
			;角色编号だけは昇順なので並べ直す
			VARSET 退避配列
			LOCAL:1 = 0
			FOR ループ用, 0, 従業員関連配列数
				SIF 従業員部屋割り配列:ループ用 < 1
					BREAK
				LOCAL:1 ++
			NEXT
			ARRAYSORT 従業員部屋割り配列, FORWARD, 0, LOCAL:1
		ENDIF
		PRINTW 房间排序已完成。
	CASEELSE
		IF 入れ替え記録 == LOCAL
			;選択を外す
			RESTART
		ELSEIF 入れ替え記録 > -1
			;既に選択されてるので入れ替える
			SWAP 従業員部屋割り配列:LOCAL, 従業員部屋割り配列:入れ替え記録
			RESTART
		ELSE
			;まだ選択されてないので選択状態になるだけ
			入れ替え記録 = LOCAL
			GOTO 表示LOOP
		ENDIF
ENDSELECT

RESTART
