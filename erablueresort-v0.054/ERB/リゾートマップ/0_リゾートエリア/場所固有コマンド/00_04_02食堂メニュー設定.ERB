
;-------------------------------------------------
;食堂设置菜单
;-------------------------------------------------

@COMNAME_PLACE_381_0_4
#FUNCTION
TSTR:コマンド名受渡 = 设置菜单
RETURNF 1

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_381_0_4
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")

@COM_TOOLTIP_381_0_4
LOCALS = <br>■设置菜单<br>
LOCALS += "设定已拥有菜单中将在食堂供应的菜品指令。<br>"
LOCALS += "<br>COM类型：可单独执行<br>获取来源：欢乐・征服(特定条件)"
LOCALS += "<br>专属经验值：无<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM381_0_4

CALL 食堂メニュー設定処理
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE381_0_4
;条件に合う場合0（不許可）を返す
;どの条件にも引っかからないなら1（許可）を返す
;一括管理
SIF GLOBAL_COMABLE(381)
	RETURN RESULT
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
;隠密中はダメ
SIF CFLAG:PLAYER:隠密
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM381_0_4
