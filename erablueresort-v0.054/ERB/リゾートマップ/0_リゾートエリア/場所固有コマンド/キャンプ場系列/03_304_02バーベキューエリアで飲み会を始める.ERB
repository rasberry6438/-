
;-------------------------------------------------
;03_304_02バーベキューエリアで举办酒会
;-------------------------------------------------
@COMNAME_PLACE_381_0_304
#FUNCTION

IF 施設改造度:3:0 == 0
	TSTR:コマンド名受渡 = 
	RETURNF 0
ENDIF
TSTR:コマンド名受渡 = 举办酒会
RETURNF 1

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_381_0_304
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("日常")
		
@COM381_0_304
CALL COM381_0_503


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE381_0_304
;条件に合う場合0（不許可）を返す
;どの条件にも引っかからないなら1（許可）を返す
;一括管理
SIF GLOBAL_COMABLE(381)
	RETURN RESULT
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
;隠密中はダメ
SIF CFLAG:PLAYER:隠密
	RETURN 0
;二日酔い時はダメ
SIF BASE:MASTER:酩酊 < 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM381_0_304

IF RFLAG:コマンド結果受渡し変数 == 1
	PRINTFORM %CALLNAME:PLAYER%与%CALLNAME:TARGET%
	SIF RFLAG:コマンド結果受渡し変数２ > 0
		PRINT 等人
	PRINTL 共同举办了酒会。
ELSE
	PRINTFORM %CALLNAME:PLAYER%邀请%CALLNAME:TARGET%参加酒会，但被拒绝了…
ENDIF
