
@アイテム個別文章表示_投掷炸弹(ARGS)
VARSET LOCALS
CALL 口上変数初期化

IF ARGS == "値段"
	詳細文文字列受け渡し変数 = 卢比
	RETURN 300
ENDIF


LOCALS += @"■投掷炸弹　　　持有数:{DT_CELL_GET("所持アイテムデータベース", 0, "投掷炸弹")}<br>"
LOCALS += "填充简易爆炸物的投掷型武器。<br>"
LOCALS += "村民用于防身的常见类型。<br>"
LOCALS += "爆炸范围小威力低，但对弱等魔物有效。<br>"
LOCALS += "<br>"
LOCALS:1 += "【效果】<br>"
LOCALS:1 += "对敌方单体造成[１００]的固定伤害<br>"

詳細文文字列受け渡し変数 = %LOCALS%%LOCALS:1%
KSTR:(K++) = %LOCALS:1%

@アイテム使用先表示_投掷炸弹
CALL 汎用使用先表示("敵単体")

@アイテム効果処理_投掷炸弹(ARG)
;ARG = エネミーが何番目か、10からカウント
CALL 口上変数初期化

アビ汎用変数:効果量 = 100
CALL アビテンプレート_ダメージ処理_無属性

SIF アビテンプレート用_キャンセルフラグ
	RETURN -1

RETURN 1
