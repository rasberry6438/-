
@指輪個別文章表示_拒绝之戒
LOCALS = 
LOCALS += @"■拒绝之戒<br><br>"
LOCALS += "生成可弹开攻击的屏障的戒指。<br>"
LOCALS += "战斗开始时赋予装备者［500］护盾值。<br>"
LOCALS += "因能抑制持久战消耗，深受佣兵青睐。<br>"
LOCALS += "但攻击性能低下，需以团队协作为前提使用。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 15000

@指輪基礎効果文章表示_拒绝之戒(指輪番号 = 0, 効果番号 = -1)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基础效果】<br>"
LOCALS += "最大ＨＰ+200 防御力+300 恢复力+100 行动速度+150 生成护盾<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【选项效果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_拒绝之戒(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 3
	RETURN 0
SELECTCASE 入手タイプ
	CASE "輸入"
		RETURN 装備レアリティ_Ｒ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_Ｒ
ENDSELECT

@指輪ステータス決定_拒绝之戒(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_200_防御力_300_回復力_100_行動速度_150
IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "拒绝之戒", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)


@固有戦闘開始時処理_指輪_拒绝之戒

CALL アビ対象選択テンプレート_自己のみ
アビテンプレート用_アビ名 = 拒绝之戒
アビ汎用変数:効果量 = 500
アビ汎用文字列:バフ・デバフ枠 = バリア
アビ汎用文字列:バフ・デバフ対象能力 = バリア
アビ汎用変数:持続ターン = -1
CALL アビテンプレート_有利効果_バフ効果セット
