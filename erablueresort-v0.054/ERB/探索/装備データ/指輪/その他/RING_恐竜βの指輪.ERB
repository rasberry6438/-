
@指輪個別文章表示_恐龙β之戒
LOCALS = 
LOCALS += "■恐龙β之戒<br><br>"
LOCALS += "纯粹提升攻击力与行动速度的爽快型戒指。<br>"
LOCALS += "另在状态栏攻击力之外，对敌人造成的伤害额外提升［攻击力5%］。<br>"
LOCALS += "寄宿了强敌「霸王龙β」之力的极为稀有的戒指。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 70000

@指輪基礎効果文章表示_恐龙β之戒(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基础效果】<br>"
LOCALS += "攻击力+450 行动速度+450<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【选项效果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

;ランダム抽選では出現しない（入手法：ダンジョン「風遊ぶ渓谷の参道」でボス「ティラノサウルスβ」を初回撃破）
;@指輪出現率_恐龙β之戒(入手タイプ, 装備ランク)

@指輪ステータス決定_恐龙β之戒(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列
#DIMS 特殊補正文字列

基本補正文字列 = 攻撃力_450_行動速度_450
特殊補正文字列 = 与ダメ上昇

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	詳細文文字列受け渡し変数 += @"%特殊補正文字列%"
	;オプション固定用にオプションランクを返す
	RETURN 4
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv1のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(4)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "恐龙β之戒", "基礎ステータス補正", 基本補正文字列, "特殊ステータス補正", 特殊補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_恐龙β之戒_与ダメージ(隊列番号, ダメージ値)
#DIM 隊列番号
#DIM ダメージ値

;攻撃力の５％追加
ダメージ値 += BATTLE_STATE:戦闘行動キャラ:攻撃力 / 20
RETURN ダメージ値
