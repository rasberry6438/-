
@指輪個別文章表示_知足者富之戒
LOCALS = 
LOCALS += @"■知足者富之戒<br><br>"
LOCALS += "灌注认为适中胸部方显真正富足的老子魔力的戒指。<br>"
LOCALS += "只有符合其认可体型的人，才能获得此戒的助力。<br>"
LOCALS += "若得此开悟之力，或许连敌人的动向都能预判。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 35000

@指輪基礎効果文章表示_知足者富之戒(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基础效果】<br>"
LOCALS += "最大ＨＰ+150 攻击力+200 防御力+100 恢复力+200<br>"
LOCALS += "行动速度+50 命中率+10% ※需符合条件<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【选项效果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_知足者富之戒(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_知足者富之戒(対象キャラ)
#DIM 対象キャラ
;男性はダメ
SIF TALENT:対象キャラ:性別 == 2
	RETURN 0
;Bが存在しないとダメ
SIF TALENT:対象キャラ:Ｂ感度 == -2
	RETURN 0
RETURN (TALENT:対象キャラ:バストサイズ == 0 || TALENT:対象キャラ:バストサイズ == 1)

@指輪ステータス決定_知足者富之戒(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_150_攻撃力_200_防御力_100_回復力_200_行動速度_50_命中率_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "知足者富之戒", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
