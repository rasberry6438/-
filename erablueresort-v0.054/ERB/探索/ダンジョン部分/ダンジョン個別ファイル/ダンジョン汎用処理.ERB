
@雑魚敵繰り返し戦闘処理

IF OPTION変数:敵探索SKIP == 0
	KSTR:(K++) = 是否寻找敌人？
	KSTR:(K++) = BUTTON_[确定]
	KSTR:(K++) = BUTTON_[取消]
	CALL メッセージ欄表示用関数(,,,0)
	$LOOP_MESSAGE
	INPUTS
	SELECTCASE RESULTS
		CASE "[确定]"
			CALL 雑魚敵遭遇汎用処理
		CASE "[取消]"
			VARSET 敵BATTLE_STATE:0:0
			VARSET 敵BATTLE_STATE_STR:0:0
			CALL 口上変数初期化
			戦闘時_特殊処理フラグ = 
		CASEELSE
			CLEARLINE 1
			REUSELASTLINE  
			GOTO LOOP_MESSAGE
	ENDSELECT
ELSE
	CALL 雑魚敵遭遇汎用処理
ENDIF
