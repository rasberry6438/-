;等待（隠密状態のみ）
@COMNAME416
#FUNCTIONS
TSTR:コマンド名受渡 = 等待

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_416
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("隠密")

@COM_TOOLTIP_416
LOCALS = <br>■等待<br>
LOCALS += "保持隐蔽状态原地待机的指令。<br>消耗15分钟。"
LOCALS += "<br>指令类型：隐匿行动<br>获取来源：无"
LOCALS += "<br>专属经验值：无<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM416
TIME += 15


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE416
;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(416)
	RETURN RESULT
;隠密時のみ
SIF !CFLAG:MASTER:隠密
	RETURN 0
;うふふ中は封印
SIF TFLAG:調教モード == 2
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
