;-------------------------------------------------
;跳蛋
;道具使用コマンド、レベル1
;-------------------------------------------------
@COMNAME40
#FUNCTIONS

	TSTR:コマンド名受渡 = 跳蛋




;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_40
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("道具系")
弱点コマンドカテゴリ = C
オートコマンド許可 = 1



@COM_TOOLTIP_40
LOCALS = <br>■跳蛋<br>
LOCALS += "使用跳蛋刺激阴蒂或阴茎的指令。"
LOCALS += "<br>指令类型：道具系<br>获取来源：快Ｃ・暴露・接触"
LOCALS += "<br>专属经验值：道具使用经验(PLAYER)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM40
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

CALL SOURCE_CALC_快Ｃ(TARGET, PLAYER,250)
CALL SOURCE_CALC_露出(TARGET, PLAYER, 50)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 60)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛
SIF 同性愛性癖判定(TARGET,PLAYER)
	EXP:TARGET:同性愛経験 += 1
SIF 同性愛性癖判定(PLAYER,TARGET)
	EXP:PLAYER:同性愛経験 += 1

EXP:PLAYER:道具使用経験 ++
RCVAR:TARGET:道具コマンドフラグ = 1
RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE40
;跳蛋実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(40)
	RETURN RESULT
;クリがない場合駄目
SIF TALENT:TARGET:Ｃ感度 == -2
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:0 == 0 && NOITEM == 0
	RETURN 0

;下半身着衣
;前鎧、めくれない服があると無理
SIF TEQUIP:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:下半身服あり && (!TEQUIP:スカートめくり可 && !TEQUIP:ずらし可)
	RETURN 0

;使用者が指を何かに使ってるなら無理
SIF TEQUIP:PLAYER:指
	RETURN 0
;クリトリスを何かに使ってる・届かないなら無理
SIF TEQUIP:PLAYER:クリトリス
	RETURN 0
CALL MODE_部位届く判定(部位_クリトリス, PLAYER, TARGET)
SIF RESULT == 0
	RETURN 0
	
;オートコマンド中の制限
SIF FLAG:オートコマンド判定中フラグ && OPTION変数:玩具プレイ禁止
	RETURN 0
	
;オートコマンド中の制限
SIF FLAG:オートコマンド判定中フラグ && TALENT:TARGET:性別 == 2 &&  OPTION変数:男性への愛撫禁止
	RETURN 0	
	
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM40
PRINTFORM %CALLNAME:PLAYER%将
IF TEQUIP:下タイツあり && !TEQUIP:下タイツ破れ
	PRINTFORM 跳蛋贴在%CALLNAME:TARGET%的
	PRINTL 下体隔着连裤袜将跳蛋贴在上面…
	RETURN 0
ENDIF
IF TEQUIP:下半身下着あり
	IF TALENT:恋慕
		PRINTFORM 主动褪下内衣做好准备
	ELSE
		PRINTFORM 强行褪下内衣暴露部位
	ENDIF
ELSEIF TEQUIP:下半身服あり && TEQUIP:スカートめくり可
	IF TALENT:恋慕
		PRINTFORM 主动掀起裙摆的
	ELSE
		PRINTFORM 掀起裙摆暴露
	ENDIF
ELSEIF TEQUIP:下半身服あり && TEQUIP:ずらし可
	IF TALENT:恋慕
		PRINTFORM 主动褪下衣物的
	ELSE
		PRINTFORM 褪去衣物暴露
	ENDIF
ENDIF
PRINTFORM 跳蛋贴在%CALLNAME:TARGET%的
;オトコ＋欲情のPALAMLV3以上
IF !(TALENT:性別 & 1) && (PALAM:欲情 >= PALAMLV:3)
	PRINTL 完全勃起的阴茎上…
;オトコ
ELSEIF !(TALENT:性別 & 1)
	PRINTL 阴茎上…
;ふたなり＋欲情のPALAMLV3以上
ELSEIF (TALENT:性別 & 3) == 3 && PALAM:欲情 >= PALAMLV:3
	PRINTL 湿润的阴唇上…
;ふたなり
ELSEIF (TALENT:性別 & 3) == 3
	PRINTL 私密处…
;欲情のPALAMLV3以上
ELSEIF PALAM:欲情 >= PALAMLV:3
	PRINTL 充血挺立的阴蒂上…
ELSE
	PRINTL 阴蒂上…
ENDIF

