;-------------------------------------------------
;挤奶机
;道具使用コマンド、レベル3
;-------------------------------------------------
@COMNAME49
#FUNCTIONS
TSTR:コマンド名受渡 = 装备穿戴式假阳具

;弱点コマンド欄に専用の文字列を表示したい場合、これを設定すること
弱点コマンド欄表示名 = 穿戴式假阳具



;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_49
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("道具系&Ｖ系")

@COM_TOOLTIP_49
LOCALS = <br>■装备穿戴式假阳具<br>
LOCALS += "将穿戴式假阳具插入自身阴道获得拟真阴茎的指令。<br>进入穿戴式假阳具装备状态，可执行插入系指令。"
LOCALS += "<br>指令类型：道具系<br>获取来源：快Ｖ・暴露"
LOCALS += "<br>专属经验值：道具使用经验(PLAYER)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM49
;着脱の処理
CALL MODE_CHANGE("假阳具", PLAYER, PLAYER)

;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
RCVAR:TARGET:道具コマンドフラグ = 1

CALL SOURCE_CALC_快Ｖ(PLAYER, -1, 100)
CALL SOURCE_CALC_露出(PLAYER, -1, 50)

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
EXP:PLAYER:道具使用経験 ++

RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------

@COM_ABLE49
;挤奶机実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(49)
	RETURN RESULT
;すでに使ってるとだめ
SIF MODE_存在判定_ターゲット側("假阳具", PLAYER)
	RETURN 0
;Vがない場合駄目
SIF TALENT:PLAYER:Ｖ感度 == -2
	RETURN 0
SIF GETBIT(TALENT:PLAYER:性別, 0) == 0
	RETURN 0
;ペニスがある場合駄目
SIF GETBIT(TALENT:PLAYER:性別, 1) == 1 && TALENT:PLAYER:Ｃ感度 != -2
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:假阳具 == 0 && NOITEM == 0
	RETURN 0
;下半身着衣
;前鎧、めくれない服、タイツ、前張りがあると無理
SIF TEQUIP:PLAYER:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:PLAYER:下半身服あり && (!TEQUIP:PLAYER:スカートめくり可 && !TEQUIP:PLAYER:ずらし可)
	RETURN 0
SIF TEQUIP:PLAYER:下タイツあり && !TEQUIP:PLAYER:下タイツ破れ
	RETURN 0
SIF TEQUIP:PLAYER:下半身下着あり == 2
	RETURN 0

;装着者が指を何かに使ってるなら無理
SIF TEQUIP:PLAYER:指
	RETURN 0
;膣使用中だとダメ
SIF TEQUIP:PLAYER:膣
	RETURN 0
;処女はダメ
SIF TALENT:PLAYER:処女
	RETURN 0
;潤滑稼ぐこと
SIF PALAM:PLAYER:潤滑 < PALAMLV:2
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM49
PRINTFORML %CALLNAME:PLAYER%将穿戴式假阳具插入自己的阴道并且固定好…

