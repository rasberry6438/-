

@COMNAME354
#FUNCTIONS
TSTR:コマンド名受渡 = 请求原谅

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_354
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("いつでも")

@COM_TOOLTIP_354
LOCALS = <br>■请求原谅<br>
LOCALS += "降低处于愤怒状态对象怒气值的指令。<br>消减量与好感度等级成正比。"
LOCALS += "<br>指令类型：随时可执行<br>获取来源：无"
LOCALS += "<br>专属经验值：无<br>"
TSTR:ツールチップ受渡 = %LOCALS%


;请求原谅
@COM354

BASE:怒り -= CFLAG:好感度レベル * 5

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE354
;告白する実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(354)
	RETURN RESULT
;いつでもだが相手がいないとダメ
SIF !TARGET
	RETURN 0
;二人きりでないとだめ
SIF GET_TARGETNUM() > 1
	RETURN 0
;相手が怒っている必要あり
SIF !CFLAG:怒り
	RETURN 0
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------

@MESSAGE_COM354
PRINTFORML %CALLNAME:PLAYER%诚心诚意地向%CALLNAME:TARGET%道歉
IF BASE:TARGET:怒り > 0
	PRINTL 然而，似乎仍未获得原谅…
ELSE
	PRINTL 总算获得了原谅。下次务必注意…
ENDIF
