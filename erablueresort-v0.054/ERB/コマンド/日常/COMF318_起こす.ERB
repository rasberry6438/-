@COMNAME318
#FUNCTIONS
TSTR:コマンド名受渡 = 叫醒

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_318
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("日常")
睡眠中許可 = 1

@COM_TOOLTIP_318
LOCALS = <br>■叫醒<br>
LOCALS += "用于在白天唤醒因故沉睡对象的指令。<br>早晨无法使用此指令。"
LOCALS += "<br>指令类型：日常互动<br>获取来源：无"
LOCALS += "<br>专属经验值：无<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM318

CFLAG:TARGET:睡眠 = 0
TCVAR:TARGET:日中睡眠 = 0

RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE318

;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(318)
	RETURN RESULT
;日中睡眠中のみ
SIF TCVAR:日中睡眠 == 0
	RETURN 0

RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM318
PRINTFORML %CALLNAME:PLAYER%摇晃着陷入沉睡的%CALLNAME:TARGET%的身体，将其叫醒了。

