;-------------------------------------------------
;自慰
;日常系指令、レベル1
;-------------------------------------------------
@COM451
#DIM C_ID
#DIM AMT
IF TARGET
	PRINTFORMW 自慰的時候、必須是誰也不能打擾的 自由和救贖的 獨自寧靜豐富的……
	RETURN -1
ENDIF
PRINTFORML 腦海中想起了誰？
DRAWLINE
FOR LOCAL,1,CHARANUM
	IF !IS_NAMED(LOCAL) || !CFLAG:LOCAL:面識
		CONTINUE
	ENDIF
	PRINTFORMLC [{LOCAL, 3}] %NAME_OUTPUT_TRANSLATION("CALLNAME", LOCAL)%
NEXT
PRINTL
DRAWLINE
PRINTL [999] 誰也不去想 [1000] 放弃
INPUT
C_ID = RESULT
SELECTCASE C_ID
	CASE 1 TO CHARANUM
		IF 玩家立绘开关
			CALL PRINT_FACE_PLAYER, MASTER, "発情", "服", "", ""
			PRINTFORMDW 「%TEXTR("既然如此/这样的话/果然")%...」
			CALL PRINT_FACE_PLAYER, MASTER, "自慰", "裸", "", ""
			PRINTFORMDL 「%TEXTR("啊...嗯...啊/咕...呜.../噗...呜呜")%❤」
			PRINTFORMDW 「%NAME_OUTPUT_TRANSLATION("CALLNAME", C_ID)%❤」
		ENDIF
		CALL PRINT_FACE, C_ID, "発情", "裸", "", ""
		PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%想象著%NAME_OUTPUT_TRANSLATION("CALLNAME", C_ID)%処理了性欲
	CASE 999
		IF 玩家立绘开关
			CALL PRINT_FACE_PLAYER, MASTER, "発情", "服", "", ""
			PRINTFORMDW 「%TEXTR("既然如此/这样的话/果然")%...」
			CALL PRINT_FACE_PLAYER, MASTER, "自慰", "裸", "", ""
			PRINTFORMDL 「%TEXTR("啊...嗯...啊/咕...呜.../噗...呜呜")%❤」
		ENDIF
		PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%思考著%TEXTR("天体的運行/山川草木/生老病死")%処理了性欲
	CASEELSE
		RETURN -1
ENDSELECT
AMT = 50 + RAND:50
IF TALENT:MASTER:濃厚精液
	AMT *= 2
	PRINTFORMW ……濃厚的{AMT}量精液出來了
ELSE
	PRINTFORMW ……{AMT}量精液出來了
ENDIF
BASE:MASTER:精力 -= AMT
DOWNBASE:MASTER:気力 = 100

TIME += 20
RETURN 1

;等待
@COM_ABLE451
;ショップ画面では不可
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(451)
	RETURN RESULT
;気力0では不可
SIF BASE:MASTER:気力 <= 0
	RETURN 0
;精力0では不可
SIF BASE:MASTER:精力 <= 0
	RETURN 0
;ウフフ中は不可
SIF CFLAG:诶嘿嘿
	RETURN 0
;停止中は不可
SIF FLAG:70 == 1
	RETURN 0
;私室以外では不可
SIF CFLAG:MASTER:現在位置 != CFLAG:MASTER:初期位置
	RETURN 0
RETURN 1