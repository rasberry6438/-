@DLCOPTIONPRINT(PAGE)
#DIM DYNAMIC PAGE
PRINTFORML 
PRINTFORML 
PRINTFORML 设定内置补丁的启动与否
PRINTFORML 点击选项可以查看补丁详细说明
PRINTFORML 如果有BUG可以找迷子反馈，但不一定会修复，因为我没有脑子
PRINTFORML 最下面的几个可能得找画蛇又添足版的维护人员反馈了，因为是他们做的
SETCOLOR C_RED
PRINTFORML 部分补丁可能会极大的降低游戏难度，请自己酌情启用！！！
RESETCOLOR
PRINTFORML 补丁适用游戏版本：4.9（适配过几次了，应该是没什么问题，不过也算是自用测试版，有bug也不奇怪）
DRAWLINEFORM =
PRINTFORML 现在的设定：
IF PAGE == 0
	PRINTFORM %"天之声补丁", 15%：
	IF SKYSOUND == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 1] 天之声补丁设置"%


	PRINTFORM %"防袭击补丁", 15%：
	IF ENCOUNTDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 2] 防袭击补丁设置"%

	PRINTFORM %"丰富的天气", 15%：
	IF WEATHERDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 3] 丰富的天气设置"%

	PRINTFORM %"幻想乡之主", 15%：
	IF DEBUGGERR == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 4] 幻想乡之主设置"%
	
	PRINTFORM %"重选欧金金", 15%：
	IF OUJINJIN == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 5] 重选欧金金设置"%

	PRINTFORM %"简单化实绩", 15%：
	IF BONUSX == 0 || BONUSX == 100
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 6] 简单化实绩设置"%

	PRINTFORM %"道歉免时间", 15%：
	IF !APLOGIZEDLC
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 7] 道歉免时间设置"%

	PRINTFORM %"无条件访问", 15%：
	IF !HOMEWEL
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 8] 无条件访问设置"%

	PRINTFORM %"工作可推倒", 15%：
	IF !JOBFUCKING
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[ 9] 工作可推倒设置"%

	PRINTFORM %"药物简易化", 15%：
	IF !DRUG
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[10] 药物简易化设置"%

	PRINTFORM %"属性简易化", 15%：
	IF ABLDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[11] 属性简易化设置"%

	PRINTFORM %"弹幕战强化", 15%：
	IF BATTLEDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[12] 弹幕战强化设置"%

	PRINTFORM %"新妻泛用化", 15%：
	IF NEWWIFE == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[13] 新妻泛用化设置"%

	PRINTFORM %"胖次弹幕战", 15%：
	IF PANGCI == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[14] 胖次弹幕战设置"%

	PRINTFORM %"生动的记忆", 15%：
	IF MEETCHECKDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[15] 生动的记忆设置"%
	
	PRINTFORM %"自己开宴会", 15%：
	IF EATDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[16] 自己开宴会设置"%

	PRINTFORM %"同居与陪睡", 15%：
	IF SLEEPTOGETHER == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[17] 同居与陪睡设置"%

	PRINTFORM %"无限化入住", 15%：
	IF HOUGONG == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[18] 无限化入住设置"%

	PRINTFORM %"易书与隙间", 15%：
	IF HANDBOOK == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[19] 易书与隙间设置"%

	PRINTFORM %"自动化浇水", 15%：
	IF AUTOWATER == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[20] 自动化浇水设置"%
	
	PRINTFORM %"高强度怀孕", 15%：
	IF BABYDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[21] 高强度怀孕设置"%
	
	PRINTFORM %"彩色动态图", 15%：
	IF RECOLOREDMAPS == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSEIF ANIMATERECOLOREDMAPS == 0
		SETCOLOR 0x00FF00
		PRINTFORM %"无动态", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"动态图", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[22] 彩色动态图设置"%

	PRINTFORM %"射精后属性增长", 15%：
	IF DEVELOPMENT == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[23] 射精后属性上限增长设置"%

	PRINTFORM %"射精倍率", 15%：
	IF SHOOTINGSTAR == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未加倍", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已加倍", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[24] 射精倍率设置"%

	PRINTFORM %"一键扫除", 15%：
	IF SWEEPGOD == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未开启", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已开启", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[25] 一键扫除设置"%

	PRINTFORM %"催眠大师系统", 15%：
	IF HYGOD == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[26] 催眠大师设置"%
	
	PRINTFORM %"精液残留补丁", 15%：
	IF JYCL == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[27] 精液残留设置"%

	PRINTFORM %"自机孕补丁", 15%：
	IF SELF_PREGNANCY == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[28] 自机孕设置"%

	PRINTFORM %"烙印系统补丁", 15%：
	IF TATTO_SYSTEM == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[29] 烙印系统设置"%

	PRINTFORM %"防抢饭补丁", 15%：
	IF ASFCConfig == 0
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ELSEIF ASFCConfig == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"由战斗能力和运气决定", 25, LEFT%
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM %"不可能被抢走", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[30] 防抢饭补丁设置"%
ELSEIF PAGE == 1
	PRINTFORM %"旅店系统", 15%：
	IF FamilyHotel == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"邀请模式", 25, LEFT%
	ELSEIF FamilyHotel == 2
		SETCOLOR 0x00FF00
		PRINTFORM %"旅店模式", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[31] 旅店系统设置"%

	PRINTFORM %"睡前扫地", 15%：
	IF AUTO_SWEEP_BEFORE_SLEEP_DIM == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"询问模式", 25, LEFT%
	ELSEIF AUTO_SWEEP_BEFORE_SLEEP_DIM == 2
		SETCOLOR 0x00FF00
		PRINTFORM %"静默模式", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[32] 睡前扫地设置"%

	PRINTFORM %"永琳通模式", 15%：
	IF IS_A_DOCTOR == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[33] 永琳通模式设置"%

	PRINTFORM %"五重绝顶魔法", 15%：
	IF Five_Orgasm_Magic == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"正常模式", 25, LEFT%
	ELSEIF Five_Orgasm_Magic == 2
		SETCOLOR 0x00FF00
		PRINTFORM %"EX模式", 25, LEFT%
	ELSEIF Five_Orgasm_Magic == 3
		SETCOLOR 0x00FF00
		PRINTFORM %"EX-PANI模式", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[34] 五重绝顶魔法设置"%

	PRINTFORM %"一分钟人模式", 15%：
	IF One_Minute_Man == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[35] 一分钟人模式设置"%
	
	PRINTFORM %"眼罩强化", 15%：
	IF Eye_mask_Blessing == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[36] 眼罩强化设置"%
	
	PRINTFORM %"非就寢時段存檔", 15%：
	IF Real_Time_Save == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[37] 非就寢時段存檔设置"%
	
	PRINTFORM %"自动修伞", 15%：
	IF Automatic_umbrella_repair == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"购买模式", 25, LEFT%
	ELSEIF Automatic_umbrella_repair == 2
		SETCOLOR 0x00FF00
		PRINTFORM %"TPS模式", 25, LEFT%
	ELSEIF Automatic_umbrella_repair == 3
		SETCOLOR 0x00FF00
		PRINTFORM %"作弊模式", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[38] 自动修伞设置"%

	PRINTFORM %"地点导航补丁", 15%：
	IF LOCATION_NAVIGATION == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[39] 地点导航设置"%
	
	PRINTFORM %"双重play", 15%：
	IF Double_Play == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[40] 双重play设置"%
	
	PRINTFORM %"禁止自慰", 15%：
	IF NO_ONANISM == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[41] 禁止自慰设置"%

	PRINTFORM %"疯狂木曜日", 15%：
	IF CRAZY_THURSDAY == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[42] 疯狂木曜日设置"%
	
	PRINTFORM %"持续同行", 15%：
	IF Always_Following == 1
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[43] 持续同行设置"%

	PRINTFORM %"加速怀孕和育儿", 15%：
	IF GETBIT(ACCELERATE_PREGANCY_AND_RAISING_CHILD, 0)
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[44] 加速怀孕和育儿设置"%

	PRINTFORM %"透视图优化", 15%：
	IF GETBIT(Xray_display_mode, 0)
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[45] 透视图优化设置"%

	PRINTFORM %"开锁系统", 15%：
	IF LOCKPICK_SYSTEM > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[46] 开锁系统设置"%

	PRINTFORM %"更多路人补丁", 15%：
	IF MORE_RANDOM_CHARACTER > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[47] 更多路人设置"%

	PRINTFORM %"摄影系统补丁", 15%：
	IF 摄影系统开关 > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[48] 摄影系统设置"%

	PRINTFORM %"催眠系统补丁", 15%：
	IF 催眠系统开关 > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[49] 催眠系统设置"%

	PRINTFORM %"诱导逆推补丁", 15%：
	IF 诱导逆推_开关 > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[50] 诱导逆推设置"%

	PRINTFORM %"睡一分钟补丁", 15%：
	IF OneMinSleep > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[51] 睡一分钟设置"%

	PRINTFORM %"玩家立绘补丁", 15%：
	IF 玩家立绘开关 > 0
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[52] 玩家立绘设置"%

	PRINTFORM %"玩家喜好补丁", 15%：
	IF PreferMaster && PfBad+PfGood!=""
		SETCOLOR 0x00FF00
		PRINTFORM %"已启动", 25, LEFT%
	ELSEIF PreferMaster
		SETCOLOR 0x994C00
		PRINTFORM %"未设定", 25, LEFT%
	ELSE
		SETCOLOR 0x994C00
		PRINTFORM %"未启动", 25, LEFT%
	ENDIF
	RESETCOLOR
	PRINTFORML %"[53] 玩家喜好设置"%	
	
ENDIF