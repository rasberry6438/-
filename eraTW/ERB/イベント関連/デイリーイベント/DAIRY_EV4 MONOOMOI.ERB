@DAILY_CONDITION_EV4

SIF RAND:7 > 0
	RETURN -1
CALL DAILY_EV4
RETURN 1

@DAILY_EV4
CALL FISHER_YATES_SHAFFLE(CHARANUM - 1)
FOR LOCAL:10, 0, CHARANUM - 1
	LOCAL = SHAFFLE_ARRAY:(LOCAL:10) + 1
	IF ABL:LOCAL:親密 >= 5 && !CFLAG:LOCAL:神社在住
		CALL KOJO_MESSAGE_SEND("DAILY", 4, LOCAL)
		CFLAG:LOCAL:翌日来訪 = 1
		BREAK
	ENDIF
NEXT

@DAILY_MESSAGE_4(ARG, ARG:1, ARG:2, ARG:3, ARG:4, ARGS:1, ARGS:2)
RESETCOLOR
;二重表示抑制
IF GETBIT(DOUBLE_MESSAGE_BLOCKED:ARG, 10)
	CALL SET_KOJO_COLOR
	RETURN
ENDIF
SETBIT DOUBLE_MESSAGE_BLOCKED:ARG, 10
PRINTFORMDW 結束了思考的%NAME_OUTPUT_TRANSLATION("CALLNAME", ARG)%、注意到自己會在不知不覺中就想著%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%
PRINTFORMDW %NAME_OUTPUT_TRANSLATION("CALLNAME", ARG)%決定明天去見%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%
PRINTL 
CALL SET_KOJO_COLOR()
